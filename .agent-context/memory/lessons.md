# Lessons Learned

<!-- Format: - **[scope]** What happened — what we learned `conf:high|med|low` (YYYY-MM-DD) -->
<!-- conf:high = proven 3+ times | conf:med = observed once/twice | conf:low = suspected -->
<!-- The date enables staleness tracking. Always include it. -->

- **[security/xss]** G-C SEC-11 audit completed (PR security/sec11-vhtml-owasp) — only 3 `v-html` call sites exist (`AgentChatStream.vue` ×2, `RefinementChat.vue` ×1), all rendering user-controlled markdown via the SSOT `src/utils/markdown.ts` chokepoint which already pipes through DOMPurify. New `src/composables/useSafeHtml.ts` covers any future direct-HTML sinks. OWASP A03: all DB access is parameterized (incl. FTS5 search via quoted-token sanitizer in `server/internal/api/search/handler.go`). OWASP A07: JWT/OAuth-state cookies are `HttpOnly`, `Secure` (non-loopback), `SameSite=Strict|Lax`; `DASHBOARD_JWT_SECRET` requires ≥32 chars. OWASP A08: plugin loader `server/internal/plugin/registry.go` is wildcard — loads any directory containing a valid `plugin.json` with no enabled-list or hash pin; loopback-bind + plugin-id regex are the current safeguards. Documented in PR body; hardening deferred to a follow-up. `conf:high` (2026-05-24)
- **[roadmap-audit]** Greping the codebase for a feature's *spec-language* (e.g. `quota_period`, `QuotaChip`, `Identity*`) misses implementations that landed under different names. Before writing a spec against a "missing" roadmap item, also grep for the *behavior* (e.g. `/api/quota`, `useAgentIdentity`, `tokensUsed`) and inspect the components that would consume it (`App.vue` header, `AgentRow.vue`). G2 (IP-1) and G3 (CI-8) specs both proposed work already shipped — wasted spec/plan/agent time. `conf:med` (2026-05-22)
- **[frontend/lint]** `pnpm lint` runs ESLint repo-wide and is **chronically red on toml/yaml config files** (antfu config's `toml/*` + `yaml/*` plugins). That noise masks real lint errors in changed source files — implementers ran `pnpm lint`, saw failures, assumed all were pre-existing, and committed lint-dirty `.vue`/`.ts`. Always lint scoped to changed paths: `pnpm exec eslint <paths>`. `conf:high` (2026-05-30)
- **[frontend/vue]** The ESLint config enforces **camelCase custom event names** (`vue/custom-event-name-casing`). Kebab-case emits (`'open-sessions'`) fail lint; declare/emit `openSessions` instead. Template listeners may stay kebab (`@open-sessions`) — Vue normalizes them. The rule flags the `emit()`/`$emit()` call + `defineEmits` key, not the listener attribute. `update:xxx` v-model events are exempt when already camelCase. `conf:high` (2026-05-30)
- **[backend/auth]** Default auth mode is `DASHBOARD_AUTH=none` (`config.go` default) → `BypassAuth=true` → `RequireAuth` middleware is **skipped**, so **no JWT payload is ever placed in the request context**. Handlers must NOT re-gate on `auth.PayloadFromContext()` and 403 when absent — that breaks every route in the default (bypass) config. Auth is enforced at the middleware layer only; handlers trust it (`payload, _ :=`, never `if !ok { 403 }`). The refine handlers (`api/refine/handler.go`) had this bug → all 3 routes 403'd in bypass mode. Fixed across the board (2026-05-30): refine + wphandler dropped the dead guards (payload discarded); `search`/`remotes`/`history` substitute `auth.BypassPayload()` (SSOT helper in `internal/auth/jwt.go`, `Sub="local"`/`IsAdmin=true`, matching `/api/me` via `auth.BypassUserID`) on `!ok` instead of 403 — needed because they key per-user data on `payload.Sub`. `presets` was already correct (`if ok { userID=&Sub }` → nil = global in bypass; deliberately left). **Invariant making the substitution safe:** `!ok` can ONLY occur in bypass mode — RequireAuth 401s unauthenticated requests before any handler runs in auth mode — and all these handlers are mounted inside the protected `r.Group`, so the local-admin fallback can never grant an unauthenticated request access. **Regression guard:** `server/internal/api/bypass_auth_smoke_test.go` (`TestBypassAuth_NoProtectedRouteReturnsAuthError`) builds the full production router in `BypassAuth=true` against in-memory SQLite, `chi.Walk`s EVERY registered route, fires an unauthenticated request (loopback Host + matching Origin), and asserts none return 401/403 except an explicit `bypassSkip` allow-list (OAuth dance, bearer-auth ingress `/api/hooks/event|pre-tool` + `/api/mcp` + `/api/channel-reply`, SSE streams, `DELETE /api/me`). New routes are covered automatically. NOTE: the test sets a huge `AuthRateLimiterConfig` — without it the per-IP limiter (burst 20) 429s the 100+ rapid requests and masks the 401/403. When adding a new bearer-token route OUTSIDE the JWT group, add it to `bypassSkip`. `conf:high` (2026-05-30)
- **[backend/hooks]** `/api/hooks/*` has **two consumer classes with different auth**: hook-script ingress (`event`, `pre-tool`) carries `DASHBOARD_HOOKS_SECRET` (bearer) and mounts OUTSIDE the JWT group; the edit-gate UI (`pending` GET, `respond` POST) is called by the **browser** (`EditGateModal.vue`) with the session cookie, so it must mount INSIDE the protected JWT group and must NOT call `requireSecret` — otherwise the browser gets 401. Don't lump all hooks routes under one auth scheme. `conf:high` (2026-05-30)
- **[backend/rewrite-contracts]** The Go server rewrite silently changed several HTTP/SSE contracts the Vue frontend still expected in the old TS shape — and only the changed side was tested, so the divergence shipped green. Found in refine (2026-05-30): (1) `GET /api/refine/{id}/turns` now returns a **bare array**, but the frontend expected `{turns, isProcessing}` → `data.turns` undefined → "Failed to load history" on every fresh ticket; (2) `claude -p` emits **plain text** forwarded as `data: <line>`, but `sendMessage` did `JSON.parse` per frame and dropped non-JSON → assistant reply rendered blank. When auditing rewritten endpoints, **diff both sides of the wire** (Go handler response shape + JSON tags vs the TS `as SomeType` cast and SSE frame parsing), not just one. Tests that assert only the producer's shape (the Go `var x []any` unmarshal) give false confidence. Guarded by `src/composables/__tests__/useRefinementChat.test.ts`. STILL OPEN: refine phase tracking — backend never emits `event: phase_change` frames and `submitTurn` stores turns without `Phase`, so the phase stepper UI never advances during/after a refinement. `conf:med` (2026-05-30). THIRD manifestation (2026-06-01): the same `sendMessage` SSE parser **dropped all line breaks** — backend emits one `data:` frame per source line (bufio.Scanner strips the `\n`), but the client appended frame contents with no separator AND read only the first `data:` line per frame → markdown collapsed into one unformatted blob (headings/tables/lists dead). The **persisted** turn was fine (`runner.go` rebuilds with `sb+"\n"`), so it rendered correctly after reload — classic "broken live, fine after refresh" tell. Fixed: iterate every `data:` line, re-insert `\n` between lines (empty line → paragraph break), strip only one SSE space (keep markdown indentation). Guarded by `useRefinementChat.test.ts` line-break cases. `conf:high`. FOURTH + the big one (2026-06-01): the **refinement→pipeline concept handoff was never implemented** in the Go rewrite. `confirm()` (`api/refine/handler.go`) wrote nothing to `task.Metadata`/`Title`/`SourceBranch`, but `implementationBuilder` (`pipeline/stage_handlers.go:225`) reads the concept EXCLUSIVELY from `task.Metadata` → empty `{}` → implementation agent refuses ("No implementable task provided. Concept block was empty ({})"), title stays placeholder "New Task", user's branch/worktree wish ignored. The concept lived only in `refinement_turns`, never bridged. Also the refine prompt (`refine/spawner.go`) never asked the agent for a structured concept. Fix: (a) prompt now requires a fenced ```json concept block at approval (`refinedTitle`/`spec`/`plan`/`toolRequests`/`sourceBranch`/`targetBranch`) — note the Go backtick raw-string template can't contain a ``` fence, so the schema is a double-quoted `conceptSchemaInstruction` const concatenated in; (b) new `refine.ExtractConcept(turns)` (`refine/concept.go`) parses the LAST json block from the newest non-`confirmed` assistant turn; (c) `confirm()` persists it in ONE `UpdateTaskInput` — routing keys (refinedTitle→Title, sourceBranch→SourceBranch which auto-triggers `progress_guards.go` worktree creation, targetBranch→TargetBranch) lifted onto columns, the rest into Metadata. Frontend hides the json block from the bubble (`JSON_BLOCK_STRIP_RE` in `RefinementChat.vue` `cleanContent`) while still parsing it for the title. Guarded by `refine/concept_test.go` + `TestConfirm_PersistsConceptOntoTask`. `conf:high`
- **[git/danger]** NEVER run `git stash` followed by `git stash pop` to "temporarily" shelve changes when the working tree might be clean. `git stash` on a clean tree saves NOTHING ("No local changes to save"), so the subsequent `git stash pop` applies an unrelated OLD stash from the stack (this repo has 38+ leftover stashes) onto your tree — instant merge conflicts in random files. Happened 2026-05-30: a speculative stash/pop around a `git show` applied `refactor/unify-audit-events: follow-up agent leftovers` and conflicted di.go/handler.go/client.go. Recovery: committed work is safe in HEAD; `git checkout HEAD -- <conflicted files>` restores them (scoped, passes the destructive-action gate that blocks `git reset --hard`); the popped stash stays in the list (a conflicted pop does not drop it), so nothing is lost. To inspect a file at another commit use `git show <ref>:<path>` directly — no stash needed. `conf:high` (2026-05-30)
- **[frontend/architecture]** Layout redesign (Plan 1) shipped a calm app-shell on branch `feat/layout-redesign`: `src/components/shell/` (AppShell, AppSidebar, NavItem, SidebarFooter, AppTopbar, AppStatusBar, DashboardToolbar, LivePulse, SkeletonCard, ChannelScriptCallout) + composables `useViewState`/`useSidebar`/`useStatusBar` + `utils/navConfig.ts`. The old `viewMode` string split into `activeView`+`dashboardLayout` (legacy `agent-view-mode` localStorage key auto-migrated). `SystemMetricsPanel.vue` + `ResourceBar.vue` deleted as duplicates (metrics now in AppStatusBar). Calm palette + indigo `--accent` token in `main.css` — every component reads tokens so the palette is a one-file lever. Plans 2-4 (per-component restyle fan-out, TaskModal decomposition, D3 palette + full blue→accent sweep) still pending. Spec: `docs/archive/specs/2026-05-30-layout-redesign-design.md`. `conf:high` (2026-05-30)
- **[backend/agent-monitor]** Monitor showed **identical session content for every Claude process in the same folder**. Root cause: `parser.FindSessionForProject` resolved sessions by **directory only** — `sessionCacheKey{cwd, configDir}` + `EncodePath(cwd)` projectDir, and `findSessionByContent` returned the newest-mtime JSONL for *all* same-folder PIDs (its `LastActivity < uptime+10s` filter is a per-process *plausibility* check, never an *identity* check). No PID→session binding existed anywhere; `merger.GetAgents` parallel-resolved each proc independently with no cross-proc distinctness. **Key non-discoverable fact:** Claude Code writes an authoritative PID→session map at `~/.claude/sessions/{pid}.json` = `{"pid","sessionId","cwd","status","entrypoint","version",...}` — but coverage is partial (only some live PIDs have one; older/resumed sessions may lack it). claude does NOT hold the `.jsonl` open (appends-and-closes) so lsof-fd mapping is useless. Fix (2026-06-01, branch `feat/layout-redesign`): layered per-PID resolver `parser.ResolveSessionForProcess(SessionRequest, claimed map)` in new `parser/pid_session.go` — precedence (1) `sessions/{pid}.json` → sessionId, (2) `--resume`/`--session-id` arg (`SessionIDFromArgs`), (3) dir mtime heuristic excluding the `claimed` set. `sessionCacheKey` gained a `pid` field; `findSessionForProjectFiltered(cwd,pid,uptime,cfg,claimed,forcedID)` is the new cache-backed core (`FindSessionForProject` is a `pid=0,forcedID="",claimed=nil` wrapper). Scanner switched `ps -eo pid,etime,comm` → `...args` so `--resume <id>` survives (`DetectProviderFromCommand` still strips to argv[0]). `merger.GetAgents` now **groups procs by (encoded cwd, configDir), resolves each group sequentially sharing one `claimed` map** (distinctness guarantee) while running groups in parallel (throughput); youngest proc resolved first so it claims the freshest session in fallback. Guarded by `parser/pid_session_test.go` (DistinctPerPID + FallbackDistinct regression tests). Trade-off accepted: a just-started agent whose `sessions/{pid}.json` names a not-yet-written `.jsonl` is skipped (shows nothing) until first write — correct, vs. previously showing a wrong session. `conf:high`
- **[frontend/refine]** Two RefinementChat reconnect bugs fixed (branch `feat/layout-redesign`): (1) **create-flow clobber** — `RefinementChat.vue`'s `watch([open, currentTask.id], loadHistory)` fired on the `null→newId` transition during task creation; `GET /turns` returns `[]` for a fresh task → `messages=[]` wiped the just-sent user message → the empty-state ("What would you like to build?") project-picker modal stayed stuck instead of switching to the chat view. Fix: `justCreatedId` guard in the component skips loadHistory for a locally-created id (sendMessage is the source of truth). (2) **no detached-run feedback on reopen** — the backend `refine.Runner` runs **detached** (survives client disconnect), persists the assistant turn **only at completion** (`runner.go`), and rejects a 2nd POST with `ErrAlreadyRunning` (no replayable live stream); progress is observable ONLY via `GET /api/refine/{id}/status`. The frontend never polled it → reopening mid-run showed just the user turn, frozen. Fix: `useRefinementChat` gained `runStatus`/`syncStatus`/`stop` — on (re)open the component polls `/status`, shows the working dots while `running`, reloads turns when it flips `done`. Also removed the composable's `watch(taskId)` reset (it raced the create flow's synchronous `isStreaming`/`runStatus` set) — `loadHistory` now resets phase state self-contained; `sendMessage` aborts via `AbortController` on close. **General rule:** when the backend exposes a detached-run + `/status` reconnect surface, the client must consume it — a one-shot history GET on open is not enough. Guarded by `RefinementChat.createflow.test.ts` + `useRefinementChat.test.ts` syncStatus/stop cases. `conf:high` (2026-06-01)
- **[frontend/d3-viz]** Two bugs broke the entire Workflows tab (visualizations) once the timestamp-400 was lifted (2026-06-02, branch `feat/layout-redesign`): (1) **blank charts** — all 4 viz components (`SankeyChart`/`SessionDagChart`/`SpawnTreeChart`/`CoOccurrenceMatrix`) used `watch(() => props.data, render, { immediate: true })` with the `<svg>` behind a `v-else`/`v-else` branch that only mounts once data is non-empty. The default (pre-flush) watcher fires BEFORE the DOM patch, so on first data arrival `svgRef.value` is still null → `render()` early-returns → permanently blank. Fix: `flush: 'post'` on all 4 watchers so render runs after the svg mounts. **Rule:** any imperative-render watcher that touches a template ref or `clientWidth` needs `flush:'post'` (or `nextTick`), especially when the ref is behind `v-if/v-else`. (2) **`Uncaught Error: circular link`** from `d3-sankey` — it only lays out DAGs, but `BuildSankey` (`analytics/visualizations.go`) emitted every `prev→curr` tool transition including self-loops (Read→Read) and bidirectional pairs (Read⇄Edit) → cycles → throw. Fix (backend, user chose DAG over `d3-sankey-circular`): skip self-loops while counting; new `acyclicSankeyLinks(map[[2]string]int)` collapses each bidirectional pair to a single **net-flow** edge (larger−smaller, equal→drop) then greedily removes back-edges (`firstBackEdgeIndex`, DFS gray-node detection, deterministic via pre-sorted links) until acyclic; nodes derived from surviving links. Frontend defense-in-depth: `SankeyChart.render` wraps the layout in try/catch → `renderError` ref shown as a message; the `<svg>` stays mounted (sibling of the error div, NOT gated behind it) so recovery re-render doesn't re-hit the null-ref mount race. Guarded by `analytics/visualizations_test.go` (net-flow/equal-drop/3-cycle-break/linear) + `handler_test.go` datetime-local cases. `conf:high` (2026-06-02)
- **[backend/session-parser]** `buildSessionInfo` (`server/internal/parser/sessions.go`) coupled cwd-capture to model-presence: `if m, cwd := extractHeadInfoRaw(headRaw); m != "" { projectPath = cwd }` — so when the first `model` sat past the 8KB head window (sessions opening with large attachments/prompts), the cwd (present on EVERY jsonl line) was discarded and ProjectName fell back to the raw encoded dir (`-Users-…`), plus CostEstimate stayed 0 (no model). Symptom surfaced app-wide: `/api/sessions` (and the new Workflows spawn-tree) showed encoded paths instead of real project names. Fix (2026-06-02, branch `feat/layout-redesign`): decouple — set `projectPath` from cwd whenever cwd != "" regardless of model; and recover model from the TAIL read (model is stable within a session) when the head missed it. Now ProjectName = `filepath.Base(realCwd)` (e.g. `agent-dashboard`) and cost/model populate when tokens exist (token counts come from the `session-meta/{id}.json` sidecar, which is often absent on older sessions → cost legitimately 0 there). Guarded by `sessions_test.go::TestBuildSessionInfo_CapturesCwdWithoutModel` + `_RecoversModelFromTail`. Lesson: when extracting multiple independent fields from one scan, never gate field B's assignment on field A being found. `conf:high`
- **[backend/cost-analytics]** Cost Analytics view (`/api/cost/summary` → `agent_cost_trends`) was permanently empty. Root cause was NOT wiring — the history importer (`server/internal/history/importer.go`) carries its **own** minimal JSONL parser (`parseTokensFromRaw`, distinct from the canonical `parser` package = SSOT violation) that filtered `type=="message"`, but Claude Code writes the token-bearing assistant turns with top-level **`type:"assistant"`** (nested `message.usage`). So it extracted 0 tokens from every modern session → `extractCostRow` returned nil → 0 rows. The unit tests used synthetic `type:"message"` fixtures encoding the same wrong assumption, so they passed green while production stayed empty — only a real-DB query (`sqlite3 ~/.claude/dashboard-tasks.db "SELECT count(*) FROM agent_cost_trends"`) exposed it. Fixed (2026-06-02, branch `feat/cost-auto-import`): match on `message.role=="assistant"` and accept `type` ∈ {`assistant`,`message`} (excludes tool_result/attachment). **Rules:** (1) when a feature reads on-disk formats, verify the parser against a REAL file, not just fixtures; (2) the two JSONL parsers (parser pkg + history importer) are a latent SSOT risk — prefer consolidating. The table is now filled by a server-side scan at boot + every `DASHBOARD_COST_SCAN_INTERVAL_MS` (default 300000); agents never write it. Multi-account/provider coverage comes from `parser.AllAgentConfigDirs()` (honours `DASHBOARD_CLAUDE_CONFIG_DIRS`) — default scans only `~/.claude`. `conf:high` (2026-06-02)
- **[backend/ent-codegen]** ent's `OnConflict`/upsert builders are **opt-in** via `--feature sql/upsert` in the `//go:generate` directive (`server/internal/db/ent/generate.go`); without it `.OnConflictColumns()` doesn't exist on the generated `*Create`. Also: this ent version only emits single-row upsert (`*XCreate.OnConflict…` → `XUpsertOne`), **NOT** bulk (`*XCreateBulk` has no `OnConflict`) — upsert N rows by looping single Creates inside one `client.Tx`. `XUpsertOne.UpdateNewValues()` refreshes all mutable cols except the immutable `id` (so it preserves the existing row id), but a field must be **mutable** (drop `.Immutable()`) to be refreshed on conflict. Regen gotcha: run ent codegen with `GOWORK=off go generate ./internal/db/ent/...`; the running `task dev`/`air` server + go test result-cache can mask codegen state — confirm with `grep -c OnConflict <file>` before trusting a build. And NEVER read `$?` after a pipe (`go build ... | head`) — the exit code is the tail command's, not the build's; chain with `&&` instead. `conf:high` (2026-06-02)
- **[cost-analytics/v2]** Cost Analytics v2 (branch feat/cost-auto-import) added tokens, per-project grouping, time-range filter, and unified the footer. Three non-obvious findings: (1) **recorded_at seed bug** — `parseTokensFromRaw` seeded lastActivity to now-24h and only took timestamps `After` it, so every session >24h old collapsed onto "yesterday"; fix = seed zero time, fall back to file mtime. (2) **tail-read undercounts cost ~10×** — the importer tail-read 32KB; switching to full-file read raised this repo's tracked spend from ~$261 to ~$6k across history. Full read + a `source_mtime` skip-unchanged guard keeps rescans cheap. (3) **the status-bar `COST 3m` was dead** — Go `agentbroadcast/loop.go` always sends `emptyTrend`, so frontend `costDelta` was permanently null. Fix was client-side: `useAgents` now keeps its own time-based `{t,cost,tokens}` ring buffer and App.vue computes a 5-min burn delta; "today's spend" comes from the shared `/api/cost/summary?from=to=today` (new `useTodayCost`). Project grouping: `services.CostProjectResolver` maps session cwd → dashboard ProjectFolder (longest path prefix) → git repo root (`--git-common-dir`, worktrees collapsed) → cwd basename, cached per cwd, injected into the importer. `conf:high` (2026-06-02)
- **[build/dist-commit-policy]** `server/frontend/dist/` is build output, NOT hand-committed. `.gitignore` has `server/frontend/dist/*` + `!server/frontend/dist/index.html` — only the placeholder `index.html` is force-tracked so `go:embed` compiles without a prior `pnpm build`. A few siblings (`sw.js`, `favicon.ico`, icons, `offline.html`, `registerSW.js`, `manifest.webmanifest`) are pre-gitignore leftovers still tracked. When you change frontend source, **commit `src/` only** — do NOT run `pnpm build` and commit the regenerated `dist/index.html`/`sw.js`; `task build` regenerates dist at build time. Real hashed JS bundles under `dist/assets/` are untracked. Consequence: the embedded SPA served on `:13120` is STALE until `task build`; to SEE a frontend change use `task dev` (Vite HMR on `:5173`) or rebuild. `conf:high` (2026-06-05)
- **[backend/channel-discovery]** Live prompt injection broke after `agent-dashboard live` was made to load the channel MCP on the no-tmux pty path. Root cause: **two discovery writers collided on one filename**. `ptyhost.go writePtyDiscovery` keyed the file by the spawned claude's `childPid`; `bridge.go writeDiscovery` keyed by `os.Getppid()` = the SAME claude pid → both wrote `~/.claude/dashboard-channel/{pid}.json`. The bridge boots ~1s after ptyhost (claude must start before it loads the MCP) so it OVERWROTE ptyhost's file, dropping `ptyInject:true` and replacing the pty inject-port with the bridge reply-port. `merger.channelDiscovery` then computed `liveInjectable=false` → frontend `useAgentPrompt.ts` took the `else` branch and **resumed** the session as a new process instead of injecting (the amber ⤳ behaviour — resume is the SYMPTOM of a broken inject, not a separate bug). Fix (commit b919b39): ptyhost writes a SEPARATE `{pid}.pty.json`; `merger.channelDiscovery` + `SendMessageToChannel` read BOTH files (channelAvailable if either exists; liveInjectable if bridge.tmuxPane OR pty.ptyInject; delivery precedence tmux→pty-port→bridge-port). Lesson: when two independent processes write per-pid sidecar files, NEVER key them by the same id+name — give each writer a distinct suffix. Also: a running process keeps its OLD code — after fixing a spawner/broker, the user must RELAUNCH the live session; rebuilding the binary is not enough. `conf:high` (2026-06-05)
- **[backend/channel-auth]** The channel bridge uses **two different tokens in two directions**, and the live bridge is the **Go** `server/internal/channel/bridge.go` — NOT the `channel/dashboard-channel.ts` stub (legacy/unused at runtime). OUTBOUND (agent→dashboard: `dashboard_reply`, `request_permission`, and the new `set_stage_output`) goes through `callDashboard(dashboardURL, mcpToken, …)` where `mcpToken = os.Getenv("DASHBOARD_MCP_TOKEN")` = the global `config.MCPToken` (di_pipeline.go) — the SAME secret `/api/mcp` validates via `api_keys` SHA-256 hash (`mcp.HashToken` + `ApiKeyRepo.GetByHash`). INBOUND (dashboard→agent HTTP server, `checkBearer`) uses `httpToken := generateToken()`, the ephemeral per-PID value written into the discovery file `~/.claude/dashboard-channel/{parentPid}.json` `"token"` field. **So `validateChannelToken` (discovery-file token) is for INBOUND only.** `channel-reply` reusing it for an OUTBOUND call was a latent mismatch (fixed PR #129 — channel-reply now uses api_keys too). A new outbound stage-agent endpoint MUST auth via `api_keys` (like `/api/mcp`), because that is the token the bridge actually sends. Discovered building `/api/channel-stage-output` (PR #127): the first cut copied `validateChannelToken` and the unit tests were GREEN-BUT-MEANINGLESS — they wrote a discovery file with the same token they sent, validating a mechanism the real bridge never uses; every real `set_stage_output` would have 401'd. **Rule: when two processes agree on a shared secret, a unit test that mocks BOTH sides proves internal consistency, not integration correctness — trace the actual token flow end-to-end.** Also: `set_stage_output` jsonschema struct tags must be plain-description form (`jsonschema:"desc"`), never `key=value` (see [[lesson_channel_bridge_jsonschema_tag]] — `key=value` panics AddTool, silently disabling the channel for ALL agents); `TestRegisterTools_NoPanic` guards this. `conf:high` (2026-06-09)
- **[tooling/gofmt-skew]** Subagents running `task fmt` (or `gofmt -w`) can reformat unrelated files repo-wide when the local Go/gofmt version aligns struct-tag whitespace or blank lines differently from the repo's last format — surfaced as ~30 modified files with equal +/- counts after a feature task (PR #127). It is pure formatting (verify with `git diff -w HEAD` → only blank-line removals / tag realignment remain) but pollutes a feature PR. Keep feature commits scoped; discard the churn (`git restore .`) or land a repo-wide gofmt in its OWN commit. **`main` itself drifts gofmt-dirty** (CI does not enforce `gofmt`): `gofmt -l server sdk | grep -v /db/ent/` found 32 dirty files on main (2026-06-14) → landed as standalone `chore: normalize gofmt` PR #154 via fresh `gofmt -w` off `origin/main` (NOT replaying stale worktree edits). `conf:med` (2026-06-09)

- **[git/gone-branch-cleanup]** Cleaning `[gone]`-upstream branches + worktrees: the `clean_gone` skill's blind `git branch -vv | grep '\[gone\]'` script has two traps in this repo. (1) **Squash-merge breaks ancestry** — merged PRs re-write history, so `git merge-base --is-ancestor <branch> origin/main` reports merged branches as UNMERGED. Do NOT gate deletion on ancestry; authoritative merged signal is `gh pr list --head <branch> --state all` (GitHub auto-deletes the remote on merge → `[gone]`). (2) **Mis-tracked upstreams** — branches whose upstream is `origin/upcoming` (now deleted) show `[gone]` for a reason UNRELATED to their own PR; e.g. `feat/ci8-quota-tracking` had NO PR + 4 own commits and the blind script would `branch -D` it, orphaning the work. Always: per gone-branch run `gh pr list --head` (MERGED = safe), and before removing a worktree diff the working tree vs its OWN HEAD (`git -C <wt> status --porcelain` + read `git diff`) — worktree HEADs are often ancient so "diff vs main" lies (shows huge phantom deletions); salvage real uncommitted work, discard junk (`go.work.sum`, compiled binaries, `coverage.out`). Use explicit branch lists piped `printf '%s\n' … | while read` — zsh `for b in $VAR` does NOT word-split. Keep locked `.claude/worktrees/*` (dashboard owns them). `conf:high` (2026-06-14)

## dist/.gitkeep is load-bearing for go:embed (2026-06-08) conf:high
`server/frontend/dist/.gitkeep` MUST stay tracked. `server/frontend/embed.go` uses `//go:embed all:dist`; a fresh CI checkout has no `pnpm build` output, so `.gitkeep` is the only file keeping `dist/` present. Delete it → `pattern all:dist: no matching files found (typecheck)` → Lint(server) + Test(server) CI fail. Local lint/test pass because local dist holds full build output, masking the break. Never judge a `.gitkeep` under an embedded dir as "stale" — read the embed directive first.
- **[test/sqlite-memory]** `db.Open(":memory:")` (`server/internal/db/client.go`) opens `file::memory:?mode=memory` WITHOUT `cache=shared` → SQLite gives each pooled connection its OWN empty database, so `client.Schema.Create` migrations on one connection are invisible to queries that land on another. Symptom: a handler test 500s with `SQL logic error: no such table: stage_runs` even though setup-time `repo.Create` on the same `*ent.Client` succeeded (setup reused the warm conn 0; the HTTP handler — especially when driven in a goroutine — grabbed a fresh empty conn). **The `bypass_auth_smoke_test` MASKED this for ages** because it only asserts routes don't return 401/403 — a 500 from a missing table passes. Fix (PR #128): `sqlDB.SetMaxOpenConns(1)` for the `:memory:` path so the whole pool shares one in-memory DB (test-only; file-backed DBs unaffected). **Rule:** an integration test that asserts a handler SUCCEEDS (not just "isn't 401/403") must run against a DB whose tables are actually reachable from the request's connection — pin `:memory:` to one connection, or use `cache=shared`. `conf:high` (2026-06-09)
- **[backend/inject-hardening]** The live-injection chain has THREE independent auth/abuse surfaces, hardened together (branch `sec/hardening`): (1) `POST /api/agents/{pid}/message` now has its OWN per-user sliding-window limiter (`inject_rate_limit`/`inject_rate_window_ms`, default 30/60000) separate from the spawn limiter — both share the extracted `slidingWindowLimiter` in `server/internal/api/agents/ratelimit.go` (DRY). (2) Each injection writes an `audit_event` row (`live_inject`/`live_inject_rejected`) with metadata ONLY (`transport`,`msgLen`,`sha256[:12]`,`outcome`) — never the raw body. (3) The inbound discovery bearer token in `bridge.go`+`ptyhost.go` now ROTATES (`DASHBOARD_INJECT_TOKEN_ROTATE_MS`, default 300000ms, `<=0` disables) via a shared `rotatingToken` (atomic current + one-rotation grace slot, constant-time compare) — the dashboard needs no change because `SendMessageToChannel` re-reads the discovery file per delivery, and the grace window covers the read-then-rotate race. Also: `sanitizeInjectMessage` strips C0/DEL/CR/LF (keeps tab) at the source, killing the embedded-`\r` premature-submit vector before send-keys/pty write. Note Sec-P4-1 (dompurify `>=3.4.0`) was ALREADY satisfied in base (commit e352af7, resolves 3.4.9) — no change needed. `ttl:90d source:discovered conf:high` (2026-06-16)
- **[tooling/gpgsign-test-helper]** Global git `commit.gpgsign=true` + a passphrase-locked SSH signing key makes the pipeline worktree test helper (`pipeline/worktree_test.go` `initRepoWithCommit` → `git commit --allow-empty -m init`) fail with `exit status 128` ("failed to write commit object"), which surfaces as a SKIP/FAIL that looks like a worktree-code regression but is purely environmental. Same blocks `git commit` for landing work. Fix: inject per-invocation config via env so no global change is needed — `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false task test` for the suite, and `git -c commit.gpgsign=false commit …` for commits (yields unsigned commits on the feature branch — user re-signs on merge if needed). `conf:high` (2026-06-12)
- **[pipeline/terminal-worktree-cleanup]** Tasks must release their git worktree when they reach a terminal stage, else `source_branch` stays checked out and `CountActiveBySourceBranch` (non-terminal-only) can't see it → a new task on that branch fails worktree creation (visible failed run since #201). Pattern: `OrchestratorOptions.RemoveWorktreeFn` seam (mirrors `EnsureWorktreeFn`); `cleanupTerminalWorktree` force-removes + clears `WorktreePath` + audits `worktree_removed`. CRITICAL: worktree removal does a DB write (`ClearWorktreePath`); modernc/sqlite is single-writer, so calling it inside the still-open `applyTransition` tx DEADLOCKS. `done` cleanup MUST run AFTER `tx.Commit()` (added `afterCommitTerminalCleanup`), NOT in the misleadingly-named `postCommit` slice which actually executes inside the tx. `cancel` goes through `NotifyTaskTerminated` (outside any tx → safe, covers REST+MCP). Defense-in-depth: git-authoritative preflight `worktree.BranchCheckedOutAt` (parses `git worktree list --porcelain`) in `ensureTaskWorktree` + create/refine confirm catches leftover/manual worktrees the task table can't. `conf:high` (2026-06-20)
- **[frontend/sse-prop-identity]** Any component that keeps LOCAL user state (selections, typed text) while being fed from the SSE agent payload must reset that state on a CONTENT signature, never on the prop object. `Agent.uptime` comes from `ps etime` and changes every scan, so `agentbroadcast`'s FNV frame-dedupe passes a frame every `sse.intervalMs` (default 3000), `useAgents` does `agents.value = data`, and every nested object (`pendingQuestion`, …) arrives as a fresh reference. `QuestionCard` watched the prop object → the user's answer was wiped every ~3 s ("you have to click fast"). Canonical helper: `screenSignature()` in `src/utils/askQuestionScreen.ts` (`AgentTerminal` had already solved it locally; it is now shared). `ttl:90d source:discovered conf:high` (2026-07-27)
- **[frontend/askq-screens]** AskUserQuestion has TWO answerable screens, not one. The modal (options + the `Type something` / `Chat about this` meta-rows) and — after the last question of a multi-question flow — the review/submit screen (`Ready to submit your answers?` + `1. Submit answers` / `2. Cancel`), which has NO meta-rows and is therefore rejected by `DetectQuestion` by design. Both detectors are hand-kept parity ports (`server/internal/askq/askq.go` ↔ `src/utils/askQuestionScreen.ts`, fixtures byte-identical). Gate the confirm screen on the Submit/Cancel option pair, NOT on the surrounding copy — that copy drifts between Claude Code releases and already broke meta-row detection once. Server-side both are probed in ONE capture via `merger.ScreenProbeFn` → `*sdk.PendingScreen`; the pty broker's `GET /screen` supersedes `GET /question`, which is kept as a 404-fallback because broker processes outlive a server upgrade. `ttl:90d source:discovered conf:high` (2026-07-27)
- **[test/e2e-own-port]** E2E runs on **13199** against a scratch DB under `.e2e/`, with `reuseExistingServer: false` — a foreign listener now aborts the run (`bind 127.0.0.1:13199: address already in use`) instead of being adopted. Until 2026-08-12 the config pinned 13120 with reuse on, so a running desktop app was silently used as the test server and the suite reported ITS embedded SPA — a stale build — as the branch's result. Overridable per run via `E2E_PORT` / `E2E_DB_PATH` (`scripts/e2e-server.sh`). A fresh DB opens the first-run onboarding dialog, which covers the page and eats every click; `tests/e2e/global-setup.ts` clears it with `PATCH /api/onboarding/status` (needs an `Origin` matching the host). `agent-dashboard settings set onboarding.completed true` also works — the CLI folded into the single `bin/agent-dashboard` binary on 2026-08-31; the separate `dashboard` CLI binary and its `task build:cli` target no longer exist. Remember `pnpm build` deletes `server/frontend/dist/.gitkeep` → `git checkout HEAD --` it before committing. `ttl:90d source:discovered conf:high` (2026-08-12)
- **[frontend/radio-group-name]** Never hard-code `name` on a radio inside a reusable component: `name` scopes a browser-native radio group across the whole DOCUMENT, so two instances (two agents with a question in the triage band, or a band card plus the terminal overlay) become one group and checking B unchecks A. Vue does NOT repair it — A's bound `:checked` still evaluates true, the vnode value is unchanged, nothing is patched — so the selection is visually gone while the component still holds it. Reads exactly like local state being reset, but no state was reset, so a content-signature guard does not help. Use `nextRadioGroupName()` (`src/utils/radioGroup.ts`); the counter must be MODULE scope — `<script setup>` is the per-instance setup body (a counter there restarts at 0 per component) and `useId()` counts per Vue app (plugin UI slots mount their own). Guard it by mounting TWO instances with `attachTo` and asserting the first stays checked; single-instance tests, including a real-browser Playwright run against the live server, all stay green. `ttl:90d source:discovered conf:high` (2026-07-27)
- **[desktop/embedded-spa]** The wails desktop app serves its SPA from `go:embed` inside the binary — no service worker, no browser cache, no dev server in between. A reload in that window can NEVER pick up frontend changes; only a new binary can. Identify which instance a bug report comes from before debugging: `lsof -nP -iTCP:<port> -sTCP:ESTABLISHED | grep -c ESTABLISHED` shows where a browser is actually connected, and the server request log reveals whether the new entry-bundle hash was ever fetched (compare against your own test-run count). `ps -o lstart` on the running app plus the binary's mtime pins the exact source state the user sees. Rebuild ONLY via `task desktop:build`: a plain `go build` omits `-tags production` and `-extldflags '-framework UniformTypeIdentifiers'`, yielding a binary that starts the HTTP server and then aborts at wails init ("Wails applications will not build without the correct build tags"), leaving the port dead. `ttl:90d source:discovered conf:high` (2026-07-28)
- **[test/verify-the-precondition]** A green assertion against a state you never established proves nothing. "0 Allow-AskUserQuestion buttons on the page" was reported as proof of a fix while no agent had `pendingToolUse` set at all — the render condition never held, so the check would have passed before the fix too. Same trap made every single-card selection test pass both before and after the radio-group fix (one card cannot collide with itself). Rule: construct the failing state deliberately (a unit test that mounts the exact props), and pair every "X no longer happens" with a sibling proving the mechanism still fires when it should. `ttl:90d source:discovered conf:high` (2026-07-28)
- **[desktop/window-verification]** The wails window can be verified without a screenshot and without quitting the user's running app: launch the freshly built shell on a scratch port and DB (`DASHBOARD_PORT=13199 DASHBOARD_DB_PATH=<scratch>.db ./bin/agent-dashboard-desktop`) and read the server's own request log. `GET / 200` followed by `/assets/index-*.js`, `/api/spawners`, `/api/me` is proof that the webview loaded the bootstrap redirect AND the SPA came up on the loopback origin — the exact chain the `Assets`→`Handler` rewrite touches. Two traps in that scratch run: a fresh DB seeds only `claude-default` + `imported-custom`, so sessions whose `CLAUDE_CONFIG_DIR` points at any other profile show NO spawner (expected, not a regression), and the first-run onboarding can spawn a real `claude --resume` under its own pty-host — check `ps -eo pid,lstart,command | grep claude` before walking away, and kill the pty child together with the shell. `ttl:90d source:discovered conf:high` (2026-08-12)
- **[tooling/golangci-config-shadowing]** golangci-lint v2 resolves exactly ONE config file (nearest, walking up from the working directory) and NEVER merges. Because `task lint`/CI lint per module (`cd server && golangci-lint run`), the per-module `.golangci.yml` in `server/`, `sdk/` and all six `plugins/*` silently REPLACED the root config, leaving every module on golangci's five defaults — `gosec`, `revive`, `misspell`, `depguard` and the `gofmt` formatter never ran on any Go code, including both depguard rules guarding ADR-0009 and ARCH-P2-2. Tell from the outside with `golangci-lint linters` (prints the RESOLVED enabled set); a clean `run` output looks identical whether a linter found nothing or was never enabled. The fingerprint of the mistaken assumption: a module config excluding `revive`/`stutters` for a linter it does not enable. Fixed in the same pass (#371): module configs deleted, root config carries their rules and resolves from anywhere, so no `-c` flag can be forgotten. TWO further traps found there: (1) `golangci-lint run` reports only 3 of the 11 files `gofmt -l` flags, so it CANNOT be the formatting gate — `golangci-lint fmt --diff` reports all 11 and exits 1; (2) golangci defaults to `max-same-issues: 3`/`max-issues-per-linter: 50`, so a summary like "24 issues" is itself TRUNCATED — suppressing the first three G204 revealed four more. Set both to `0` before trusting any count. `ttl:90d source:discovered conf:high` (2026-08-17)
- **[tooling/rebase-signing-reschedule]** A rebase in this repo can loop forever with `hint: It has been rescheduled; To edit the command before continuing … git rebase --edit-todo`, listing the same `pick` as both "done" and "remaining". The message points at the todo list; the cause is **commit signing**. Global `commit.gpgsign=true` plus a passphrase-locked SSH key makes git's internal commit hang/fail, so the picked commit stays staged, the pick is rescheduled, and `--continue` answers `error: you have staged changes in your working tree … git commit --amend '-S'` — the `-S` is the only tell. Fix: `GIT_SEQUENCE_EDITOR=true GIT_EDITOR=true git -c commit.gpgsign=false rebase …`. Same root cause as [[tooling/gpgsign-test-helper]] but a different symptom, and the rebase form gives no signing error at all. `ttl:90d source:discovered conf:high` (2026-08-31)
- **[git/remote-fetch-push-asymmetry]** `origin` fetches over SSH and pushes over HTTPS (`git remote -v` shows two different URLs). When the ssh-agent key expires mid-session, `git fetch` dies with `Permission denied (publickey)` while `git push` keeps working — which reads like a repo/permission problem and is neither. Fetch explicitly over HTTPS instead of editing the remote: `git fetch https://github.com/lx-wnk/Agent-Dashboard.git 'refs/heads/main:refs/remotes/origin/main'`. `ttl:90d source:discovered conf:med` (2026-08-31)
- **[ci/golangci-install-network-flake]** Three separate red CI runs in one afternoon (#398 `Lint (plugins/github-oauth)`, #399 `Lint (sdk)`) were all the same thing: the **Install golangci-lint** step failing to download a dependency of golangci-lint itself — `github.com/daixiang0/gci@v0.13.6` from `proxy.golang.org` and `github.com/manuelarte/funcorder@v0.2.1` from `sum.golang.org`, both `stream error: … INTERNAL_ERROR; received from peer`. Zero lint findings involved. Two tells that separate this from a real failure: the failing job belongs to a module the PR does not touch, and the error path points into `go/pkg/mod/github.com/golangci/golangci-lint/...` rather than into repo code. `gh run rerun <id> --failed` cleared it every time. `ttl:90d source:discovered conf:high` (2026-08-31)
- **[git/drift-is-not-content]** Judging a branch, a worktree or a stash by diffing it against `origin/main` measures how far **main has moved**, not what the thing carries. Squash-merged branches showed 3–48 "differing" files while being fully merged; a stash holding 4 changed lines showed 49913. Use the reference that shares the object's own base: for a branch, `git diff <its PR's squash commit> <branch>`; for a stash, `git stash show` (against its own parent); for a rebased branch, compare the *deltas* — `git diff <old base> <old head>` against `git diff origin/main HEAD` — never the trees. `ttl:90d source:discovered conf:high` (2026-08-31)
- **[go/cobra-command-tree]** Two traps when merging a command set into an existing cobra root, both silent. (1) A duplicate `Use` name is **not an error**: cobra keeps both commands in the tree and dispatches to whichever was added first, so the second is unreachable and nothing warns. A green build proves nothing — diff the incoming `Use` names against the existing ones by hand, then execute `<binary> --help` and read the printed list. (2) `PersistentPreRunE` on a shared root runs for **every** sibling command, because cobra executes the nearest ancestor's hook. The folded CLI (#409) loaded its config there; moving that to the `agent-dashboard` root would have made a malformed `~/Library/Application Support/dashboard/config.json` stop `serve` from starting. Attach config loading to the subcommands that need it instead. Verified by constructing the failure, not by reading the code: with a deliberately corrupt config, `agents list` fails with `config error: parse config: …` while `serve` starts clean — cf. [[test/verify-the-precondition]]. `ttl:90d source:discovered conf:high` (2026-08-31)
- **[go/struct-literal-field-removal]** Deleting a field from a Go struct literal leaves the remaining keys aligned to the **old, wider** key column, and `go build`/`go vet`/`go test` all stay green on it — only a formatter notices. CI's `Lint (server)` job runs `golangci-lint fmt --diff`, which fails the run with a bare alignment diff and no other message, so a PR that passed every local gate goes red for a reason none of those gates could have shown (#408, `mcp/tools/control.go` after dropping `PreApproved:`). Any change that removes or renames a field in a literal, a const block or a struct definition must be followed by `gofmt -l <pkg>` before commit. Note `golangci-lint run` is NOT the formatting gate here — see [[tooling/golangci-config-shadowing]]. `ttl:90d source:discovered conf:high` (2026-08-31)
- **[test/live-run-finds-what-units-cannot]** A first end-to-end run of the memory/registry visibility panels against a real server (fresh binary, isolated DB and port, grants revoked then granted) found four defects that the unit suite could not see, and all four sat on the same seam: **one process caching or narrowing what another one writes.** (1) `agent-dashboard settings set` writes straight to SQLite, but `settings/service.go` fills its snapshot once at startup ("Call once at startup") — so a running server keeps answering the old value, even for a key the registry marks `Apply: ApplyLive`, and the CLI prints success either way. Grants are the opposite (the gate queries per request, so `grants add`/`revoke` flip 403↔200 live), which actively teaches the wrong expectation. Verified by constructing the failing state: DB `true`, `GET /api/settings` `"false"`, restart, `"true"`. (2) The Memory panel could search entries but never list them — `retrieve.go` returns an empty slice for an empty FTS query BY DESIGN (a blank query must not dump the store into a prompt) and the space rows had no activation, so a populated store answered "No entries matched this search." Both halves are individually correct and jointly misleading. (3) A revoked grant read as an active one: the STATUS column carrying "Revoked …" sat behind horizontal scroll at 1400px, leaving MODE `allow` as the only visible verdict and text opacity as the only contradiction. (4) The create-entry error rendered below the submit button, off-screen — a denied write looked like no reaction at all. **Rule: for any panel over a gated resource, run it once with the capability revoked AND once granted, at a real viewport. The five distinguishable states (loading / not-asked / denied / confirmed-empty / failed) can all be correct in the unit tests and still collapse on screen, because scroll position, cache boundaries and a route's by-design narrowing are invisible to a mounted component.** `ttl:90d source:discovered conf:high` (2026-09-02)
- **[capability/context-must-be-supplied]** `capability.Decide` drops every grant whose `ContextKind`/`ContextRef` is not in the request's own chain (`decide.go:157`, before the rank lookup). So a context kind can be fully implemented — ranked in `contextRank`, accepted by `grant_repo`, offered by the CLI's `--scope` and the Grants panel, stored, listed as active — and still decide nothing, because no caller ever names it. `routine` was in that state from the gate landing until #421: `pipeline/spawner.go` builds a fixed synthetic `task` context, `memory/authorize.go` built scope + global, and nothing else read the table at all. **Tell from the outside:** grep for the constant's *writers*, not its definition — a kind whose only hits are the constant, a validation list and a rank entry has no producer. The UI cannot tell you: an inert grant renders exactly like an effective one. Same class as [[glob-permission-rules-inert]]. Second finding from the same trail: `DASHBOARD_MCP_TOKEN` was documented in `spawner.go` as "a per-task credential" and is not — it is one `config.MCPToken` value handed to every spawn, so an MCP tool call carries no task identity and cannot be attributed to a routine. Do not build a per-caller authorization on it without per-task keys. `ttl:90d source:discovered conf:high` (2026-09-02)
- **[env/task-dev-frontend-icu4c]** `task dev`'s `dev:frontend` dep (`pnpm dev` → vite) can die silently at startup while `dev:go`/air keeps running — go-task does not cancel sibling deps or retry a failed one, so the backend stays healthy and masks a dead frontend. On this Mac the cause was `/opt/homebrew/opt/icu4c` (Homebrew's plain, non-versioned symlink) getting repointed to `icu4c@78`'s keg by some other install/upgrade, breaking `node` (Homebrew node 21.5.0 is dyld-linked to the exact path `.../opt/icu4c/lib/libicui18n.73.dylib`) with `Library not loaded` — Vite then never binds :5173 and any curl to it hangs/refuses. Diagnose with `timeout 12 pnpm dev` run directly to surface the dyld error (task dev's own output isn't visible to an agent in a different shell). Fix: `brew link --overwrite --force icu4c` (icu4c is keg-only, needs `--force`) restores the 73.2 symlink without touching/upgrading node itself; then the user must restart `task dev` since the already-failed dep isn't retried. `ttl:90d source:discovered conf:med` (2026-09-07)
- **[onboarding/mcp-register-not-idempotent]** `POST /api/onboarding/register-mcp` (`server/internal/api/onboarding/handler.go`) rotated the stored "onboarding" API key hash unconditionally, then ran `claude mcp add ...` — but `claude mcp add` errors when a server of that name is already registered, which is true on every call after the first success. Net effect: "Connect the dashboard" in the UI worked exactly once ever; every later click (a retry, a second browser tab, "Re-check") minted a fresh token, invalidated the just-registered one, and then failed to re-add — permanent "Could not connect automatically". Fixed (2026-09-07) by best-effort `claude mcp remove --scope user dashboard-tasks` (ignored error) immediately before the `add`, making the whole handler idempotent. Verified live: two consecutive `POST register-mcp` calls each returned `ok:true` and `claude mcp get dashboard-tasks` showed `✔ Connected` after both. `conf:high` (2026-09-07)
- **[perf/dedupe-the-promise-not-the-value]** Three separate request storms this repo has now hit share one root cause: a cache or composable that deduplicates *sequential* callers but not *simultaneous* ones. `useSystemResources` allocated a fresh poller per caller (4 consumers → 4 mount fetches → HTTP 429, panels rendered empty); `fetchDynamicCommands` cached the resolved value, so N PromptInputs mounting together all missed the still-empty cache and each fetched. The fix in both cases is to hold the **in-flight promise** (plus ref-counted start/stop for pollers), and to **evict on failure** so a 429 is retried rather than cached as a permanent empty result. Tell from the outside: a burst of identical requests at mount, and a per-IP 429 from `authRateLimiter` (10 r/s, burst 20). Unit tests that mount ONE consumer cannot reproduce any of it — the regression tests mount four and assert a single fetch. `conf:high` (2026-09-09)
- **[css/flex-max-width-loses-to-min-content]** A chat bubble capped at `max-w-[80%]` still overflowed its column (778px inside 725px). `max-width` was resolving correctly; the child blew past it because the bubble used `align-items:flex-start`, which sizes children to **max-content**, and a wide `<pre>` then set the min-content width of every ancestor — CSS resolves `min-width` above `max-width`, so the cap loses. `min-w-0` on the flex item is NOT enough on its own; the inner block needs `max-w-full`, and a `<pre>` needs `max-width:100%` before its own `overflow-x:auto` can engage. Diagnose by measuring the chain (parent clientWidth, bubble clientWidth vs scrollWidth, widest descendant) rather than adding `min-w-0` hopefully — two speculative fixes changed nothing before the measurement named the real element. `conf:high` (2026-09-09)
- **[localscope/proxy-and-status-mapping]** LocalScope binds 127.0.0.1, answers `Access-Control-Allow-Origin: null` and validates the Host header (DNS-rebinding guard), so the browser CANNOT call it directly — it must be proxied same-origin (`/localscope/*` → Vite in dev, `server/internal/api/localscope` in prod). Vite's proxy returns **500** on ECONNREFUSED, which is indistinguishable from a collector-side failure, so a stopped collector rendered as "request failed" instead of "not connected"; its `configure` hook must write 503 explicitly. LocalScope's own contract already encodes the null-vs-zero rule (`summary.ts`: null = not collected, never zero), so map null→notAsked and a number→ready/empty and never invent a fallback. Its real routes are `/api/system/ports` (not "services"), `/api/system/processes`, `/api/system/devices`, `/api/system/summary`. `conf:high` (2026-09-09)

- **[test/playwright-browser-and-sse-waits]** Two traps when driving this dashboard with a headless browser. (1) The `browser-automation` skill bundles patchright, which pins its own chromium revision (`chromium-1228`) that this machine does not have in `~/Library/Caches/ms-playwright/` — it dies with "Executable doesn't exist" and telling the user to `npx playwright install` would download a *second* browser set. The repo's own Playwright (1.59.1, used by `pnpm test:e2e`) has matching browsers already, so import it by absolute path instead: `import { chromium } from '/…/node_modules/.pnpm/playwright@1.59.1/node_modules/playwright/index.mjs'`. (2) Never `waitUntil: 'networkidle'` on this SPA — it holds four permanent SSE streams (`/api/agents/stream`, `/api/tasks/stream`, `/api/projects/stream`, `/api/spawners/stream`), so the network is never idle and the call times out at 30s on a page that rendered fine. Use `domcontentloaded` plus an explicit wait for content. Related: a `page.reload()` mid-load reports dozens of `net::ERR_ABORTED` for in-flight Vite modules — cancellation noise, not failures; set the view via `context.addInitScript` before the first `goto` rather than reloading. `ttl:90d source:discovered conf:high` (2026-09-10)
- **[build/dev-only-code-still-ships]** A `defineAsyncComponent(() => import('...'))` at module scope emits its chunk into the PRODUCTION build even when the only thing that renders it is a runtime `v-if` that can never be true — Rollup cannot prove reachability from a template condition, so the dynamic import is always a real split point. Guarding the *render* is not guarding the *bundle*: the DesignSandbox chunk (fixtures included) shipped in dist until the import itself was moved into an `import.meta.env.DEV` ternary, which Vite substitutes with a literal `false` and Rollup then drops as dead code. This matters beyond bundle size in this repo, because dev-only surfaces hold representative fake data and fake data must not ship beside surfaces showing real data. Verify by grepping `server/frontend/dist/assets/` for the chunk name AND for a fixture string — a `v-if` guard passing review proves nothing. Also note `ls -S dist/assets/*.css | head -1` picks the main bundle; checking a lazy chunk instead reports every utility as missing. `ttl:90d source:discovered conf:high` (2026-09-10)
- **[env/golangci-lint-absent]** `task lint` fails on this machine with `"golangci-lint": executable file not found in $PATH` — it is not in `~/go/bin` (only `air` is), so the repo's Go lint gate cannot run without `go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.1.6` (the version `task setup` pins; a different version changes formatting output). `go vet ./...` per module and `gofmt -l <pkg>` still run and are the fallback, but they are NOT the same gate — see [[tooling/golangci-config-shadowing]] for why `golangci-lint fmt --diff` catches formatting that `golangci-lint run` and vet both miss. `ttl:90d source:discovered conf:high` (2026-09-10)
- **[test/vue-fragment-root-hides-attributes]** A `<template>` whose first node is an HTML comment makes the component a FRAGMENT, so `wrapper.attributes()` describes the mount container (`{"data-v-app":""}`) rather than the element you wrote the attribute on — the attribute is present in `wrapper.html()` and still reads as `undefined`. Bit AppBadge, whose template opens with a `<!-- UX-15 ... -->` note. Query the element instead (`wrapper.get('[data-state]').attributes(...)`); do not "fix" it by deleting the comment. `ttl:90d source:discovered conf:high` (2026-09-10)
- **[localscope/degradation-is-payload-scoped]** LocalScope's `degraded[]` cannot be attributed to an individual metric, and this is a property of its contract rather than a gap in ours. `packages/shared/src/envelope.ts` scopes it to the endpoint's whole payload — "empty array means every collector contributing to `data` fully succeeded" — and for `/api/system/summary` that `data` is all six counts at once. `source` is an open free-form identifier (observed: `lsof:listen`, `adb`, `simctl`, `docker`, `ps`, `broken`) with NO declared mapping to a count. So any source→metric table is a guess this repo invents, and it fails both ways: a new or renamed collector is either silently dropped (hiding a real degradation) or pinned to the wrong number. The dashboard shipped the opposite mistake — pinning every degradation onto every card — which made a simulator-runtime problem read as though the Network count were partial. Correct handling: staleness IS snapshot-wide (one reading, one moment) so it belongs on every card; degradation belongs at machine scope, stated once. `ttl:90d source:discovered conf:high` (2026-09-10)
- **[go/tygo-pointer-becomes-optional]** tygo emits a Go pointer field as TS optional (`workspace?: WorkspaceRef`) while the JSON tag without `omitempty` actually sends `null`. So the generated type says `undefined` where the wire says `null`, and a check written against it tests for a value that never arrives. `src/types.ts` is where this is corrected — it already does exactly this for `SessionMeta` — by re-exporting an `Omit<_Base, 'field'> & { field: T | null }`. Making the narrowed field REQUIRED (not optional) is deliberate: it forces every fixture to opt in explicitly instead of silently defaulting to undefined. Note `task generate` also runs `go generate ./...` which regenerates ent — run `tygo generate --config tygo.yaml` plus `pnpm eslint --fix src/sdk.generated.ts` directly to avoid that. tygo is NOT installed by default (`go install github.com/gzuidhof/tygo@latest`). `ttl:90d source:discovered conf:high` (2026-09-10)
- **[localscope/cwd-is-the-only-identity-evidence]** For resolving a LocalScope observation to a workspace, `cwd` is the only usable evidence and `discoveredProject.rootPath` must NOT be a fallback. Three reasons, all verified: (1) LocalScope derives the project FROM the cwd ("null when the cwd resolved to no project"), so rootPath cannot rescue a missing cwd — live, 0 of 8 services and 0 of 45 processes had a rootPath without a cwd, so the fallback would never have fired; (2) `confidence` is a SINGLE joint qualifier over kind+label+discoveredProject with no per-field granularity — a live service sits at `low` (port-only guess for its label) while carrying a perfectly good lsof-observed cwd, so label-confidence must not be applied to path evidence, nor rootPath trusted at that confidence; (3) the Process type carries discoveredProject with NO confidence field at all, so the fallback would be strictly unqualifiable there. cwd is observed (lsof/ps), the same class of evidence as an agent's cwd, so both sides of a correlation are derived alike. `ttl:90d source:discovered conf:high` (2026-09-10)
