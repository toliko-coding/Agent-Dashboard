/**
 * Public surface of the Runtime feature: this machine as one page — what is
 * running, where it runs, and how current that knowledge is.
 */
export { default as RuntimeView } from './components/RuntimeView.vue'
