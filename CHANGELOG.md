# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

From the first tagged release onward, release notes are generated automatically
from [Conventional Commits](https://www.conventionalcommits.org/) by GoReleaser.

## [Unreleased]

Preparing the first public release.

### Changed
- **Pipeline stages renamed: `concept` is now `backlog`, `backlog` is now `ready`.** The holding pen where refinement chat runs is now called `backlog`, and the starting gun that auto-advances to implementation is now called `ready`. A one-shot data migration rewrites existing rows in `tasks.current_stage`, `stage_runs.stage`, and `task_schedules.current_stage` in collision-safe order (backlog→ready first, concept→backlog second). It runs exactly once, recorded in a new `applied_migrations` table, and it has to: the rename is chained, so a second pass would take the rows the first pass wrote as `backlog` and push them on to `ready` — every task parked in the refinement holding pen would start running by itself after a restart. The stored data cannot settle the question either, because a database holding no `concept` row is indistinguishable from a migrated one. `stage_runs.stage` is rewritten for all rows including terminal runs, because `GetLatestByTaskAndStage` lookups on non-terminal runs would break otherwise, and the rename is name normalization, not history falsification. The `refine.Concept` domain type, `inject_concept` tool name, and `conceptOutput`/`conceptJSON` variables are unchanged — they describe the domain object, not the pipeline stage.

### Added
- **A dashboard-owned model of the local machine, replacing four surfaces that each read LocalScope's wire format.** The Overview cards, the System Map, the LocalScope page and the agent workspace's service correlation each decoded `CollectorResult<T>` themselves, so LocalScope's envelope was known in four places and two of them could disagree about whether the collector was up. Three endpoints now do the translation once — `GET /api/localscope/snapshot` (six counts), `/api/localscope/services` and `/api/localscope/processes` — and the UI consumes a model this repository owns. Lists are separate from the snapshot on purpose: the snapshot is six integers polled every 5s by always-mounted surfaces, while the process list is dozens of records that only the LocalScope page needs, and folding them together would push a process table through the Overview's poll. All three share one freshness state machine, so `source`, `collectedAt`, `ageMs` and `degraded` mean the same thing everywhere. Staleness outranks degradation, because "one collector had trouble" must not mask "this whole reading may no longer describe the machine". **A stale reading keeps its values**: when the collector goes away the last successful reading is served marked `stale` with its age, and a fetch that just failed is stale regardless of age — the alternative is turning "I cannot see this machine" into "this machine is empty". `items: null` (not known) and `items: []` (the collector looked and found none) stay distinct all the way to JSON, and a malformed response is never allowed to decode into an empty list. The Overview and the System Map are now one ref-counted 5s poller between them rather than two independent ones, so they cannot show different counts for the same moment. The hardened path is unchanged: fixed loopback base URL, `GET` only, an allow-list of upstream paths, no inherited client headers, bounded deadline and body; these endpoints build no query string at all, so `all=true` is not reachable through them.
- **LocalScope integration: the dashboard consumes a separate system collector instead of growing one.** LocalScope is its own project and owns system collection — listening ports, developer-process filtering, process/project correlation and Android device detection. The dashboard is a **consumer**: no `ps`/`lsof`/`adb` parsing, no process classification and no project correlation is reimplemented on this side, and no system command is executed from a Vue component. Reachability needed a proxy rather than a fetch, because the collector deliberately binds `127.0.0.1`, answers `Access-Control-Allow-Origin: null`, and validates the `Host` header as DNS-rebinding protection — so a browser cannot call it cross-origin. Vite proxies `/localscope/*` in development and a new Go handler (`server/internal/api/localscope`) does the same for the embedded build, so the UI behaves identically in both. That handler is deliberately **not** a general reverse proxy, since it forwards into loopback: the upstream host/port are fixed at construction from `DASHBOARD_LOCALSCOPE_PORT` (default `7317`, `0` disables the feature entirely) and never read from the request; only an allow-list of collector paths is forwarded, and a path outside it is refused **without contacting the upstream at all**; only `GET` is routed; the request is rebuilt with no inherited headers, so no cookie or `Authorization` can leak to the collector; only `all` survives from the query string; the upstream `Host` names `127.0.0.1` so the collector's own rebinding guard still passes rather than being weakened; and there is an 8s timeout and an 8 MiB response cap. It is mounted inside the authenticated group, because it exposes this machine's process and port table. Unavailability stays distinguishable from failure — a refused connection is `503` and a deadline `504`, both of which the UI reads as "LocalScope not connected", while a status the collector itself returned is passed through so a real fault still looks like one. LocalScope's contract already carries the rule the dashboard enforces (a count of `null` means *not collected*, never zero), so the mapping is direct and nothing invents a fallback: **Network stays unavailable even when the collector is healthy and reporting other counts**, driven by its own `null` rather than hardcoded, and flips to real data the moment that collector lands. Relevance reasons are rendered as sentences ("Known development runtime", "Listening service") and never as LocalScope's internal codes, because the point of the reasons is that the filtering is explainable. A new **LocalScope** view lists services, developer processes (with an opt-in "show all", whose render cap is stated rather than silently truncating), devices, and the not-yet-collected network section.
- **An agent workspace replaces the details drawer.** Selecting an agent opened a 560px side panel, which forced the conversation — the thing you open an agent to read and reply to — into a column narrower than a phone. It is now a workspace: a header across the top, an intelligence column beside a large conversation (measured at 1680px: 1500x920, conversation 68% against intelligence 32%). The previous Overview/Transcript tabs are gone because they are superseded, not because the content was dropped — intelligence sits permanently beside the conversation. The header shows only fields with real backing (model, task ratio when the session wrote TodoWrite items, a start time derived from uptime, spawner when attributed, cost); there is **no Pause and no Stop**, because no such endpoint exists for an agent process, and no ETA, because nothing can compute one. Task progress is TodoWrite items rendered as a completed ratio and never as a phase number: a phase is only real for a pipeline-linked agent, and the item count is whatever the session last wrote. A connections diagram draws only relationships that exist and omits a branch whose data is absent, distinguishing a local service observed by LocalScope from an attachable surface from the agent's own work — its cross-system edge links a listening port whose project root sits inside the agent's working directory, which genuinely is a server running in the project that agent is editing.
- **Overview became a command center.** A metric row, a fixed-topology system map (hand-placed inline SVG — d3 is present but a force simulation would rearrange a known seven-node shape on every mount), an activity feed, quick actions, a system-resources panel and a project summary, above the five pre-existing cockpit panels, which are unchanged. The organising rule is that *unavailable* and *zero* are different claims, so `MetricCard` reuses `CockpitPanel`'s state vocabulary rather than inventing a second one: `empty` renders `0` because the server answered with nothing, while `notAsked` renders a dash and a reason because no collector exists. **"System Health: Good" is deliberately not implemented** — `GET /api/system/health` returns a hardcoded `"ok"` that never degrades, so presenting it as a verdict would assert something never evaluated; the panel is "System Resources" and reports only the three measurements actually taken. The activity feed reads the audit log and nothing else: agent hook events are per-session, in-memory and opt-in, and "status transitions" would have to be synthesised by diffing SSE snapshots in the browser, so they would vanish on reload and double-count across two tabs. Quick actions only route to destinations that exist.
- **Navigation regrouped into Main / Automation / Tools / Insights / System.** Nothing was removed: Pipeline, Schedules, Workflows, Cost and Eval moved groups, and a test now guards that every previously shipped view stays reachable. Cockpit and Dashboard are relabelled **Overview** and **Agents** but keep their view ids, because those ids are persisted in `localStorage` and renaming them would strand every existing user on the fallback view. New destinations: **Projects**, **LocalScope**, **Terminal** and **System**. Settings moved from the sidebar footer into a System group so the rail has exactly one Settings entry. A **Local machine** card in the sidebar carries CPU/memory/disk, and the status bar keeps only what the card does not show (load, cores, uptime, build) so the same numbers no longer appear twice.
- **`GET /api/projects` returns folder paths.** The list reported `folderCount` but not the folders, so nothing could associate a running agent with a project: the folder paths are the only reliable link, since `Agent.projectName` is just `basename(cwd)` and collides across unrelated checkouts. `ListWithFolderCount` already eager-loads the edge and was discarding everything but the length, so this costs no extra query and creates no N+1. The Overview's project rows now show real active-agent counts, derived by segment-safe path containment (so `/home/u/webapp` is not treated as inside `/home/u/web`) and never by name, with a project that has no registered folder reporting "—" rather than a `0` that would claim the association was checked.
- **Routines are real resource-registry entries.** Every `task_schedule` now has a persistent `resources` row (`kind = "routine"`), created inline on schedule creation and reconciled at boot for existing schedules. Deleting a schedule marks its resource row as `orphaned` (not deleted), so capability grants anchored to that routine still resolve. `GET /api/resources?kind=routine` returns persisted resource rows instead of read-time projections, **narrowed to the calling user's schedules** — the `resource` table is scoped by `(scope_kind, scope_ref)`, which is global/project/path and never a person, so routines cannot be served by the generic list path without showing every user every other user's routines; identity still comes from `task_schedule.user_id`, and only the row is now persisted rather than synthesized. All four mutations sync: create, update, enable/disable (its own writer, separate from update, and the one the MCP schedule tools call), and delete. `NewTaskScheduleRepo` builds the registry repo itself rather than accepting one, because an injected `ResourceRepo` gives every call site a way to pass `nil` and lose the sync silently — which is what all three mutating call sites did, leaving the whole lifecycle dead in production while every unit test passed. Nothing substitutes it, so the parameter bought only that failure.
- **The refinement chat offers answers, not only a text box.** Refinement was a free-form chat: every reply the agent asked for had to be composed from scratch, including the ones where it had already worked out the two or three plausible answers itself. The agent now suggests up to three, and the user picks one. Mechanically this is not a new model call and not a second round trip — the turn was already being spawned and streamed, so what changed is that its output carries structure alongside the prose. It reuses the protocol `__phase_done:` established: an out-of-band block the user never sees, stripped out of the content before anything renders it. That stripping now happens in three places, once per surface the text passes through — `refine.ExtractOptions` for what gets persisted, a line-by-line state machine in `useRefinementChat.ts` for the live stream, and `OPTIONS_BLOCK_RE` in `RefinementChat.vue` as a last guard on displayed content — and each of the three names the other two, because the grammar is shared across two languages with no module that can hold it. The options ride on the turn as an optional JSON field, so a conversation recorded before this change reads back exactly as it did. Buttons appear under the **last** assistant message only and vanish the moment the user answers, whether by clicking or by typing: options belong to the question that is still open, and leaving them up invites a click that would answer a question two turns back. The free-text input never goes away — this is a shortcut, not a replacement. A block the agent opens but never closes (a cut stream, the five-minute refinement timeout) is not treated as options at all: its lines are put back into the visible answer rather than collected into a block that never arrives, because the alternative is an answer that silently truncates mid-sentence.
- **Skills the database owns now reach the disk that agent runtimes read.** `ResourceKindSkill` existed as a registry kind with no writer and no content — `GET /api/resources?kind=skill` answered `[]` and always would. A skill resource now carries content (`skill`), and a new materializer produces the `SKILL.md` files from it. The target is deliberately **not** "the filesystem": it is node × config dir × provider, so a machine running `~/.claude-personal` alongside `~/.claude` gets both, and every enabled non-Claude provider gets a *visible no-op* rather than a silent gap — none of Codex, Gemini, Junie or pi has a `SKILL.md` equivalent, and a user who authored a skill and saw nothing at all for Codex would have been misled. This is the only component in the dashboard that writes into a user's own directories, so most of it is refusal. A file it did not write is **never touched**, reported once as `foreign` and then remembered so it stops nagging. A file it wrote that a person has since edited is a `conflict`: it stops for that resource, does not merge, does not overwrite, and does not queue a retry that would overwrite it later. Ownership is proved by the recorded path, not by the marker in the file's frontmatter — a copied file carries the marker too, which is the lesson `agent-dashboard hooks install` already learned about `settings.json`. Writes are atomic (temp file, `Sync`, `Chmod`, `Rename`) and refuse any symlinked directory below the config root, while the config root itself may be a symlink because a `~/.claude` linked into `~/.claude-personal` is an ordinary dotfiles layout. Two dashboard instances on one machine cannot fight over the same files: a run takes a `coord_lock` node lease first, and the instance that does not get it degrades to read-only, reports exactly what it would have written, and names the holder — reachability is not ownership. `POST /api/skills/materialize` is the trigger and is **dry-run by default**: `{"dryRun": false}` is the opt-in to writing, and an absent field or an empty body writes nothing. It is session-authenticated, single-flighted, and takes no capability grant — the lease and the refusal rules are what gate it.
- **`skills-lock.json` is gone, together with the install command that could never work.** Nothing in the tree ever read the file — not Go, not TypeScript, not a shell script — and the documented `jq` one-liner in `docs/guides/agent-skills.md` read a `.name` key that does not exist (the name was the map key) and handed `curl` an `owner/repo` slug where it expected a URL. Its `computedHash` values were verified by nothing, and its seven entries contradicted the five the same document listed as "current skills". Provenance now lives on the registry row's `origin`/`origin_ref`, and the guide documents materialization instead. Leaving a documented command that cannot work is worse than having no command.
- **A cockpit landing view, and GitHub as the dashboard's second resource-registry Application.** The dashboard now opens on **Cockpit** rather than **Dashboard** — a glance-first grid of five panels (Agents, Pipeline, Routines, Memory, GitHub) that sits beside the existing dashboard rather than replacing it; **Dashboard** is still one click away in the **Monitor** nav group, unchanged. Each panel renders exactly one of five states — `loading`, `notAsked` (nothing configured, and the panel says why), `denied` (the server refused and named a reason), `empty` (a confirmed nothing), `failed` (the request itself errored) — or its content, never two at once: `CockpitPanel.vue` is their single owner, and every state carries its own `data-testid` (`cockpit-<panel>-<state>`) so a panel that ever collapsed a refusal into an empty list, or an unconfigured install into a healthy quiet one, would fail its own test rather than just look wrong. GitHub is the panel that reaches all five states; the Agents and Pipeline panels stream from mount and are not gated, so `notAsked`/`denied` are structurally unreachable for them, and that narrowing is recorded rather than silently assumed. GitHub itself is registered as a resource-registry **Application** (`server/internal/apps/github`, `github.Register`) with four capabilities — `github.read`, `github.search`, `github.comment` (class `reach`, the same class `obsidian.*` and `WebFetch` use), and `github.merge` (class `spend`, so `capability.defaultEffect` sends a no-grant merge straight to deny rather than to an ask a tired human could click through). Configure it from **Settings → GitHub**: `github.token` (a fine-grained PAT, `Secret: true`, encrypted the same way `obsidian.apiKey` is), `github.repos` (a comma-separated `owner/name` allow-list), and `github.baseURL` (defaults to `https://api.github.com`; override for GitHub Enterprise) — `token`/`repos` are a required pair exactly like the Obsidian trio, `baseURL` is not (it always has a default), and all three apply only after a restart. The repository allow-list (`Client.AllowsRepo`) is checked **before** the capability gate on both surfaces — `GET /api/github/summary`, `GET /api/github/search`, `POST /api/github/comment`, `POST /api/github/merge`, and the matching `github_read`/`github_search`/`github_comment`/`github_merge` MCP tools — so a capability grant can never reach a repository outside the list, and `github_search`/`GET /api/github/search` bound the query itself with an appended `repo:` qualifier per configured repository rather than filtering the response afterward. Three new MCP scopes (`github:read`, `github:write`, `github:merge`) take the tool catalogue from 47 tools / 9 scopes to **51 tools / 12 scopes**; `github:write` implies `github:read` but deliberately not `github:merge`, and `github:merge` implies `github:read` but deliberately not `github:write`, so a comment-only key cannot merge by accident and a merge-only key has no business editing discussions — see [`docs/guides/mcp.md`](docs/guides/mcp.md#scopes) and [`docs/guides/security.md`](docs/guides/security.md#githubs-token-and-repository-boundary). The client dials through the same SSRF guard the rest of the server uses (`validation.SafeDialContext`), which refuses loopback, private, link-local, and CGNAT addresses — so a GitHub Enterprise host on a LAN address (`10.x`, `192.168.x`) cannot be reached today; widening the shared guard for one application was rejected the same way it was for Obsidian, and the documented follow-up is a narrow per-client dial policy, not a change to `validation.IsBlockedIP`. **The slice's own success criterion was that a second application could run through the resource-registry/capability kernel without any kernel-specific change**, and that held at all four checkpoints: the capability declaration (`apps/github/app.go`), the REST client (`apps/github/client.go`), the four HTTP routes, and the four MCP tools each shipped with zero lines touched under `internal/capability/`, `memory.Gate`, or the capability repos — confirmed by diff, not asserted. The one shared edit across the whole slice was `server/serverapp/di.go` gaining a `GitHub` field on `ServerComponents` and a `GitHubHandler` field on `RouterDeps`, the same per-application slots that struct pair already carries for Obsidian (`Obsidian`/`ObsidianHandler`), so even that was additive wiring rather than a kernel change.
- **A routine is a thing the system has, and a grant can name it.** `capability.Decide` has ranked a `routine` context since the gate landed, the grants API and CLI accept `--scope routine:<id>`, and the Grants panel showed such a row as active — but nothing in production ever put a routine into a request's context chain, and `Decide` drops every grant whose context the request does not name. A routine grant was therefore stored, listed, and inert: it decided nothing, and there was no way to tell that from looking at it. Two things were missing, and neither was the gate. First, a task did not know which routine produced it: the link ran the other way, `task_schedule.last_task_id`, which is overwritten on every fire and so names only the most recent task. Tasks now carry `routine_id`, stamped by the scheduler's materializer with the schedule's own id. It is deliberately **not** a field of the task-create request body — a caller able to name a routine on a task it creates could hand that task another routine's permissions — so the scheduler is its only writer, and `task.metadata` (which *is* caller-supplied, and is copied verbatim from the schedule) was rejected as its home for the same reason. Second, the pipeline's automatic memory push now passes that id to `memory.Gate.Authorize` as an extra context, so `agent-dashboard grants add memory.read --pattern '*' --scope routine:<schedule id> --mode allow` decides the push for the tasks that routine fires, and outranks a project- or global-level grant, which is what the specificity order always promised. An empty `routine_id` yields no context at all rather than a blank one, because a `routine` context with an empty ref would match every grant stored with an empty `ContextRef`. **This is the memory push only.** An MCP tool call — `memory_search`, `obsidian_write`, any of them — still resolves with no routine context, because `DASHBOARD_MCP_TOKEN` is a single value taken from config and handed to every spawn (`serverapp/di_pipeline.go`), so the token an agent presents identifies no task and therefore no routine. A routine-scoped grant on `obsidian.write` remains inert; the fix for that is per-task MCP credentials, not a change to the gate. `GET /api/resources?kind=routine` also stops answering an empty list: it projects `task_schedule` rows into the registry view, narrowed to the calling user exactly as `GET /api/schedules` is. The registry table gets no routine rows and deliberately never will — a schedule already carries the trigger, the task template and the budget a routine is made of, so a mirrored identity row would be a second truth to re-sync on every create, rename, enable and delete. The projected id is the schedule's own, which is exactly the id to grant against, and an enabled and a disabled schedule report distinguishable states rather than both reading as present.
- **A memory space can be opened, not only searched.** `GET /api/memory/entries` ran an FTS query and nothing else, and `Retriever.Retrieve` returns an empty slice for an empty query text on purpose — a blank query must never dump a whole store into a spawn prompt. The panel had no other listing path and its space rows were inert text, so a space holding entries answered "No entries matched this search." unless the reader guessed a word inside one of them: a populated store and an empty one were the same screen. The route now takes an optional `spaceId` and lists that space's valid entries (`MemoryRepo.ListValid`, which already existed for the retriever) instead of searching. It authorizes `memory.read` **before** resolving the space, on the same empty value and scope the search branch checks, so an ungranted caller cannot use it to probe which space ids exist; a space whose own scope differs from the requested one answers the identical 404 a missing id does, so a grant in one scope cannot read another scope's space by id. Both branches answer the same response shape — the route keeps exactly one contract. **Settings → Memory** makes each space row a real button, shows which space is being browsed with a way back to the search, and distinguishes "this space has no entries yet" from "no entries matched this search": the two blanks mean different things and previously read as one.
- **The resource registry is readable from the dashboard.** Applications, routines, skills and memory spaces have shared one identity table since the registry landed, but the only rows anything rendered were memory spaces, reached through `/api/memory/spaces`. A new read-only route, `GET /api/resources?kind=…`(optional `scope`/`scopeRef`), answers a hand-written camelCase DTO over the existing `repo.ResourceRepo` — no schema change and no new table. It does **not** introduce a new capability, but it is not ungated either: `kind=memory_space` requires `memory.read` **in the scope the request asks for**, the same grant `/api/memory/spaces` demands for the identical rows. Without that, this route served what the gated one refuses, and the `scopeRef` echo turned it into a confirmation oracle for arbitrary filesystem paths — any extra row beyond the always-returned global ones proves that path has spaces and names them. The gate is deliberately per-kind: `application` rows are already public through `/api/plugins`, and nothing writes `routine` or `skill` yet. `kind` is **required** and is checked against an enumerated set (`application`, `routine`, `skill`, `memory_space`) rather than against the table's distinct values, so a typo is rejected as unknown even while the registry is empty; an unknown kind is a 400, and a kind nothing writes yet answers `[]` rather than 404 — "none of these exist" and "this is not a thing" are different answers. The DTO is hand-written for the same reason the memory routes' now are: encoding `*ent.Resource` directly puts snake_case on the wire and republishes every column added to the schema later, whether or not a client should see it. **Settings → Registry** renders it, with a kind picker and a scope selector, and distinguishes all five of its states — still loading, no scope ref entered yet, denied, confirmed empty, and request failed — instead of collapsing them into an empty table. Denied is a state and not an error because nothing grants `memory.read` by default, so on a fresh install a 403 on the memory spaces kind is the ordinary first thing a user sees; it names what the server actually said rather than asserting a cause, since a missing grant, a rate limit and a grant-store failure all arrive as 403. A registry that shows nothing because it was never asked looks exactly like one that holds nothing.
- **The Grants panel says whether a grant is enforced by anything.** `GET /api/capabilities` has always shipped `enforceable_by` per capability, and `agent-dashboard grants list` grew an `ENFORCEMENT` column for it, but the dashboard's own panel did not: a grant on a capability no production path resolves looked identical to one that takes effect. Each row now states which enforcement point backs it, so a grant that is stored and read by nothing is visible as such at the moment it is created rather than after it fails to do anything.
- **Each stage run now shows the memory it was actually given.** `ent.MemoryInjection` has recorded one row per memory push into a stage spawn since the memory store landed — which entries went in, the character budget, the characters spent, and how many candidates the ranker had to choose from — but the record was reachable only by `curl`, so the retrieval heuristic could be argued about and not measured. The task modal's **Stages** tab now renders it under the run it belongs to (`N entries`, `used / budget chars`, `from N candidates`), issuing one `GET /api/memory/injections?stageRun=…&stageRun=…` for the whole tab: the route grew a repeated-id form precisely so it would not be one request per run. That is a permission concern, not a performance one — `Gate.Authorize` *records* a rate-limit use on every call, so a per-run read spent one unit of `memory.read` per stage run per render, and opening a ten-run task modal against a ten-uses-per-hour grant exhausted the window. The pipeline's own memory push authorizes the same capability against the same grants, so merely looking at what memory an agent received could stop the next agent from receiving any. The single-id form is unchanged, and the id list is capped. `listInjections` authorizes `memory.read` at *global* scope unconditionally — an injection can span several spaces, so it has no single owning space to match a grant pattern against — which means a project-scoped `memory.read` grant does **not** back this call. A 403 therefore renders as one tab-level notice naming the global scope, not as the same warning repeated on every row, and a transport failure renders as a separate error: the tab shows nothing at all for a run that received no push, so "not granted", "not fetched yet" and "nothing to show" must not all arrive as the same blank.
- **The Obsidian vault integration is reachable now, not just catalogued.** `settings.Service` gained encrypted-value support: `app_setting` has new `secret`/`nonce` columns (base64 AES-256-GCM ciphertext + nonce via `internal/secretbox`), a `Definition` can be marked `Secret: true`, and a secret definition carrying a `Default` now panics at package init (`validateDefinitions`) instead of a guard that could never fire on the live write path — a secret's default would otherwise be returned in clear before anything was ever stored. `obsidian.apiKey` is the first setting to use it: encrypted on write, and masked to `********` on every read except `Service.Secret`, the one accessor that decrypts. Setting a secret to the empty string **clears** it rather than encrypting an empty value, on the HTTP surface and the CLI alike — empty means "no value", so a cleared secret reads back as unset instead of as a mask that made it look configured, and clearing is what lets the Obsidian trio return to the all-empty "vault off" state. The two surfaces have to agree: a key cleared from the CLI that still read back as `********` would show the Settings panel a filled API-key field, let its trio guard permit the save, and fail the next boot on the key it was told was there. Clearing needs no master key either — removing a secret must not depend on resolving one, least of all in the recovery command. Both the HTTP settings surface (`GET`/`PATCH /api/settings`) and the `agent-dashboard settings` CLI (`list`/`get`/`set`) now mask secrets consistently; the CLI encrypts a secret write instead of storing it plain, refuses to persist the `********` sentinel as a real value (`ErrMaskedValueRejected` — it means "leave unchanged", not "the secret is eight asterisks"), and resolves its master key lazily on the first secret write, so a read-only `list`/`get`/`grants`/`caps` invocation never generates key material as a side effect. Three more Obsidian settings joined `obsidian.apiKey`: `obsidian.baseURL`, `obsidian.vaultRoot`, and `obsidian.tlsMode` (enum `verify`/`pinned`/`insecure-loopback`, default `verify`) — **all four apply only after a server restart.** `buildObsidianClient` (`server/serverapp/di_obsidian.go`) constructs the vault client at boot from them: nothing set at all means the integration is off (logged, not an error); any one of `baseURL`/`vaultRoot`/`apiKey` set without the other two **fails the boot outright**, naming the missing settings keys — a half-configured client would otherwise come up looking healthy while every request shipped an empty `Authorization` header and failed 401. `ensureObsidianSpace` idempotently creates a global-scope `"obsidian"` memory space at boot regardless of whether the vault is configured yet, so the indexer always has somewhere to write once it is. `POST /api/obsidian/index` runs one on-demand pass of `obsidian.IndexNotes`: vault notes become `kind = "pointer"` memory entries (a sanitized first-line summary and the note's own path — never the note body). A second run while one is already in flight gets `409` rather than racing the first into permanently duplicated pointers (an `atomic.Bool` single-flight guard); an unconfigured vault gets `503`; a missing `obsidian.read`, `obsidian.search`, or `memory.write` grant gets `403` naming the capability, because this handler's `memory.Gate` is built with **no `Asker`** — nobody is watching an on-demand trigger resolve a capability question, so an `ask` decision has to deny rather than hold the HTTP response open. **A fresh install therefore denies the index run by default** until those three grants exist (`agent-dashboard grants add obsidian.read --pattern '*' --scope global --mode allow`, repeated for `obsidian.search` and `memory.write`, or the same three from **Settings → Grants**). Four new MCP tools — `obsidian_read`, `obsidian_search` (`obsidian:read` scope) and `obsidian_write`, `obsidian_delete` (`obsidian:write`, implies `obsidian:read`) — let an agent reach the vault directly, gated by the same `memory.Gate`, this time built **with** an `Asker` shared with the memory MCP tools: an agent is genuinely waiting on the tool response, so an `ask` decision may legitimately hold instead of failing closed. `RegisterObsidianTools` registers none of the four when the vault is unconfigured — an agent discovering a tool it can never use is worse than one it never sees. `obsidian_read`/`obsidian_write`/`obsidian_delete` all normalize the note path (`Client.NormalizeNotePath`) once, before the capability check, and reuse that same normalized string for both the grant check and the vault call; passing the raw path to the gate while the client separately normalized its own copy would have let `notes/../secrets/x.md` pass a `notes/*` grant and then resolve to `secrets/x.md` once it reached the client — worst on `obsidian_delete`, since a deletion cannot be undone. `obsidian_search` goes through `Client.SearchUnderRoot`, not `Client.Search`: the vault's search endpoint is vault-wide by design, so returning its results verbatim let a single `obsidian.search` grant enumerate note paths outside `VaultRoot` — filenames and existence past the boundary every other call enforces — and the surviving hits are reported root-relative, the form `obsidian_read` actually accepts. The MCP tool catalogue grows from 43 tools / 7 scopes to **47 tools / 9 scopes** (see [`docs/guides/mcp.md`](docs/guides/mcp.md#scopes)). **Settings → Obsidian** configures all four settings (base URL, vault root, a masked API key field, and a TLS-mode select) and adds an **Index now** button; saving is **blocked** while the required `baseURL`/`vaultRoot`/`apiKey` trio is only partially filled in — that is the state the server refuses to boot with, and the dashboard is what would die; all three empty stays allowed and is the working off switch (the API key field is sent empty, which clears the stored secret), and the panel turns the index endpoint's `403`/`503`/`409` into the readable messages above instead of raw status codes.
- **Grants are manageable from the dashboard, not only from the CLI.** Four session-authenticated routes — `GET /api/grants` (newest first, optional `?capability=` filter), `POST /api/grants`, `DELETE /api/grants/{id}`, and `GET /api/capabilities` for the catalogue — close half of the "no grant-management surface" gap the `agent-dashboard grants` CLI entry names. The endpoint mirrors every refusal the CLI already makes, because the reason for each is a property of the resolver rather than of the CLI: `capability.Decide` silently drops a grant whose mode or context kind it does not recognise, a `global` grant carrying a ref is unmatchable because every caller builds the global context with an empty ref, and a non-global grant with an empty ref is unmatchable because `repo.Scope.Normalize()` collapses an empty ref to `global`. A write path that accepts any of those stores a row that reads as a permission and behaves as nothing, so an unknown capability name, an invalid mode or context kind, a ref on `global`, a missing ref elsewhere, a limit count without a positive window, a non-positive expiry and a missing pattern are each refused with 400 and the valid values named. `granted_by` and `revoked_by` are taken from the request's auth payload — they exist so "who granted this" stays answerable, and an empty string would leave that permanently unanswerable. `DELETE` tombstones rather than deletes: the method describes the caller's intent, `GrantRepo.Revoke` describes the storage truth. The routes sit in the JWT group but are not admin-gated, matching `presets` (the legacy form of this data) rather than `spawners`.
- **A human's answer to a capability ask is now recorded.** `ServerEnforcer` could hold a call open for a person to allow or deny (see the entry above), but the click that turned a denial into a permission left no trace: `Asker.Resolve` handed the decision to the waiting caller and returned. `Resolve` now returns the `Pending` it resolved — the ask id is a UUID minted per ask and names nothing once the hold is released, so a caller wanting to record the decision could otherwise say only *that* something was approved, never *what*. `POST /api/capabilities/decisions/respond` writes a `capability_allow` or `capability_deny` audit event carrying the capability, the value, the context and the reason, attributed to the deciding user from the request's auth payload. The write lives in the handler rather than in `serverask`, because that is where the acting user is; putting it deeper would make `serverask` learn about authentication in order to name an actor. It is deliberately non-fatal and detached: a failed write logs and the decision still stands (it had already taken effect — a 500 would report that nothing happened when something did), and the record is written on a `context.WithoutCancel` of the request, so a browser that navigated away cannot drop the trail of a decision it already made.
- **A grants panel in Settings.** The HTTP surface added above gets its interface: **Settings → Grants** lists every grant newest-first, creates one, and revokes one. The capability name is a select fed by `GET /api/capabilities` rather than a free-text field, because a name the catalogue does not know is refused by the server and there is no reason to let it be typed. The form makes the structurally impossible unreachable — the context ref is cleared and disabled when the context is `global`, since a global grant carrying a ref is unmatchable — but it does not restate the server's rules as a second source of truth: everything else is submitted and the server's own message is surfaced verbatim on a refusal. A revoked grant stays visible and is labelled with who revoked it and when, rather than disappearing; `revoked_at`/`revoked_by` exist so that history stays readable, and hiding the row would throw away the only thing the tombstone was for. Rows carrying `granted_by = "migration:legacy"` are marked as such, so a grant the backfill inferred from a legacy `task_permissions` or `permission_presets` row is distinguishable at a glance from one a person deliberately created.
- Internal resource registry: every managed resource now has one identity row carrying kind, slug, scope, node, origin and lifecycle state. Existing plugins are reconciled into it on boot. The registry's slug rule caps plugin ids at 64 characters — a plugin manifest with a longer id is now rejected at discovery, at load, and by the plugins API. Otherwise this is the foundation the capability gate and the memory store build on.
- **Capability gate.** Permission-relevant actions are now named as capabilities (`server/internal/capability`) — a class (`tool`, `reach`, `resource`, `spend`) plus the enforcement points that may apply it — and resolved by one pure Decider that ranks the grants bound to a request's context (agent session beats task beats routine beats application beats project beats global) and, within the winning level, lets deny beat allow beat ask. Grants live in new `capabilities`, `grants`, and `grant_usages` tables: a boot-time seed catalogues every grantable Claude Code tool name, and an idempotent backfill migration turns existing `task_permissions`/`permission_presets` rows into grants (`granted_by = "migration:legacy"`) — see [`PRIVACY.md`](PRIVACY.md) for what those tables hold. The six-level ranking above describes what the Decider does when more than one context level has a matching grant; only one level is ever actually populated today, because the one enforcement path resolving real requests builds its grants directly from `task_permissions` at a single synthetic task-level context rather than reading the `grants` table — so the backfilled rows (and the project-level grants the preset backfill writes) are written and not yet read by anything in production. Of the three points a capability can declare itself enforceable at, the **spawn** point resolves decisions through the Decider in production for every task-pipeline agent: `server/internal/pipeline/spawner.go` now translates each granted `TaskPermission` into a capability grant and renders the allow-list from what the Decider returns, replacing the ad hoc filter chain that used to do the same job by hand. The in-process **server** enforcer now has a real production caller too — see the memory store entry below — which also makes the `grants` table's project/global rows readable for the first time, though still only at two of the six context levels (a memory scope's own context plus the global fallback), not the full hierarchy. The terminal-session **hook** point still runs its own, separate deny-rule matcher rather than the Decider's — see [`docs/guides/security.md`](docs/guides/security.md#capabilities-and-the-permission-gate) for exactly what each point guarantees, including the hook's deliberate fail-open timeout.
- **A human can answer a capability `ask`.** The server enforcement point (`ServerEnforcer`) now has an `Asker` wired to it (`server/internal/serverask`, built in `server/serverapp/di.go`): a decision that resolves to `ask` holds the caller's request open, surfaces as a card in the dashboard's triage band — capability, value, context and reason — and **Allow**/**Deny** there posts to `POST /api/capabilities/decisions/respond`, releasing the held call. Only the two callers with someone actually waiting may block: the HTTP memory handler (a browser request has a human on the other end) and the memory MCP tools (an agent is waiting on the tool response). The pipeline's memory push at spawn and the Obsidian vault indexer's three checks (`memory.write`, `obsidian.search`, `obsidian.read`) keep constructing a `memory.Gate` with no asker, so an ask there denies instead of stalling a spawn or a background index run — "must never ask" is a property of how the `Gate` is built, not a rule every call site has to remember. Nothing is wired under `DASHBOARD_AUTH=none`, where the respond route is not mounted either and "a human decided" would mean "any local process decided"; an `ask` there still fails closed to `ErrAskRequired`. An ask nobody answers within 25 seconds denies — this point is the complete backstop and has no fallback prompt to fall through to. `capability.Asker` now receives the `Request` alongside the `Decision`, because a `Decision` names no capability and no value, so a card built from it alone could not say what it was asking to approve. The hold-and-answer mechanism itself moved into `server/internal/askgate`, shared with the PreToolUse hook bridge, which keeps its opposite fail-**open** posture: the store only reports whether a decision arrived, never what "no decision" should mean, and each caller maps that itself.
- **`agent-dashboard grants` CLI.** Four subcommands — `add <capability>`, `list`, `revoke <id>`, `capabilities` — create, list, and revoke `grants` rows, and list the capability catalogue, closing half of the "no grant-management surface" gap noted above (an HTTP route and a settings page for the same job remain unbuilt). Like `agent-dashboard settings` and `agent-dashboard plugins`, it opens the SQLite database directly rather than going through HTTP, so it also works as a recovery path while the server is down. `add` and `capabilities` seed the capability catalogue on demand (`repo.SeedCapabilities`) before touching it, because the catalogue is otherwise only seeded at server boot, and a database the server has never opened would look empty to both commands. `add` refuses a capability name the catalogue doesn't recognise rather than writing an inert row: an unknown name has no catalogue entry, so `capability.Decide` would resolve it to a zero-value `CapabilityView` and deny forever, leaving a grant that writes successfully and then can never take effect. `revoke` tombstones (`revoked_at`/`revoked_by`) rather than deleting, so grant history survives. `GrantRepo.Create` now also rejects an invalid mode, an invalid context kind, a non-empty context ref on the `global` context, and an empty ref on any other kind, so a typo in `--scope` or `--mode` fails at write time with a listed set of valid values instead of producing a grant nothing can ever match. Only the first two mirror a rule `capability.Decide` applies when ranking — it drops a grant whose context kind or mode it does not recognise. Decide has no global-ref rule at all: it tests scope by exact struct lookup (`inScope[Context{Kind, Ref}]`), so a `{global, "x"}` grant is unmatchable only incidentally, because every caller builds the global context with an empty ref. An empty ref on a non-global kind is unmatchable for the mirror-image reason — `repo.Scope.Normalize()` collapses an empty ref to `global`, so no caller ever emits that context — and the read side (`memory.ParseScope`) already rejected it. `add` likewise refuses a `--limit-count` without a positive `--limit-window`: the window is counted backwards from the moment of the check, so a zero window finds no prior usage and the limit never triggers — a grant that reads as rate-limited and behaves as unlimited. `--expires-in` must likewise be a positive duration; a zero or negative one used to report success and create a grant that was already expired. `--pattern` is required rather than defaulting to the empty wildcard, because scope, mode, and pattern defaults compounded into the widest grant the system can express (global, allow, matches everything) from `agent-dashboard grants add Bash` alone — pass `--pattern '*'` to mean every value, which is stored literally and matches identically, since `capability.Match` routes a trailing `*` through a prefix test with an empty prefix. `revoke` refuses a grant that is already revoked, via one conditional `UPDATE ... WHERE revoked_at IS NULL` rather than a read-then-write pair, so a second call cannot overwrite who revoked it first. `add` writes the grant either way, but warns on stderr when nothing reads it: the `grants` table has exactly one production reader (`internal/memory.Authorize` through `capability.ServerEnforcer`), so any capability not enforceable at the **server** point — which is every Claude Code tool name — is stored and read by nothing until a reader exists. `list` reports that same fact per row in a new `ENFORCEMENT` column (`server` or `none`), alongside a `GRANTED-BY` column, and orders newest-first whether or not `--capability` is passed. **Specificity is resolved before mode:** the most specific context level with any matching grant decides outright, so a `project:/x` allow beats a `global` deny — a global deny is not a safety net over a narrower allow; see [`docs/guides/security.md`](docs/guides/security.md#creating-and-revoking-grants).
- **System-owned memory store.** A new local memory lets agents (and you) persist facts, preferences, and lessons that outlive one task, and pushes a budgeted, ranked extract into a stage's spawn prompt automatically. A **memory space** is not its own table — it is a row in the existing resource registry (`kind = "memory_space"`), scoped global/project/application like every other resource. Entries (`memory_entries`) carry a short `summary` (what gets pushed into a prompt) and a full `content` (pulled on demand), a `kind` (fact/preference/lesson/entity/pointer), a source, a confidence, and a validity window; writes are sanitized first — invisible/control characters are stripped, then known secret patterns are scrubbed — because a memory entry outlives the read-time scrub the rest of the app relies on; `summary` is additionally collapsed to a single line (so it can never forge the `\n\n---\n` section boundary the assembled prompt uses), while `content` keeps its original line structure — code blocks, PEM bodies, and numbered steps round-trip intact rather than being flattened. Entries are indexed with SQLite FTS5 and ranked by lexical match, scope specificity, recency, confidence, and kind — one retrieval path (`internal/memory.Retriever`) backs both the read surfaces below. Every stage spawn now retrieves up to 2,000 characters of ranked memory (a character budget, not a token count — there is no tokenizer in this codebase) and appends it to that stage's **user** prompt, never the system prompt (which is truncated head-first at 10,000 characters and would otherwise push the stage's own instructions off the end); a retrieval error, a nil injector, or a non-positive budget all degrade to no memory block rather than blocking the spawn. Access is gated by two new capabilities, `memory.read` and `memory.write` (class `resource`, enforceable only at the **server** point — memory has no spawn-time allow-list or PreToolUse hook to check against), enforced identically for the HTTP surface (`GET/POST /api/memory/spaces`, `GET/POST /api/memory/entries`, `PATCH/DELETE /api/memory/entries/{id}`, `GET /api/memory/injections`) and two new MCP tools (`memory_search`, `memory_write`, gated behind new `memory:read`/`memory:write` MCP scopes) through one shared `internal/memory.Authorize` call. **No asker is wired to the server enforcer**, so a capability decision of "ask" fails closed to a denial today rather than prompting a human — see [`docs/guides/security.md`](docs/guides/security.md#capabilities-and-the-permission-gate). There is no delete path for a memory space or a memory entry: `DELETE /api/memory/entries/{id}` marks an entry expired rather than removing it, and `PATCH .../{id}` marks one superseded — both keep the row for the audit trail. See [`PRIVACY.md`](PRIVACY.md) for what these tables hold and how (not) to remove it. **An `agent-dashboard grants` CLI now creates and revokes `grants` rows** (see the entry below) — there is still no HTTP route or settings page that does, and granting is a separate question from asking: `--mode ask` is accepted and stored, and an `Asker` is now wired to the server enforcer (see the entry above), so an ask-mode grant reaches a human instead of denying outright — except under `DASHBOARD_AUTH=none`, where no asker is built and it still fails closed. So a fresh install still denies `memory_search`/`memory_write` and every `/api/memory/*` request by default — that default has not changed — but there is now a supported, user-facing way to grant `memory.read`/`memory.write` instead of writing directly to the SQLite database.
- **Per-stage reasoning effort.** A spawner's `adapter_config` now accepts an `effort` key for adapter_type `claude` only (`server/internal/api/spawners/validation.go`); every other adapter type still rejects the key. The accepted values — `low`, `medium`, `high`, `xhigh`, `max` — are the five levels the `claude` CLI's own `--effort` flag takes, verified against `claude --help` (v2.1.248) rather than assumed; `EFFORT_OPTIONS` in `src/utils/models.ts` is a hand-kept copy of that same set for the settings dropdown, because the Vue client cannot import the Go constant — both lists say at their own definition that they must stay in parity, so a value set outside the dropdown (e.g. through the API) is still honoured, not shown as unrecognised. The control lives next to model selection in the spawner edit form: always visible, but disabled with a stated reason for the four adapter types that don't support it, so the UI can never submit a value the server would reject; a stored value outside the known five renders blank with a visible warning naming the actual value instead of silently picking a default. The resolved value reaches the spawned process — the task pipeline's native-Claude execution path reads it off the spawner it has already resolved for that stage, validates it against the known set, and renders `--effort <value>` after `--model`, dropping the flag entirely rather than forwarding anything unrecognised. `services.ResolveEffort`, added to answer "what effort is in force, and which tier of the task → project-stage → project → global-stage → default chain supplied it", has **no production caller**: the spawn path reads the already-resolved spawner directly instead of resolving a second time, so effort is now savable, editable, and honoured at spawn time, but no surface yet displays where a resolved value came from.
- **Obsidian vault as a resource-registry Application** (`server/internal/apps/obsidian`), registered at boot (`server/serverapp/di.go`) with four capabilities — `obsidian.read`, `obsidian.search`, `obsidian.write`, `obsidian.delete` — all class `reach` (vault content reaches the model, the same reasoning that puts `WebFetch` in `reach`) and enforceable only at the **server** point, since access runs through an in-process client rather than anything a spawn allow-list or a `PreToolUse` hook can see. `obsidian.write` and `obsidian.delete` are marked `Reversible: false` in the catalogue, but **nothing reads that column yet**: `capability.CapabilityView` carries only `Name`, `Class`, and `EnforceableBy`, and `capability.Decide` never references reversibility. The rule this was meant to back — a preset alone must not satisfy an irreversible capability — needs two design decisions nobody has made (extending `Decide`'s contract, and a way to tell a preset-applied grant from an explicit human one, which the grant schema cannot express today) and was **descoped**; the column is written and documented at the write site as reserved for that future decision, the same treatment `ServerEnforcer` got on the prior increment. The vault client (`obsidian.Client`) fails closed in its constructor on anything unparseable — an empty `BaseURL`/`VaultRoot`, a non-`https` scheme, an unrecognised `TLSMode` — and pins the resolved host, IP, and port at construction, so a DNS answer that changes afterwards is refused rather than re-trusted; see [`docs/guides/security.md`](docs/guides/security.md#obsidians-tls-trust-model) for the three TLS modes and what `insecure-loopback` actually restricts. `obsidian.IndexNotes` turns vault notes into memory pointers — a short first-line summary plus the note's own path, never its body — gated by the same `memory.write` capability check an agent's own write would go through, plus new `obsidian.search` and `obsidian.read` checks before, respectively, searching and reading each note, and confines every result to the client's configured `VaultRoot` before reading anything, because the upstream search endpoint is vault-wide by design. **State this plainly: that confinement is `VaultRoot`-only, not per-grant-scope** — a `memory.write` grant's scope (global/project/application) carries no vault-path semantics, so a grant narrower than the configured root does not narrow what gets indexed. **Nothing constructs this client in production today.** The settings surface meant to carry `BaseURL`, `APIKey`, `VaultRoot`, and `TLSMode` — a settings route, a settings panel, and encrypted storage for the API key — was not built on this branch; `obsidian.Register` only writes the registry identity and the four capability rows. `obsidian.IndexNotes` is the one production path that exercises `obsidian.search` and `obsidian.read` — a missing grant denies the run, the same as `memory.write` already did. `obsidian.write` and `obsidian.delete` remain untouched by any caller: nothing calls `Client.Write` or `Client.Delete` in production, and `Client.Read`/`Client.Write`/`Client.Search`/`Client.Delete` themselves still take no capability repos, so a caller that reaches the vault client directly instead of through `IndexNotes` bypasses the gate entirely. The API key that a settings surface would carry has no encrypted home either: the one plugin-agnostic key/value store, `AppSettingRepo`, persists a plain unencrypted string with no masking, and the existing AES-GCM secret path belongs to the subprocess plugin mechanism this Application deliberately does not use. **This is a security-relevant gap** — see [`PRIVACY.md`](PRIVACY.md) and [`docs/guides/security.md`](docs/guides/security.md). Even once a settings surface exists, these capabilities inherit the same gap the memory store already has above: **no `Asker` is wired to the server enforcer**, so an `ask` decision still fails closed to a denial regardless of what `agent-dashboard grants` writes. `IndexNotes`'s `obsidian.search`/`obsidian.read` checks therefore still deny by default on a fresh install, exactly like `memory.write` — the `agent-dashboard grants` CLI below can now grant them explicitly, but nothing does so by default; `obsidian.write` and `obsidian.delete` have no production caller yet for that default to apply to. **Superseded by the entry above:** the settings surface, encrypted API-key storage, the `POST /api/obsidian/index` trigger, and MCP-tool callers for `obsidian.write`/`obsidian.delete` all now exist.
- **Answer a permission prompt from the dashboard.** `agent-dashboard hooks install` registers a `PreToolUse` and a `Notification` hook, after which a session that needs approval is held at the hook instead of stopping at its own terminal prompt: the agent's card in the needs-you band offers **Allow** and **Deny**, and answering releases the run immediately. It works for any session started after the install, including ones launched by hand — hooks are ambient configuration rather than a start-time handshake, which is why ACP and the MCP endpoint cannot reach those sessions. If nobody answers within 25 seconds the hold lapses and the session falls back to asking in its terminal exactly as before; the card then reads **Answer in terminal** and offers a standing rule for future runs instead. Every failure path — dashboard stopped, secret missing, no `curl` — answers "no decision", never "allow", so a broken bridge degrades to today's behaviour rather than approving anything. The hook reads its secret from `~/.claude/dashboard-hooks-secret` (`0600`) rather than from `settings.json`. `agent-dashboard hooks uninstall` removes the entries it installed — the ones running the script out of `~/.claude/dashboard-hooks/` — and leaves other hooks alone; an entry running your own copy of the script from elsewhere is named on stderr rather than deleted, and install refuses rather than replacing it. Nothing is intercepted by default — arm a session from the lock on its card, or from **Intercept next** on a card whose prompt already reached the terminal — because the hook fires *before* Claude Code decides whether to prompt at all, so holding every call would stall every session on the machine. The script is extracted from the binary into `~/.claude/dashboard-hooks/` rather than read from a checkout, so a moved binary, a deleted clone or a tarball install all keep working; re-running the command repairs a registered path that has gone stale. New requests are announced to screen readers through a live region, focus stays inside the card when a hold lapses out from under it, and the shared `--success` token moved from `green-600` to `green-700` so white text on every success button reaches WCAG AA (3.2:1 → 4.9:1). The bridge is not mounted under `DASHBOARD_AUTH=none`, where "a human decided" would reduce to "any local process decided" — sessions prompt in their terminals there, as they do without it. Your own `permissions.deny` rules stay the floor: a hook "allow" short-circuits Claude Code's permission evaluation entirely, so the bridge reads the deny lists from your user and project `settings.json` first and offers no **Allow** for a call they cover — the card names the rule instead, and the endpoint refuses an allow for it regardless of what the client sends.

- The running build names itself. `GET /api/system/health` reports a `version`, and the status bar's system panel shows it as **BUILD**. The dashboard reaches its user through three build paths — the `serve` CLI, the macOS desktop shell, and a release build — each embedding its own copy of the SPA, and only the CLI was ever version-stamped: a window from a months-old bundle was indistinguishable from a fresh one, which is how a rebuild can look like it did nothing. Both desktop build tasks now stamp the same `git describe` value the server binary gets. An unstamped local build reports `dev`, which is a useful answer in itself.
- Subagents open like the session they belong to. A subagent used to be a read-only line in the agent modal's details drawer — id, type, last tool — with no way to see what it actually did. Clicking one now shows its full transcript in place of the session transcript, with the same rendering, and a **Back to session** control. The session-output endpoint resolves subagent transcripts too (they live one level deeper, under the parent session's own directory), so no new endpoint was needed. Read-only by design: a subagent has no channel to send to, and its transcript is not polled.
- A **your turn** marker on live agents that have stopped on their own — the session is done for now and will not move again until you send it something. It reads the same `working` signal the animated **Working** badge does, so an agent blocked on a permission prompt or an open question is not marked (those wait for an answer and already surface in the needs-you band), and a finished agent is not marked either (its process is gone, so there is nothing to continue). Shown on both the card and the roster row.
- Maskable PWA icon: the web-app manifest now declares a `purpose: 'maskable'` 512×512 icon, so installed-PWA launchers on Android/Chrome render the icon in their adaptive shape without letterboxing. Also added a `docs/launch-checklist.md` operator playbook for the public launch (release tag, README hero, `good-first-issue` labels, social preview, awesome-list submissions).
- Guided first-run onboarding flow: opens automatically on first launch (until dismissed) and walks through detecting the Claude Code CLI (version or install command), one-click MCP registration to connect the dashboard to Claude (with a copy-the-command manual fallback), and discovering existing Claude sessions to make one controllable with one click via `--resume`. New endpoints: `GET /api/onboarding/status`, `POST /api/onboarding/register-mcp`, `PATCH /api/onboarding/status`; new `onboarding.completed` setting tracks whether the flow has been dismissed or finished.
- **Re-run first-run setup** button under **Settings → Appearance**: re-opens the onboarding flow on demand without affecting the persisted `onboarding.completed` setting, so you can revisit the CLI/MCP/session steps after the initial run.
- A macOS desktop app shell (`desktop/`, [wails](https://wails.io) v2, `//go:build darwin`): one binary that both runs the dashboard server and opens a native WKWebView window for it, no sidecar process. It starts the existing HTTP server in-process via the new `server/serverapp` package (extracted so the server can be run either out-of-process by the `serve` CLI or in-process by a host like the desktop shell), waits for `/api/system/health` before opening the window, and cancels the server context on quit for a graceful drain. An embedded bootstrap page redirects the webview to the address the server actually bound (`http://127.0.0.1:13120` by default), so the page runs on that loopback-http origin and the server's existing same-origin mutation guard applies unchanged. Non-macOS builds get a stub `main` that points users at `agent-dashboard serve`. Smoke-buildable today via `cd desktop && CGO_ENABLED=1 go build -o agent-dashboard-desktop . && ./agent-dashboard-desktop`. Distributable packaging via `task desktop:dist`/`desktop:dmg` (wails CLI) produces an unsigned `bin/Agent Dashboard.app`/`.dmg`; signing and notarization are a documented, deferred operator step — see [`docs/desktop-distribution.md`](docs/desktop-distribution.md).
- Live **Terminal** tab in the agent detail modal for spawned/live-injectable sessions — a real `xterm.js` terminal streamed over a WebSocket, so you can watch and interact with the session's actual pty output instead of a parsed transcript. New endpoint: `GET /api/agents/{pid}/terminal` — an authenticated WebSocket proxy that bridges the browser to the session's pty broker.
- Answer a Claude **AskUserQuestion** prompt directly from the Terminal tab: an overlay detects the question from the live terminal screen and renders the options inline (radio for single-select, checkboxes for multi-select); **Send answer** drives the session's selector with real keystrokes over the same WebSocket — the option's number hotkey for single-select, and Space/Down/Enter to toggle and confirm for multi-select (Claude's selector takes keypresses, not free text). Because it drives the pty directly, it works over any transport (tmux or the built-in pty broker), not just tmux.
- Answer an **AskUserQuestion** from the main **needs-you band** too, not only the Terminal tab — questions are detected server-side from the session's rendered screen (the pty broker's scrollback, or `tmux capture-pane`), so they surface as an answerable card wherever the agent appears. Answering posts to `POST /api/agents/{pid}/answer-question`, which derives the keystrokes and delivers them over the session's injection path (pty broker or tmux). Any injectable session — dashboard-spawned or started with `agent-dashboard live` — is now fully controllable from the dashboard.
- End-user install paths: binary (one-liner via `install.sh`), Homebrew cask on macOS (`brew install lx-wnk/tap/agent-dashboard`), and Docker (`ghcr.io/lx-wnk/agent-dashboard`) — no Go or build tools required at runtime. See [`docs/guides/install.md`](docs/guides/install.md).
- One-command MCP connect: the API key dialog's **CLI command** block copies a `claude mcp add --scope user --transport http …` one-liner with the generated key substituted. Documented in [`docs/guides/mcp.md`](docs/guides/mcp.md#connect-the-dashboard-to-claude).
- GoReleaser config now publishes a Homebrew cask to `lx-wnk/homebrew-tap` and a multi-arch Docker image (`linux/amd64`, `linux/arm64`) to GHCR on `v*` tag pushes. See [`docs/RELEASING.md`](docs/RELEASING.md).
- Issue → Task seeding: paste a GitHub or Jira issue reference into the **Import from issue** field in the New-Task form and click **Fetch** to pre-fill the title, slug, and description (issue body plus a `Source: <url>` line). GitHub personal-access tokens and Jira API tokens are stored encrypted at rest (reusing the existing AES-GCM secretbox) and masked in the settings UI (**Settings → Tracker**); unchanged secrets are preserved on save. New endpoints (JWT group, same-origin checked): `POST /api/tracker/fetch {ref}` resolves a GitHub or Jira issue by reference (full URL, `owner/repo#n`, bare `#n` with a configured default repo, or a Jira `KEY-123` / browse URL) and returns a normalised Issue (title, body, URL, labels), with typed error mapping (400 bad ref / missing config, 401 auth rejected, 404 not found, 502 upstream); `GET` / `PUT /api/tracker/settings` manage the encrypted tracker credentials.
- Per-turn checkpoint / revert for pipeline-task worktrees. A gitignore-aware filesystem watcher (~2 s debounce) captures each agent turn as a hidden git ref (`refs/checkpoints/<task>/<seq>`) using a temporary index, so the agent's index and HEAD are never touched. The task modal's new **Checkpoints** tab lists snapshots (newest first) with a Revert button; reverting kills the live agent, saves the current state as a recoverable pre-revert checkpoint, restores the worktree to the chosen snapshot, and parks the task (`awaiting_user`) for manual resume. Endpoints: `GET /api/tasks/{id}/checkpoints` and `POST /api/tasks/{id}/checkpoints/{cpId}/revert`; live updates over the `checkpoint_added` SSE event. Retention prunes to 50 checkpoints per task; refs and rows are cleaned up when the worktree is removed.
- `create_project` MCP tool (scope `tasks:write`) — an agent that finds no matching project in `list_projects` can now create one itself instead of stopping for a human to open **Settings → Projects**. Takes `slug` and `name` plus the optional `description`, `color`, and `defaultSpawnerId` fields, rejects a slug that is already taken, and returns the same project shape as `list_projects`. `create_task` additionally accepts `projectSlug` as an alternative to `projectId` (the two are mutually exclusive) — it resolves an existing project only, so a mistyped slug fails loudly instead of quietly creating a second project. A project created this way starts with no folders and never carries a `setupCommand`: that field runs an arbitrary `sh -c` in the worktree, so the HTTP writer restricts it to admins (when `auth.mode` is not `none`) and the MCP tool — reachable with a plain `tasks:write` token — omits it from its input schema unconditionally. The create is published on `/api/projects/stream` as a `project_created` event, so an agent-created project shows up in the dashboard live rather than on the next reload. `list_projects` and `create_project` report `hasSetupCommand` as a boolean. The command text is deliberately never put on the wire: it routinely carries registry tokens and `tasks:read` is enough to call `list_projects`.
- `create_project` (MCP) is attributed: every successful call writes a `project_created` audit event (target `project:<id>`, metadata `{slug, source}`) and logs a `mcp: project created` line carrying the slug, the project id, and the id of the API key that made the call. A top-level entity created by a bearer token previously left no trace of who created it. The name and description are agent-supplied free text and are recorded in neither sink.
- `create_project` (MCP) rejects unknown arguments instead of ignoring them. Its schema declares `"additionalProperties": false` and the handler enforces it (the JSON-RPC layer does not validate arguments against the advertised schema), so a misspelt key — or a smuggled `setupCommand` — fails the call naming the offending key and the accepted set, rather than being silently dropped. It is the only tool with this rule: the other 40 have live callers whose harmless extra keys must not start failing.
- Project `name` and `description` are now length-bounded on both writers — 200 and 10 000 characters, matching the `task.title` cap and the task-description cap the SPA already enforces. Neither `create_project` (MCP, reachable with a plain `tasks:write` token) nor `POST /api/projects` bounded them before, and no MCP tool can rename or delete a project afterwards. The limits live in `server/internal/validation` so the two writers cannot drift apart.
- `create_project` (MCP) now names the folderless dead end it creates: the tool description says so, and the success payload carries it as `nextStep`. A project created over MCP has no folder, and the dashboard's New-Task form takes its working directory only from a project's folders — so that form cannot be submitted for the new project until someone adds a folder under **Settings → Projects**. The `project_created` SSE payload is unchanged (the advisory is for the tool caller, not the SPA).
- `wait_for_port` MCP coordination tool — agents can block until a TCP service is reachable (max 300 s, returns `reached`/`timedOut`).
- Per-project `setup_command` field — run once in the worktree after creation; failure logs and continues.
- Prompt templates with `{{placeholder}}` fill-in — picker in the full prompt input, CRUD at `/api/prompt-templates`.
- Natural-language schedule phrases that the built-in rule set can't parse now fall back to an LLM translator (`claude -p`), so expressions like "first weekday of every month at 7am" resolve to a cron instead of failing. The LLM's output is re-validated as a 5-field cron before use.
- Token pricing for `gpt-5`, `gpt-5-codex`, `gemini-2.5-pro`, and `gemini-2.5-flash` so Codex and Gemini agents report a real cost instead of "unknown".
- Eval drift alerts are pushed live to the dashboard over SSE (`eval_drift` event) the moment a drift is detected, instead of only appearing on the next 60-second poll.
- `GET /api/usage` — rolling-window token and cost aggregator (5h session-equivalent, 7d weekly-equivalent) derived from session JSONLs across all configured Claude config dirs; replaces the permanently dead `/api/quota` endpoint.
- `usage.budget.session` and `usage.budget.weekly` settings (token counts; 0 = unset) to optionally derive a % bar in the status bar.
- Status-bar USAGE segment: worst-case % bar when a budget is set, compact consumption text otherwise; popover shows both windows, per-account breakdown when multiple accounts exist.
- `POST /api/admin/restart` triggers a validated, graceful restart. The endpoint refuses with **409** if an active `auth_provider` plugin is currently unhealthy (restarting in that state would cause an auth lockout on the next boot). Default `DASHBOARD_RESTART_MODE=reexec` replaces the process image in place (no supervisor needed); set `DASHBOARD_RESTART_MODE=exit` and run under systemd (`Restart=always`), launchd (`KeepAlive`), or a wrapper loop (`while true; do ./bin/agent-dashboard serve; done`) for supervised setups. Activating an `auth_provider` plugin requires a restart to apply — auth is boot-wired.
- `agent-dashboard plugins list` / `disable <id>` / `enable <id>` CLI commands operate directly on the SQLite database (no HTTP, no auth gate), so a broken `auth_provider` plugin that prevents boot can be disabled offline. The change applies on next server start; lifecycle hooks are skipped (it is a recovery tool, not the normal activate path).
- Disabling a **UI-extension plugin** now prompts a page reload so the browser fully unloads its ES module code (browser ES modules persist in the registry until the page is reloaded).
- **Per-plugin settings UI**: a schema-driven form (`Settings → Plugins`) lets you view and edit each plugin's settings inline. Fields are rendered per type (`string`, `url`, `int`, `bool`, `enum`); secret fields are masked and unchanged secrets are preserved on save (the masked sentinel is sent to the server, which keeps the stored value).
- Plugin author SDK (`plugin-sdk/`): a `plugin.json` JSON Schema (draft-07) for editor
  autocomplete and validation, TypeScript UI-addon types (`addon.d.ts`) mirroring the
  host slot contracts, and a quickstart README covering the backend contract, lifecycle
  hooks, UI addon authoring, and per-capability liveness.
- Consolidated plugin developer guide (`docs/plugin-guide.md`): full manifest v2
  reference (slots with priority/mode, settings with encrypted-secret masking,
  lifecycle hooks, permissions), capability liveness table, slot composition modes
  (sibling/override/extend + parent handle), per-plugin settings UI, enable/disable
  lifecycle endpoints, offline CLI hatch, and a "Build your first plugin" walkthrough.
- Plugin process groups with group-kill (no orphaned child processes), suppression of
  restarts for intentionally stopped plugins, and crash supervision that marks a plugin
  unhealthy (HTTP 503) instead of silently removing it from the dispatcher.
- Automatic **`.env` file loading**: a `.env` in the working directory is now read at
  startup for both `task dev` (via air) and `./bin/agent-dashboard serve`. It uses the
  same `DASHBOARD_*` keys as the environment, and an explicit shell `export` always
  takes precedence over a file value. Previously `.env` was only a template for manual
  `export`/`cp` and was ignored by the server process — so settings like
  `DASHBOARD_FORCE_WORKTREES` had no effect unless exported by hand.
- Task **dependency enforcement**: the orchestrator now actually gates on `add_dependency`/`POST /api/tasks/{id}/dependencies` relationships. A task is not started until every upstream dependency reaches its `required_stage` (lazy picker-gate — no extra state, picked up automatically once the upstream finishes). When an upstream is **cancelled**, the downstream's `on_cancel_action` decides the outcome (`cancel` cascades the cancellation down the chain, `start` lets it proceed, `on_hold` leaves it parked). Enriched tasks now report `isBlocked` (waiting on an unfinished upstream) and `isUnsatisfiable` (an upstream reached a terminal stage it can never satisfy), which the board and task cards already surface. The REST create default for `onCancelAction` is aligned to `on_hold` to match the MCP tool and schema.
- Coordination primitives: shared **scratchpads** and lease-based **locks** via a new `agent:coord` MCP scope (`write_scratchpad`/`read_scratchpad`/`list_scratchpad`/`acquire_lock`/`release_lock`), with a read-only Coordination tab in the task modal. Locks use lazy lease expiry (no background sweep).
- Opt-in **plan-review** pipeline stage (`planMode`, default off): after the concept is approved, a planning agent auto-generates the execution plan and self-reviews it in one pass; the vetted plan is surfaced for your approval (Approve / Reject + feedback) before the implementation stage edits any files. Configurable per-task and as a per-project default.
- Plugin **lifecycle foundation**: plugin state is now DB-backed (`plugin` table, states `discovered`/`inactive`/`active`) with a lifecycle API (`GET /api/plugins`, `POST /api/plugins/{id}/{install|activate|deactivate|uninstall}`) and per-plugin settings (`GET`/`PUT /api/plugins/{id}/settings`). Manifest v2 adds optional `slots`, `settings`, `lifecycle`, and `permissions` sections to `plugin.json` (v1 manifests still load). Secret settings are encrypted at rest with AES-256-GCM using a new `DASHBOARD_SECRET_KEY` master key (auto-generated to `~/.claude/dashboard-secret.key` if unset) and masked in the API. The legacy `plugins.enabled` setting is superseded by the `plugin` table (migration is automatic on first boot). Activation's serving effects (route mounting, UI slots) land with SP2/SP4.
- `anthropic` spawner adapter — run pipeline stage agents and refinement chat against the Anthropic Messages API via an out-of-process binary, keeping the SDK out of the server module. Requires `ANTHROPIC_API_KEY` in the server env and the `anthropic-spawner` binary on `PATH` (or `DASHBOARD_ANTHROPIC_SPAWNER_CMD`). Default model: `claude-opus-4-8`.
- Documented using the existing `openai` adapter with OpenAI-compatible multi-model gateways (OpenRouter, Together AI, Inflection, …) — set `base_url` + `api_key_env` + `default_model`, no new code needed.
- Restructured dashboard toolbar: the controls that **narrow** the roster (search, filters) sit on the left, the ones that **arrange** what is left (sort, group, density) on the right. The roster search moves out of the topbar into the toolbar — it only ever filtered agents, so on every other view the topbar field promised a filter that never ran. Project and spawner filters move behind a **Filter** button carrying a count badge, and every active narrowing — including the search term — is mirrored as a dismissible chip below the toolbar with a **Clear all** that also empties the search. A result counter (`12 / 40 agents`) appears as soon as anything narrows the roster, the spawner filter is dropped entirely when no spawner is configured, and the density toggle moves into a **⋮** overflow menu. The settings gear returns from the topbar to the sidebar footer, beside the other global actions (Install, Sessions, Theme); the topbar keeps only the view title, its CTA, and the offline badge.
- **Group by spawner** on the dashboard roster, and a spawner filter that finally matches every session rather than only orchestrated ones. Each agent is attributed to a configured spawner server-side, from the pipeline task it runs, or — for sessions started outside the dashboard — from the `CLAUDE_CONFIG_DIR` its live process carries, which is what separates one profile from another when both run the same command. Reading it from the running process is exact: two config dirs that symlink to the same store cannot be told apart from session files alone. It is the same value the process scan already reads to resolve a session's config root, so it costs no extra process inspection and it survives the process exiting — a finished session keeps the profile it ran under instead of dropping onto the default one. Agents that match no spawner collect in a trailing **Unassigned** group, and a group whose attribution was derived rather than recorded is marked with `~`. A session whose process environment could not be read at all (already exited, owned by another user, a hardened `/proc`) lands in **Unassigned** too rather than on the default spawner — `~` always marks a value that was read, never a fallback. A spawner that declares no `CLAUDE_CONFIG_DIR` targets the user's default profile, `~/.claude`; the config dir the server process itself was started under is a different thing and is not treated as that default. The grouping option is hidden while the spawner filter narrows the roster to a single spawner (it would be redundant) and returns once the filter is cleared. New agent fields: `spawnerId`, `spawnerName`, `spawnerSource`.
- Collapsible agent groups — when a grouping (project/status/model/spawner) is active on the
  dashboard roster, click a group header to collapse or expand it. On first load only
  the highest-priority non-empty group is expanded (Active, else Waiting, else Idle,
  else Finished); the rest start collapsed. Collapsed state is remembered per grouping
  mode in localStorage and survives live re-renders.
- Opt-in monitoring of Codex CLI, Gemini CLI, Junie CLI, and pi.dev agents via a declarative provider registry (off by default; enable via the `providers.enabled` runtime setting or custom descriptors in `DASHBOARD_PROVIDER_DIR`). pi.dev's per-session JSONL (`~/.pi/agent/sessions/`) carries token usage, model, provider, and cost per turn.
- Ollama-backed local models are now reported at $0 cost instead of unknown.
- Agents now show an animated **Working** badge while actively generating — derived from conversation turn-state plus live tmux/pty output — distinct from the idle/waiting staleness states.
- Agents spawned from the dashboard now run as interactive **live** sessions (tmux when available, headless pty broker otherwise) so you can converse with them live, instead of a one-shot run. The spawned agent's chat opens automatically once it appears.
- Controllable (channel/MCP-connected) agents now remain on the dashboard as a
  dismissable **Finished** card after their process exits, so results no longer
  disappear into the Sessions list. Click ✕ on a finished card to dismiss it
  (removes its channel discovery file). Finished cards are tracked per server run
  and appear in the card grid view. (#192)
- Live subtasks on agent and task cards — token usage, duration, and latest output for each active subagent are parsed from JSONL in real time and surfaced directly on the roster cards; child-task summaries are projected onto enriched tasks, gated on pid-liveness (PR #203).
- Per-stage spawner and model configuration — choose which engine (e.g. Codex for
  `self_review`, Claude Code for `implementation`) and which model runs each of the
  three agent stages (`implementation`, `self_review`, `finalization`). Settings are
  scoped globally or per-project; the resolution chain is: task `spawner_id` →
  project `stageSpawner.<stage>` → project `default_spawner_id` → global
  `stageSpawner.<stage>` → claude-default (for spawner); spawner `ModelOverride` →
  task `metadata.model` → project `stageModel.<stage>` → global `stageModel.<stage>`
  → coded default (for model). New endpoints: `stageSpawners` field added to
  `GET`/`PUT /api/pipeline/config`; new `GET`/`PUT /api/projects/{id}/pipeline-config`
  for project-scoped overrides. Drag-and-drop task reordering within stage columns
  is now supported. (PR #206)
- Per-task autonomy levels (`manual`, `spec_gated`, `full`) over REST and MCP
  `create_task`/`update_task`. New tasks default to `spec_gated`, which auto-approves
  permission requests (blanket Bash, audit-logged as `permission_auto_approved`);
  `manual` keeps the human-gated behaviour. Pre-existing tasks without the field stay
  gated. A self-describing `availableActions` set and an `approve_all_pending` control
  let blocked tasks be unblocked in one step (PR #200).
- Natural-language cron scheduling for pipeline tasks — describe a cadence in plain
  English ("every weekday at 9am") and the dashboard stores it as a cron expression,
  firing offline and deterministically. New REST endpoints under `/api/schedules`
  and MCP tools `list_schedules` / `manage_schedule` (PR #197).
- Multi-provider session resolution — Codex and Gemini agents resolve their JSONL
  session logs under each provider's own config dir, so foreign CLI agents can surface
  in the roster (cost reported as unknown until a real foreign session schema lands)
  (PR #199).
- In-dashboard `~/.claude` config explorer — browse and edit skills, slash commands,
  and memory files from the UI without leaving the dashboard (PR #190).
- Git worktree panel — create and remove worktrees for pipeline tasks directly from
  the task UI (PR #193).
- Opt-in HTTP hook receiver (`/api/hooks/event`, `/api/hooks/pre-tool`) for
  per-event agent rescans, gated by an auto-generated shared secret — keeps the
  no-hooks default intact. The receiver records a bounded per-session history of
  recent lifecycle-hook events (`PreToolUse`, `PostToolUse`, `Stop`, …) and
  surfaces them in the agent modal under **Hook events**; payload previews are
  truncated to 512 bytes and nothing is written to disk. The cap is controlled
  by `DASHBOARD_HOOK_EVENTS_PER_SESSION` (default 50). Stale sessions are swept
  on every record call (PR #205).
- Frontend plugin slot framework — named extension points (`refinement`, `settings`,
  and others) that sidecar plugins can mount custom UI into (PR #168).
- Lean, front-door `README.md` and a structured `docs/` tree (configuration, MCP,
  agent control, security, statusline, architecture overview).
- Release tooling: GoReleaser config + `release` workflow producing cross-compiled
  binaries (macOS/Linux, amd64/arm64) with the SPA embedded, plus an `install.sh`
  one-liner installer.
- `agent-dashboard --version` (version injected at build time via ldflags).
- `task setup`, `task doctor`, `task build:frontend`, and `task build:all` to lower
  the contributor entry barrier.
- Community health files: `SECURITY.md`, `CODE_OF_CONDUCT.md`, Dependabot config,
  and `FUNDING.yml`.
- Settings → Providers panel to enable or disable Codex/Gemini/Junie monitoring per provider, persisted in the database and applied within one scan tick.
- **DB-backed settings store**: operational config (auth mode, rate limits, scan
  intervals, plugin/provider enablement, `git.allowPush`, `git.allowPull`,
  `spawn.allowedCommands`, `worktree.force`,
  eval/drift tuning, …) now lives in the database `app_setting` table — the single
  source of truth is `server/internal/settings/registry.go`. Edit it in the new
  generic **Server** settings panel (registry-driven) or via the new direct-DB
  `agent-dashboard settings list|get|set` CLI. The CLI edits the SQLite file directly, so
  it works while the server is down and is the **lockout-safe recovery path** (e.g.
  `agent-dashboard settings set auth.mode none`). DB resolution: `--db` → `DASHBOARD_DB_PATH`
  → `~/.claude/dashboard-tasks.db`.
- Live **plugin enable/disable** via the lifecycle API — discovered plugins now default
  to **all-off** and are activated individually via the backend lifecycle endpoints
  (`POST /api/plugins/{id}/activate|deactivate`), applied live without a server restart.
  The Plugins settings-panel toggle that drives these endpoints is wired in SP4b.
- **Server reconnect overlay** — a full-screen blocking overlay appears automatically
  whenever the server goes down (e.g. during a restart). The UI polls
  `/api/system/health` every 1.5 s and reloads the page on the first successful
  response, recovering without any user action. Plugins with the `auth_provider`
  capability are **boot-wired** (they affect server startup) and cannot apply live:
  after toggling an `auth_provider` plugin the settings panel shows a
  "Restart required to apply" badge and a **Restart server** button, which triggers
  the restart and hands off to the reconnect overlay automatically.
- Plugin slot composition -- multiple addons targeting the same slot can now declare a
  `priority` (number; higher renders outer/first) and a `mode` (`override` to own the
  slot exclusively, `extend` to wrap the lower-priority chain). An `extend` addon's
  `mount(el, ctx, parent)` receives a `parent` handle it may invoke to compose the addons
  below it. Addons with no mode remain independent siblings and are unaffected.
- **A pipeline agent's MCP calls now carry task and routine identity.** Every stage run
  the pipeline spawns reached `/api/mcp` through the same machine-wide key a person
  registered by hand, so `capability.Decide`'s `task`/`routine` context levels were
  reachable for a grant to name but nothing a request ever sent — see the entry above on
  the routine context, and its own note that `DASHBOARD_MCP_TOKEN` (a single value from
  config, handed to every spawn) identifies no task and never will: it authenticates the
  channel bridge's three callback tools (`dashboard_reply`, `request_permission`,
  `set_stage_output`), a different endpoint from the one the Decider governs, and stays
  exactly that. Instead, immediately before `--mcp-config` is written
  (`server/internal/pipeline/stage_handlers.go`), the spawner now mints a second,
  ephemeral credential — a `kind = "stage_run"` row on the existing `api_key` table
  (`stage_run_id`, `expires_at` added as nillable-or-defaulted columns; the human-facing
  `kind = "user"` rows are unaffected and `ApiKeyRepo.List`/`list_api_keys` keep filtering
  to them) — and writes it into a second `mcpServers` entry, `dashboard-tasks`
  (`server/internal/channelconfig`), alongside the existing stdio `dashboard-channel`
  one. That file is now passed with **`--strict-mcp-config`**, without which the whole
  narrowing above would have been decorative: `--mcp-config` alone *adds to* the user- and
  project-scope registrations, and onboarding registers `dashboard-tasks` at user scope
  under the very same name with the broad, long-lived key (`tasks:read`, `tasks:write`,
  `pipeline:control`) — so a stage agent could simply have used that one instead. Strictness
  cuts an agent off from the servers its operator configured, so the user-scope servers from
  `~/.claude.json` are merged into the spawn's file (`server/internal/claudeconfig`, the
  reader `GET /api/onboarding/status` already used) with the dashboard's own two names
  reserved: a user-scope `dashboard-tasks` or `dashboard-channel` entry is dropped, never
  merged, which is the point of the exercise. A `~/.claude.json` that is missing, unreadable
  or malformed is logged and skipped, never fatal to a spawn. A project-scope `.mcp.json` is
  not carried over. `mcp.CallerResolver.Contexts` (`server/internal/mcp/caller.go`) resolves that key's
  `stage_run_id` through `stage_run.task_id → task.routine_id` into
  `[{task, <id>}, {routine, <id>}]` and every call to the memory MCP tools
  (`memory_search`, `memory_write`) and the four `obsidian_*` tools now passes it to
  `Gate.Authorize` as extra context, so a `--scope task:<id>` or `--scope routine:<id>`
  grant decides those calls the same way the automatic memory push already did — no other
  MCP tool goes through the capability gate today, so this is the two tool families that
  do. Every issued key gets one fixed scope set — `tasks:read`, `agent:coord`,
  `memory:read`, `memory:write`, `obsidian:read`, `obsidian:write` — six scopes, deliberately
  **not** eight. An earlier draft also included `pipeline:control` and `tasks:write`, but since
  no other MCP tool resolves through the gate, either one would have been a capability granted
  outright rather than one the gate narrows later: `pipeline:control` reaches
  `grant_permission`, `resolve_permission_request` and `approve_all_pending` on scope alone,
  letting a spawned agent approve its own spec and resolve its own permission requests, and
  `tasks:write` reaches `manage_task`'s `grant_permissions` action, letting it widen its own
  permissions. `keys:manage` is excluded for the same family of reason: a spawned agent able to
  mint its own keys could mint one with no stage run and escape its own attribution. It carries
  two independent expiries:
  the orchestrator revokes it (`RevokeForStageRun`, `active = false`) the moment its stage
  run reaches a terminal state, and independently `expires_at` (the stage's timeout plus a
  five-minute buffer, `mcp.StageKeyTTLBuffer`) is enforced by `ApiKeyRepo.GetByHash` itself
  — an expired row is refused with the same message an unknown token gets, before it ever
  reaches a tool handler. A new hourly sweep (`mcp.SweepExpiredKeys`, started in
  `server/serverapp/serverapp.go` alongside the cost-history importer and the
  drift-detection scanner) hard-deletes expired `stage_run` rows — deleted, not
  tombstoned, since each names a stage run that already has its own record. A credential
  that fails to mint is not fatal to the spawn: the agent then runs with no task API access
  at all, since the strict config shuts the user-scope one out. **Still out of scope, deliberately:**
  interactive sessions started by hand keep using the machine-wide key and resolve with no
  task context — the `agent_session` context level still has no producer — and the
  spawner's own `--allowedTools` rendering (`SpawnEnforcer`) still builds synthetic grants
  from `task_permissions` at one fixed context and never reads the `grants` table; neither
  changes here. See [`docs/guides/security.md`](docs/guides/security.md#capabilities-and-the-permission-gate)
  and [`docs/guides/mcp.md`](docs/guides/mcp.md#pipeline-agents-already-have-a-key).

### Deprecated
- `GET /api/config/memory` (the config explorer's list of a project's context files — `CLAUDE.md`, skills, etc.) is superseded by `GET /api/config/context-files`, which returns the byte-identical response. The old route keeps answering (it now delegates to the new one and logs a deprecation notice once per process) so nothing breaks today, but new code should call the new route — freeing the name "memory" for the new memory store (see Added, above) is the reason for the rename. Removal is deferred to a later release, after clients have had time to move.

### Changed
- The CLI folds into the single `agent-dashboard` binary — no more separate `dashboard` binary or `task build:cli` target. The CLI lived in its own main package built only by a local Taskfile target, so no release artifact ever shipped it; its subcommands include the offline lockout hatches (`agent-dashboard settings set auth.mode none`, `agent-dashboard grants add`, `agent-dashboard plugins disable`) that open SQLite directly and are meant to work while the server is down — exactly when building from source is not an option. Folding the CLI into `agent-dashboard` means every subcommand now ships in the same release artifact as the server, so a user who installed a release (not built from source) has the recovery path available too.
- `task lint` and `task fmt` now agree with the CI job they exist to predict. `task lint` reported 967 phantom `undefined` errors whenever a Go newer than `.go-version` was on `PATH`: the newer toolchain writes export data golangci-lint cannot decode (`export data version 4 is greater than maximum supported version 2`), so every package failed to typecheck. `task fmt` had the mirror-image problem — it rewrote a file CI considers correctly formatted, because `gofmt` and the formatter `task lint` actually runs disagree on trailing-comment alignment in composite literals. The two failures have different causes: formatting depends on the toolchain golangci-lint was **built** with, typechecking on the one present at **run** time. A single top-level `GOTOOLCHAIN` derived from `.go-version` covers both, rather than an `env` block per target.
- The agent modal drops its bottom drawer. Five unrelated things — tool timeline, hook events, tasks, subagents, a token table — used to share one 200px scrolling panel behind a `<details>` summary, next to a **Waterfall** tab squeezed into 300px and a **Terminal** tab. Now: session context (tasks, subagents, tools, hooks) sits beside the transcript, because it is what you read *while* reading the transcript; the transcript gets the full modal height; the token breakdown moved behind the same ⓘ affordance the agent card already uses; and the bottom is the prompt input alone.
- The live terminal moved from that drawer to the agent card — a **⌨** action on any session the dashboard can drive, opening the terminal at full size instead of in a 320px tab.
- The `docs/` tree separates documentation from history. It carried 93,589 lines, of which 93% were dated specs and implementation plans under `docs/superpowers/` — a directory named after the tool that produced them, listed nowhere in the docs index, sitting beside 4,600 lines of actual product documentation. Those plans and specs move to `docs/archive/`, which says what they are and states plainly that they are not maintained. The three live harness contracts move out of that archive to `docs/harness/`, and the single `docs/concepts/` page joins `docs/architecture/`. The docs index now lists everything the tree contains, including all twelve ADRs — it previously listed three. No document was deleted, and archived files keep their original internal links.
- The **All clear** state of the needs-you band is a quiet line instead of a filled green banner. Nothing waiting on you is the normal case; a full-width banner made the band look equally loud whether or not anything actually needed attention, and it competed with the roster below it.
- CI job matrices all derive from one place. The module list was hard-coded in six spots across `ci.yml`; a `matrix` job now holds `WORKSPACE_MODULES` and `PLUGINS` once and every other job's matrix reads its outputs, with the security list computed from the other two so it cannot drift. Adding a plugin means one entry in `PLUGINS`. (The lists live in a job output rather than a workflow-level `env:` because GitHub does not expose the `env` context to `strategy.matrix`.) Alongside it, the Go toolchain is pinned once in a new **`.go-version`** file that every `setup-go` step reads, and the `go` directives in the nine module files drop back to a plain `go 1.26` — a minimum requirement, as intended, instead of the de-facto build pin they had become. A toolchain bump is now a one-line change instead of nine, and a **Toolchain consistency** step fails the build if any module drifts from `.go-version` or grows a `toolchain` directive (Go has no mechanism to inherit the `go` directive, so agreement is checked rather than derived). `dependabot.yml` uses the globbing `directories` key for the same reason.
- Go dependencies moved to their current minor/patch releases: `chi` 5.3.1, `go-sdk` 1.7.0, `fsnotify` 1.10.1, `sqlite` 1.55.0, `libc` 1.74.1, and `x/sync`/`x/sys`/`x/term`. Two consequences worth knowing: chi 5.3.1 registers the HTTP `QUERY` method on wildcard mounts, which adds `QUERY /*` and `QUERY /api/plugins/{id}/proxy/*` to the route set — both are covered by the existing guards, because `RequireSameOriginForMutations` allowlists `GET`/`HEAD`/`OPTIONS` and treats every other method as a mutation, and the plugin proxy is mounted inside the authenticated group. And go-sdk 1.7.0 deprecates MCP logging (SEP-2577), which is how the dashboard delivers messages to a connected agent; the deprecation removes the feature rather than replacing it, so the call is kept and the warning suppressed at the call site until a different transport is designed. The SDK keeps it working for at least twelve months.
- Every dropdown in the app is now a custom listbox instead of a native `<select>`. WKWebView renders native select popups as macOS system chrome — system font, light background, no dark mode — and CSS cannot reach the option list, so the desktop app showed foreign-looking dropdowns throughout. The replacement builds the option list from real DOM (ARIA select-only combobox: `role="combobox"` trigger, teleported `role="listbox"` panel, full keyboard support including type-ahead), so it is themed like the rest of the app in both the browser and the desktop shell. The migration also removed the styling drift the 31 native elements had accumulated: hardcoded `blue-500` focus borders, `focus:` rings that fired on click rather than only on keyboard, ring widths that disagreed with the app's 3px, and one plugin enum field that carried no styling at all — replacing the four ad-hoc vertical paddings with two deliberate size variants, `default` and `compact`, so a select renders at the same height as whichever sibling control it's paired with (WorkflowsView's filter bar, TemplatePicker). The compact selects in TaskDependenciesTab's dependency row are an intentional exception: they match the row's other 11px controls, not the taller `AppInput` beside them.
- Desktop-shell build tasks renamed to the `build:`/`dev:` prefix used by every other task: `desktop:build` → **`build:desktop`**, plus a new **`dev:desktop`** for wails hot-reload. `build:all` stays server-only — the Playwright suite builds through it and must not start needing Xcode CLT — and the new **`build:everything`** adds the desktop shell on top. `build:frontend` is now `run: once`, so a chained build cannot compile the SPA twice.
- Remaining German UI strings in `ProjectSettings` and `RefinementChat` translated to English.
- `AgentModal` is now lazy-loaded, reducing the initial JS bundle size.
- `ProjectRepo.Create` and `Update` accept `setup_command` (nullable).
- `worktree.force` setting now defaults to `true` — pipeline tasks automatically create a git worktree per task without requiring explicit `SourceBranch`. Set to `false` to restore the previous opt-in behaviour.
- Plugin route extensions now serve under `/api/plugins/{id}/proxy/*` and enable/disable
  live via the lifecycle endpoints (`POST /api/plugins/{id}/activate|deactivate` — no
  server restart). The interim `PATCH /api/settings/plugins-enabled/{id}` and
  per-plugin boot-mounted routes are removed.
- Agent card redesign — prominent, readable project name; a compact
  cost · tokens · uptime metric row with the full labeled detail (last activity,
  burn rate, cache costs) moved into a hover ⓘ popover and the agent modal; the
  prompt input is now always docked at the bottom with a larger output area.
- Accessibility: clickable agent rows are now native `<button>` elements, and the
  agent-modal summary uses a higher-contrast token.
- SSE poll and retry intervals are centralized in `src/utils/sse.ts` instead of
  being hard-coded at call sites.
- **BREAKING — env vars moved to DB-backed settings.** These `DASHBOARD_*` variables
  are **no longer read** from the environment; configure them in the Settings UI or
  with `agent-dashboard settings set <key> <value>` (a still-set env var is ignored and
  logs a warning on boot): `DASHBOARD_AUTH` → `auth.mode`,
  `DASHBOARD_PROVIDERS_ENABLED` → `providers.enabled`,
  `DASHBOARD_ALLOW_GIT_PUSH` → `git.allowPush`,
  `DASHBOARD_ALLOW_GIT_PULL` → `git.allowPull`,
  `DASHBOARD_FORCE_WORKTREES` → `worktree.force`,
  `DASHBOARD_SSE_INTERVAL_MS` → `sse.intervalMs`,
  `DASHBOARD_SHUTDOWN_TIMEOUT_SECONDS` → `shutdown.timeoutSeconds`,
  `DASHBOARD_HOOKS_DEBOUNCE_MS` → `hooks.debounceMs`,
  `DASHBOARD_HOOK_EVENTS_PER_SESSION` → `hooks.eventsPerSession`,
  `DASHBOARD_SPAWN_RATE_LIMIT` / `DASHBOARD_SPAWN_RATE_WINDOW_MS` →
  `spawn.rateLimit` / `spawn.rateWindowMs`,
  `DASHBOARD_SPAWNER_ALLOWED_COMMANDS` → `spawn.allowedCommands`,
  `DASHBOARD_INJECT_RATE_LIMIT` / `DASHBOARD_INJECT_RATE_WINDOW_MS` →
  `inject.rateLimit` / `inject.rateWindowMs`,
  `DASHBOARD_COST_SCAN_INTERVAL_MS` → `cost.scanIntervalMs`, and the
  `DASHBOARD_EVAL_*` family → `eval.scanIntervalMs` / `eval.windowHours` /
  `eval.minSamples` / `eval.rateDropPP` / `eval.stddevK`.
- **BREAKING — plugins now default to all-off.** Previously every plugin found in the
  plugin directory loaded automatically; you must now enable each plugin explicitly
  (Plugins settings panel or `plugins.enabled`).
- Apply semantics: plugin and provider enablement apply **live**; all other settings —
  **including `auth.mode`** — require a **server restart** to take effect.

- Interactive question answering no longer reads the JSONL transcript: both the **Needs you** triage band card and the Terminal tab's overlay are driven by the session's live rendered screen (see Added, above). The old flow only worked for tmux-backed sessions and left non-tmux sessions read-only; the new one works for any live-injectable session.

### Removed
- **The admin role is gone, because it never existed.** `is_admin` defaulted to false and no code path outside the generated ORM ever set it, so `RequireAdminOrBypass` rejected *every* authenticated user in JWT mode and passed *everything* through under `DASHBOARD_AUTH=none`. The five route groups it wrapped — settings writes, spawner CRUD, system prompts, plugin lifecycle, and admin restart — therefore protected nobody and blocked everybody, which is why nobody noticed: JWT mode could not configure a spawner or install a plugin at all. `RequireAdmin`, `RequireAdminOrBypass`, `adminPrivilegeTTL` and the `isAdmin`/`aga` JWT claims are removed, and `/api/me` no longer reports a role. **State the consequence plainly: under JWT auth those five groups are now reachable by any authenticated user, where they previously returned 403 to everyone.** Bypass mode is unchanged. Spawner CRUD remains RCE-equivalent — it is now guarded by authentication and the loopback bind alone, which is what the deleted middleware actually provided in the only mode anyone runs. Setting a project's `setup_command` (also an arbitrary `sh -c`) is now permitted *only* in loopback single-user mode rather than to a role nobody held. A second, unrelated job rode on the same flag: `TaskRepo.ListForUser` and `TaskScheduleRepo.ListForUser` use it to decide whether a listing is scoped to one user, and `BypassPayload()` set it to true — so a naive deletion would have hidden every task not owned by the implicit local user. That parameter is renamed `unscoped` and now follows the deployment mode, leaving both modes' visibility exactly as it was. The `is_admin` column stays in the schema: dropping it forces SQLite's 12-step table rebuild, which has broken this project's migrations before, and an unused boolean is cheaper than a broken migration.
- The **Waterfall** view in the agent detail modal. It was broken and not worth repairing: it charted a session's tool calls on a d3 timeline that no longer matched the data it was given, and the transcript already answers the question it was meant to answer. The modal now shows the transcript directly instead of offering a two-way toggle with one working side. The d3 packages stay — the eval charts use them.
- **`task_permission.pre_approved` is gone, because nothing ever read it.** Four writers set the flag — `BulkGrantPermissions`, the MCP `grant_permission` and `resolve_permission_request` tools, and the repo default — and no query, guard or handler ever branched on it, so it recorded a distinction that changed nothing. The one place it looked like a reader was the task **Permissions** tab, which rendered a `pre-approved` / `runtime` badge from `p.preApproved`; ent serialises the column as `pre_approved`, so that property was never present on the wire and the badge said `runtime` for every row it ever displayed. The field is dropped from the ent schema, from `CreateTaskPermissionInput`, from both MCP call sites, and the dead badge and its TypeScript field are removed with it. **The SQLite column stays on existing databases**: ent's auto-migrate is non-destructive and the project deliberately does not enable `WithDropColumn`, because dropping a column forces SQLite's table rebuild, which has broken this project's migrations before. The column was generated as `NOT NULL DEFAULT (false)`, so inserts that no longer mention it still succeed — `TestOpen_LegacyPreApprovedColumnSurvives` opens a pre-drop database on disk and proves both the read and the write path.


### Fixed

- **Two spellings of one config directory produced two materialization targets.** A dotfiles layout that links `~/.claude` at `~/.claude-personal` has `AllClaudeConfigDirs` return both, and they are the same directory — found by the first dry run against a real machine, which listed three roots where two were one. The consequence is not a duplicate write: the first target writes the file and records it under its own `target_key`, the second finds content it has no record of, because the record belongs to the other key, and `Classify` correctly calls that **foreign** — so the materializer reports a file it wrote itself as somebody else's, on every run from then on. Targets are now deduplicated by resolved path. The resolved path is a comparison key only and never becomes `Target.Root`: writing it there would rewrite `target_key` for every directory reached through a link — on macOS even `/var/…` resolves to `/private/var/…` — orphaning every materialization row already recorded and turning the whole existing state foreign at once.


- **The channel bridge authenticated with a global environment variable that installs need not set, so every callback answered 401.** `set_stage_output`, `request_permission` and `dashboard_reply` all post back to the dashboard using `DASHBOARD_MCP_TOKEN`, taken straight from config with no fallback. An install that never set it had the bridge posting an empty token, and the refusal was visible only inside the agent's own transcript — the dashboard saw nothing at all. The spawn's MCP config now hands the bridge the stage run's own short-lived key, the one issued per run and revoked with it. No new exposure: the same token was already in that file, in the task API server's `Authorization` header.
- **The `stage_output_submitted` audit event was never written**, because the handler was constructed with `auditEventRepo` one line before that variable was assigned. It received a nil interface, and its own nil-guard treated that as "auditing disabled" — silently and by design, so nothing logged and the counter stayed at zero through four measurement runs. Introduced with the event itself.


- **Per-task permissions never reached the spawned agent.** The pipeline rendered its allow- and deny-list into `.claude/settings.json` in the agent's working directory. Claude Code applies `permissions.allow` from a project settings file only in a workspace that has been *trusted*, and a stage worktree is created fresh, so it never has been — the CLI discards every entry and says so: `Ignoring N permissions.allow entries from .claude/settings.json: this workspace has not been trusted`. The dashboard recorded grants, the agent received none, and the failure surfaced much later as a denied tool call. The deny list went the same way, which made the `Bash(git push:*)` block decorative in exactly the allow-all configuration that needs it. Both now travel as `--allowedTools` and `--disallowedTools` on the spawn command line, which carries no trust condition. Measured on an isolated instance before and after: three tasks that previously looped through implementation retries now run end to end — **15 stage runs, zero failures**, against a 49 % implementation success rate in the recorded history.
- **Stage agents were told to use the fallback output channel, so they always did.** The system prompt every spawn carries said *"when you produce structured output, wrap it in a fenced json block"* and never mentioned `set_stage_output`; the instruction to call the tool lived only in the stage prompt, which the resume path replaces wholesale — so from the second attempt onward an agent was told only about the fence. Measured on an isolated instance after the tool was permitted and discoverable: **zero of twelve completed runs called it**, and the transcript shows it surfacing from tool search but never once as a tool call. The system prompt now names the tool first and states the fence as the fallback it always was, and the resume instruction repeats how to submit. This was the third of several defects behind `agent did not produce a stage output block`, the pipeline's most common failure; the earlier two were the missing allow-list entry and the blind retry that never said what was wrong. It was not the last — see the permissions entry above.
- Corrected comments in `spawner.go` that claimed the channel allow-list reaches the agent as `--allowedTools`. It does not: `writeSettingsFile` renders it into `.claude/settings.json` under `permissions.allow`, and the spawn command line carries neither flag. Verified by reading the command line of a running stage agent.


- **The `set_stage_output` MCP tool was never callable by a spawned stage agent.** A spawn runs with `--allowedTools`, and the channel allow-list named only `dashboard_reply` and `request_permission` — the bridge registered `set_stage_output` as well, but nothing permitted it. Every stage prompt says *"call `set_stage_output` … if it is unavailable, instead emit the same object as a json-fenced block"*, so from the agent's side the tool genuinely was unavailable and the fallback fired on **every single run**, by construction. Measured on an isolated instance: 10 agent-driven stage runs completed, **zero** went through the tool. That is what made a fenced JSON block the only path a stage result could take, and a stage whose final message did not carry a well-formed fence fail outright — the single most common pipeline failure. The allow-list is now derived from the bridge's tool list instead of being written out a second time, and a test reads `channel/bridge.go` directly and fails if a registered tool is missing from it.


- **A stage agent that produced text but no output envelope now gets told what was missing.** Such a run was classed `Infra: true`, the same bucket as "no session JSONL found" and "session read error", which routes to a blind requeue: the stage is retried with backoff, up to the whole auto-retry budget, and the agent is never told what went wrong — so it repeats the miss until the budget runs out and the run hard-fails. The sibling case, a schema mismatch, was already `Retryable` and gets an `IterateTransition` whose feedback quotes the error and the previous output back to the agent, escalating to the user on the second miss. The case with the *most* diagnostic information had the *least* informative recovery. Envelope misses now take the feedback path, and the message names the primary channel the agent skipped (`set_stage_output`) rather than only the fallback fence. A run that produced no text at all stays on the infra path — there is nothing to feed back. **Note for anyone comparing pipeline metrics across this change:** an envelope miss now resolves through `IterateTransition`, which marks the superseded run `done` and opens a fresh one, so these no longer accumulate as `failed` rows.
- **The new-entry form stops inviting a slug it cannot accept, and shows its error where the eye already is.** The space-slug field was free text although the valid spaces are listed two rows above it, and `createEntry` deliberately never creates a space on the fly — so a typo came back as a 403 or 404 rather than being impossible to type. The input is now backed by a `<datalist>` of the loaded spaces: suggestions when they are known, free text still accepted, because `memory.read` and `memory.write` are separate grants and a scope whose spaces table is denied or empty must still be writable. The create error also rendered *below* the submit button and outside the form, which at the modal's height put it off-screen — a denied write looked like the button had done nothing at all. It now renders above the submit control, inside the form.
- **The last two endpoints that answered with a raw ent entity now answer a DTO.** `GET /api/grants`, `POST /api/grants` and `GET /api/capabilities` encoded `*ent.Grant` and `*ent.Capability` straight to the wire, so they were the one pair of routes #412 and #415 left behind — and the same file's `POST` body already took camelCase, meaning one route spoke two conventions in two directions. Casing was the smaller half: ent's tags all carry `omitempty`, so `limitCount: 0` — which *means* unlimited — was never sent, and neither was `limitWindowSeconds: 0`, an empty `contextRef` (which is what a global-scoped grant has), an empty `pattern`, `revokedBy` or `reason`. A client could not tell "unlimited" from "absent". Both responses are now hand-written camelCase DTOs with no `omitempty`, `enforceableBy` encodes as `[]` rather than `null`, and `nodeId` — which the client never declared because the entity's `omitempty` hid it on every single-node install — is part of the declared shape. A parity test reads the Go struct tags and asserts the TypeScript interfaces declare exactly those keys: renaming fixtures alongside a component keeps a component suite green whatever the server actually sends, which is how this drift survived a whole wire-format sweep. It caught the missing `nodeId` on its first run.
- **The grants table has one column fewer.** `GRANTED BY` and `STATUS` answered one question between them — who put this grant here, and is it still standing — so they are now a single `Provenance` column with the actor on one line and the revocation detail beneath it. The `Active` badge is dropped rather than moved: since a revoked grant now carries a `Revoked` marker beside its mode, an active one is the row with neither, and two affirmative signals for the same fact is one more than the table can afford. A test asserts the header count, so the next column added has to be argued for rather than appended.
- **The grants table scrolls inside its own box.** Ten columns did not fit the settings dialog, and with no scroll container of its own the table pushed the whole dialog body sideways — so the columns past `ENFORCEMENT` were not merely cut off, they were reachable only by scrolling a surface nothing indicated was scrollable.
- **A revoked grant no longer reads as one that is still in force.** The Grants panel's `STATUS` column carries "Revoked <when> by <who>", but it is the last column in a table that scrolls horizontally inside the settings modal — at a 1400px viewport it is off-screen, so what a reader actually sees of a revoked row is `MODE: allow`. The only thing contradicting that was the row's `opacity-60`, which carries no meaning for assistive technology and vanishes as a signal when every visible row happens to be revoked, leaving nothing to compare against. A revoked grant now says so in text beside its own mode, where the misreading happens; the status column keeps the full detail and the row opacity stays as a secondary cue.
- **`agent-dashboard settings set` says when the value it just wrote will not reach a running server.** The settings CLI writes straight to SQLite on purpose — that is what makes `settings set auth.mode none` usable as the lockout escape hatch when the server is down or authentication is misconfigured — but `settings.Service` reads every row once at startup and nothing invalidates that snapshot from outside the process. For a key the registry marks `restart` that is exactly what the registry already promises; for one marked `live` the CLI reported success while the running server kept answering the old value, with no error anywhere. Grants make the wrong expectation likelier still, since they are read per request and a `grants add` or `grants revoke` does take effect immediately. `set` now prints a one-line warning to **stderr** (stdout stays clean for scripting) for `live` keys only, naming the mechanism rather than guessing whether a server is running.
- **One place decides how a timestamp reads.** Nine call sites rendered dates and five of them were copies rather than deliberate differences: `SessionList.vue` and `SessionDetailModal.vue` each carried a byte-identical relative-age ladder (its 1h/24h/168h thresholds could drift apart in one view without the other), and `SchedulesView.vue`, `ScheduleForm.vue` and `utils/taskFormat.ts` each re-implemented `new Date(iso).toLocaleString()` with different guards around it. The ladder is now `formatRelativeThenDate` and the three `toLocaleString` copies use the existing `formatDateTime`, so schedule and task timestamps read the same way as every other date in the app. Two of those copies wrapped the call in a `try`/`catch` that could never fire: neither the `Date` constructor nor `toLocaleString` throws on an unparseable string — they yield the literal `"Invalid Date"` — so a malformed timestamp printed `Invalid Date` in all five places despite the guard. `formatDateTime` now checks `Date.parse` explicitly and falls back to the raw string, which is what those `catch` blocks were written to do. The four genuinely different renderings are left alone: `ApiKeySettings.vue` (date only), `TaskCard.vue` (month and day, for card density) and `ConfigExplorer.vue` (which takes Unix seconds, not an ISO string) are not duplicates of anything.
- Every remaining endpoint that answered with a raw ent entity now answers a DTO in the shape the client declares. #412 fixed the task permission and audit routes; this is the rest of that class. `GET /api/tasks/{id}/stage-runs` sent `session_name`, `started_at` and `ended_at`, so the Stages tab showed a blank `—` for both ends of every run and the task modal's Active Runtime row disappeared entirely for every task that has ever run. `GET /api/tasks/{id}/dependencies` and `/dependents` needed more than a rename: `taskTitle`, `dependsOnTitle` and `dependsOnStage` are not columns on `task_dependency` at all, so the dependency list rendered nameless rows under an empty stage badge — they are now resolved from the referenced tasks in one extra query on the display route, deliberately not by eager-loading in the repo, which would have added joins to the orchestrator's dependency sweep for data it never reads. `GET/POST/PUT /api/settings/system-prompts` dropped `priority: 0` — the value the create form submits by default — because of the entity's `omitempty`, so the settings table showed a blank cell and the edit form re-seeded itself from `undefined`. The six `/api/memory/*` routes were converted too, ahead of the memory panel that will consume them: `GET /api/memory/entries` was answering PascalCase Go field names (`ID`, `SpaceID`, `Summary`), because the retriever's own result struct carries no json tags. The task-returning routes (`PATCH /api/tasks/{id}`, cancel, hold, `POST /api/plan/{taskId}/approve`, `POST /api/refine/{taskId}/confirm`) now share one exported `TaskResponse`, which `EnrichedTask` embeds rather than redeclaring — the enriched payload is unchanged byte-for-byte. The downloaded `tasks.json` export was the one artifact whose wrong shape outlived the session that produced it, and it now carries the same names as the API, nested stage runs included. `POST /api/tasks/{id}/advance` sat beside the now-fixed `/retry` and still assigned a raw `*ent.StageRun` into its `result` field, so a retry dispatched through `/advance` sent `created_at` and an empty `edges` object and dropped `task_id`, `stage`, `status`, `iteration`, `tokens_used` and `cost_cents` under `omitempty` — it now goes through the same DTO. Its `approve_all_pending` primary carried the same result struct MCP's `approve_all_pending` tool and the dedicated `POST /api/tasks/{id}/approve-all-pending` route already returned, but without json tags, so `/advance` answered `{"Approved":3,"Requeued":true}` while the other two answered `{"approved":3,"requeued":true}`; the struct now carries tags and all three transports agree. Six endpoints across four deliberate exceptions were left alone: `GET/POST /api/grants` and `GET /api/capabilities` are snake_case on purpose and `useGrants.ts` says so at its interface definitions; `GET /api/plan/{taskId}/status` ships `gate_state`/`approved_plan`, which `usePlanReview.ts` reads under exactly those names; and `GET /api/coord/{namespace}/scratchpads` / `/locks` answer `{"entries": [...ent.Scratchpad]}` / `{"locks": [...ent.CoordLock]}` of raw ent rows, because `useTaskCoordination.ts` declares `ScratchpadEntry` and `CoordLock` in snake_case (`updated_at`, `owner_task_id`, `acquired_at`, `expires_at`) to match — converting the pair without its client would break the coordination panel.
- The task permission and audit endpoints answer in the shape the client declares. `GET /api/tasks/{id}/permissions`, `POST /api/tasks/{id}/permissions`, `GET /api/tasks/{id}/audit` and `GET /api/audit` replied with the raw ent entity, whose JSON tags are the storage column names — so `taskId`, `requestedAt`, `decidedAt`, `decidedBy` and `expiresAt` arrived as `undefined` in the browser, and the Permissions tab's decided-by column rendered empty for every row it ever displayed. The `omitempty` on those tags made it more than a rename: a `granted: false` or an absent pattern was dropped from the payload entirely instead of being sent as `false` and `null`, and ent's empty `edges` object was sent instead. The audit rows were further off than casing — the entity stores `ts` and `metadata` where the client reads `timestamp` and `details`, and has no actor column at all, so every audit row printed an invalid date under a blank actor badge. Both now map to a response DTO with camelCase tags and no `omitempty`, the same shape `permissionRequestResponse` in the same package already uses; `actor` is derived from the metadata tag, else from the presence of a user id, else `system`. `manual_override` is deliberately absent from the wire: it is the permission gate's own state and no client reads it. `TaskPermission.decidedBy` in `src/types.ts` claimed `'user' | 'auto' | null`, which the server has never sent — it records the caller's identity, a JWT subject or an API key id — and is now `string | null`.
- A truncated permission pattern no longer reads as a complete one. The hook bridge caps agent-authored text at 400 runes before it reaches a human's **Allow** button and then discarded the count, so the card showed a cut pattern with nothing to mark it as cut — a trailing `…` could be the agent's own ellipsis or the cap's, and the reader had no way to tell. `sdk.PendingPermission` now carries the count through to the display as `patternElided` and `deniedByElided`. They are two fields rather than one shared counter because they describe different text: `displayPattern` is the exact string a human is about to approve, while `deniedBy` is the deny-rule line on cards that offer no **Allow** at all — nobody clicks it, but a silently shortened reason is a misleading reason. `sanitize.ForDisplayCapped` increments only on the cap branch, so stripping a bidi override without truncating still reports zero. Same convention as `sdk.RecentTool.Elided`.
- The route golden can now see the auth-only routes. `TestRouteGolden` built its router with authentication bypassed, which is exactly the configuration in which the three auth-only routes do not exist — the tripwire meant to catch a route losing its guard was structurally blind to every route it was supposed to watch. A second golden now pins the difference between the bypass-auth and auth-on routers, so a route that stops being auth-only fails the build.
- The grant backfill no longer writes grants nothing can match. It converted legacy `task_permissions` and `permission_presets` rows with two raw `INSERT INTO grants ... SELECT` statements, bypassing every check `repo.GrantRepo.Create` applies. Two legacy shapes therefore produced a grant that was written successfully and could never take effect: a preset with an empty `project_cwd` became a `project` grant with an empty `context_ref`, which no caller can ever build because `repo.Scope.Normalize()` collapses an empty ref to `global`; and a pattern `capability.ParsePattern` rejects became a grant `capability.Match` can never satisfy. Both sat in the table as permanent dead weight, indistinguishable in a listing from a working grant. Each candidate row now passes the same checks before insertion, and a row that fails is skipped and logged with its identity and the reason rather than written. Package `db` cannot call `GrantRepo.Create` — `db/repo` imports `db` back — so the rules come from `capability.ParsePattern`, `capability.IsValidMode` and `capability.IsValidContextKind`, the same functions `Create` calls and the same maps `Decide` ranks with; only the insert is local, the vocabulary is not duplicated. One unparseable legacy row must never make the server unbootable, so a rejected row is a warning and a `continue`, not a returned error.
- A rate limit now bounds use, not merely unattended use. When a grant's limit was exhausted, `ServerEnforcer` downgraded the allow to an ask and a human could approve it — but that approved use was never counted. `memory.Authorize` calls `RecordIfWithinLimit` once, before any ask, and its exhausted branch inserts nothing (`grant_usage_repo.go`), so no second call ever recorded what the human let through: a limit of 3 meant "three silently, then unlimited at one click each". A consciously approved use now records its own usage row through a new `RecordUsage`, which writes unconditionally and deliberately skips `RecordIfWithinLimit`'s atomic check-and-insert — the limit has already done its job of bounding unattended use, and there is no concurrent caller left to race, only the one person who just answered. The within-limit path is untouched, so nothing is double-counted, and a failed write is logged rather than returned: the permission had already been given, and refusing afterwards would report that nothing happened when something did. Rejected on the way: resetting the window on approval (it discards the fact that the limit was exceeded) and moving all accounting behind the human's answer (it re-opens the race the atomic insert was built to close).
- The command shown next to a needs-you card's **Allow** button is sanitized before it is rendered. The bidi-override and zero-width strip added for the recent-tool trail did not reach it: `PendingToolUse.Pattern` is the grant identity, matched against a stored permission preset by exact equality, so it is carried verbatim and cannot be normalised. It was also the string the card printed — meaning the protection sat on the passive trail in the modal and not on the text a human reads while deciding to write a standing rule. The payload now carries both: `pattern` verbatim for the API, `patternDisplay` sanitized for the eye.
- The needs-you band decides from a positive rule which agents may be granted a permission, instead of excluding one state by name. `Attention` gained a required `grantable` flag, so a kind added later has to declare its answer rather than inherit "grantable" by omission. The control was previously gated on nothing but the presence of an unresolved tool call, minus `AskUserQuestion` by name — that name check is kept as a second, narrower filter. So a leftover tool call on an agent that had simply finished its turn still offered an **Allow** button: nothing was waiting for that grant, and accepting it wrote a standing rule for a prompt nobody had raised. Only a detected **permission** prompt now counts as that evidence. A question does not: it is answered rather than granted, and the tool call a grant would be written for is a different object from the question on screen, so an open question next to an unrelated running command used to offer a rule for that command.
- `task dev:desktop` reaches its own Vite dev server. Vite's default host is `localhost`, which Node resolves to `::1` first on a dual-stack machine, so the dev server listened on `[::1]` only — while the shell polls and redirects the webview to `127.0.0.1:<port>`. Every run ended at "vite dev server not reachable". `server.host` is now pinned to `127.0.0.1`, the same address the proxy targets below it already spell out for the same reason. Browsers opening `http://localhost:5173` are unaffected; they fall back to IPv4.
- `task dev:desktop` fails loudly when its port is taken, and no longer adopts whatever answers on it. Vite had no `strictPort`, so it moved to 5174 while the shell's readiness poll accepted any answer on 5173 — a foreign loopback process could become the desktop window's origin, serving no CSP while the Vite proxy handed it the same-origin guard. `strictPort` alone does not close that: it makes *our* Vite exit on a conflict, which is exactly when someone else holds the port. The poll now asks for `/@vite/client`, which only a Vite dev server serves and serves as JavaScript, so a squatter is rejected instead of adopted. It then probes `/api/system/health` through Vite's proxy, because the proxy's target comes from `process.env.DASHBOARD_PORT` while the shell resolves its own port through config file, `.env` and environment — when the two diverge the proxy swallows the connection error and every call fails silently. CI also compiles and tests the desktop shell under both build tags: only the `dev` half ran before, so the shipping `frontendAddr` had an assertion nobody executed.
- The Vite dev port is written down once. `VITE_DEV_PORT` was a Task variable exported only on the wails command line, so `dev:frontend` never received it and Vite fell back to its own literal — changing the value made the shell poll a port nothing served, then advise running the task that was already running. It is now a Taskfile-level variable both children export (a parent task's `env:` does not reach its `deps`), overridable per run with `task dev:desktop VITE_DEV_PORT=5199`.
- `task dev:desktop` no longer builds an SPA it never serves. The one-shot `build:frontend` ran on every start, cost a full build, and deleted the tracked `server/frontend/dist/.gitkeep` that `//go:embed all:dist` needs to compile without a frontend build — a deletion that breaks CI if committed. Under the `dev` build tag the window points at Vite and the embed is never read; `build:desktop` and `desktop:dist` still depend on `build:frontend`, which is where it belongs.
- `task dev:desktop` now hot-reloads the frontend. The desktop window loaded the SPA from the Go embed, baked in by a one-shot build before wails started, so frontend edits never reached the running app and wails' own `Please specify -assetdir or -reloaddirs` warning pointed at asset directories it was never serving — the redirect page is the only thing it hands out. Under the `dev` build tag the window is now pointed at the Vite dev server instead, which already proxies `/api` and `/auth` to the backend with the Origin rewrite the same-origin guard needs. The task starts Vite itself and owns the backend on 13120, so it must not be run alongside `task dev`; a missing Vite server now fails with a message saying so instead of opening a blank window. Production builds are untouched — the redirect target is chosen by build tag.
- A stalled agent no longer offers to approve a tool nobody asked about. Classifying an unresolved tool call as stalled rather than as a permission prompt was only half the fix: the card's approve control was gated on the tool call being present, not on what the agent was actually waiting for, so a stalled card still rendered a green **Allow** button and printed the entire shell command underneath it. The control now appears only where a prompt genuinely is on screen, and a stalled card says which tool has gone quiet and for how long instead of reproducing its command.
- **Recent tools** in the agent detail view says what each call did. It listed bare tool names, so an agent that had run five shell commands showed five identical `Bash` rows and told you nothing. Each entry now carries the tool's own argument — the command for `Bash`, the file path for any tool that names one (`Read`, `Edit`, `Write`, …) — taken from the same input the pending-tool display already decodes. Long arguments are collapsed to one line and cut at 120 characters, and the size of the cut travels as its own field rather than as a suffix in the text: a marker written into the string is one the command itself could forge, so an agent-authored argument ending in `… (+400 chars)` would read as a truncated prefix of something longer. The row renders the kept text and a separate `+87 more`, and no longer clips it a second time in CSS — the server already capped it, so what is on screen is everything the browser was given. The remainder is not retrievable — the cut happens before the value reaches the browser. Characters that can make a command render as a different one, such as bidi overrides and zero-width spaces, are stripped at the same boundary. An entry without an argument still shows its name.
- The **Needs you** band no longer asks you to approve a tool that nobody is waiting on. An unresolved `tool_use` was treated as a permission prompt, but a tool that is simply still running leaves exactly the same trace in the session log — so a busy agent appeared in the needs-you band naming a tool while the very same agent was rendered as **Working** elsewhere. The `permissionsBypassed` guard did not help: it catches only the flags that skip prompting outright, while an allow-listed tool or a partial permission mode silences the prompt just as completely, and neither reaches the client. A card now requires positive evidence that a prompt is actually on screen; an unresolved tool call that has sat still past the same dwell a stalled session uses is reported as stalled, which is all that can honestly be claimed about it.
- A live agent whose badge reads **Working** is no longer filed under **Idle**. The band was built from the server's `status`, a pure time bucket over the last JSONL line, while the badge preferred the live turn state — so a long-running tool that emits nothing for five minutes produced an agent that was `idle` and `working` at once, and the two disagreed on screen. The derivation now lives in one place (`agentDisplayStatus`) and the band, the card, the roster row and the modal all read it; the roster row had in fact been showing the raw status all along, so it disagreed too. A working agent folds into **Active** rather than opening a fourth band.
- **Your turn** is now surfaced in the needs-you band instead of only as a word on the card. It ranks below a pending question and below a permission prompt, because those block a run mid-task while this one only means the agent finished its turn and is waiting — an agent that has both is still reported as the question. An agent whose process has actually exited is unchanged and stays out of the band.
- Subagents in the detail view are ordered active-first, then by most recent activity. They were listed in directory order, and the filenames are content hashes, so the ordering was effectively random with respect to activity: on a real session the most recently active subagent sat ninth of fourteen, which read as "only the completed ones are shown". Completed subagents now collapse behind a disclosure once there are more than a handful, the heading states how many of the total are active, and the list has its own scroll bound instead of competing with three sibling lists for one 170px region.
- Clicking an agent card while it had active subagents, or while hovering it long enough for the inline prompt box to expand, often did nothing. The card had two competing click owners — a full-bleed overlay button with no explicit `z-index`, and the card's own root click handler — so every panel with its own controls (the active-subagents list, the last-btw line, the prompt input) defended itself with `@click.stop`, and each became a dead zone where neither handler fired. It was worst on hover, when the expanding prompt box covered most of the card's height. The overlay is gone; a single visible header-row button now owns opening the card (keyboard-reachable, same aria-label as before), purely presentational panels no longer stop the click from reaching it, and the subagent list is capped to its own scrollable height so it can no longer squeeze the other panels off the card.
- The dashboard listed Claude Code's own internal daemon/spare-pool processes as if they were user-addressable sessions. Claude Code 2.1.234 spawns `daemon run`, `bg-pty-host`, and `bg-spare` grandchildren inside a session's own working directory as part of its daemon/spare-pool architecture, and because they inherit the parent's MCP config the scanner picked them up as ordinary `claude` processes and even reported a channel for them — one showed up as **Not live-injectable** while the real session it was masking was fully injectable the whole time. Detection now matches these three subcommands by argv position right after the `claude` binary, not a substring anywhere on the command line, so a resume id or a prompt that merely mentions "daemon run" is not caught. A new `internalProcess` flag marks the card and roster row instead of hiding the process — a hung spare process stays discoverable. `PromptInput`'s send hint used the same "Not live-injectable" wording for three different causes; it now distinguishes a disconnected session, a connected session with no live input path, and an internal process that cannot be messaged at all.
- The **Eval / Drift Detection** view no longer looks broken when it is only empty. Its nine metric-trend cards rendered a bare "No data" whenever the fixed 168-hour window held no stage runs — indistinguishable from a scan that never ran, or one that silently failed — and a fetch failure surfaced only as a toast that faded, after which the empty `snapshots` state looked pixel-identical to a legitimately empty one. Each card now names the reason and the actual window ("No stage runs in the last 7 days"), plus the date of the last recorded snapshot when one exists outside that window; a fetch failure instead renders a persistent banner in the view itself, which the empty-state text is suppressed under so the two can never be mistaken for each other. A new range selector (7/30/90/365 days) makes older snapshot history reachable without widening the hard-coded default — chosen deliberately over an automatic fallback to the newest N snapshots, since that would silently change what the chart's window means. The copy is worded so "Run scan now" is never read as able to back-fill a period nothing ran in — it can only record what happens after it runs.
- An ACP stage run no longer refuses every tool call that needs approval. The adapter was built without a permission gate, and a client with no gate denied substantively — the reason class that is allowed to answer `reject_always`, so the agent remembered the refusal and stopped asking for the rest of the session. Nothing was logged, so it looked like an agent that simply would not work. Permission requests now route into the same approval flow the native Claude path uses: the stage context gained a reader for a filed request (alongside the writer it already had), and the pipeline builds a polling gate from the two. A gate that is still missing now denies transiently and says so in the log, so a wiring fault stays recoverable instead of disabling approvals for the session.
- `task lint` and CI now run the linters this project configured. golangci-lint v2 resolves exactly one config file and does not merge them, so the per-module `.golangci.yml` in `server/`, `sdk/` and each of the six plugins shadowed the root config and silently reduced every module to golangci's five default linters — `depguard`, `gosec`, `misspell`, `revive` and the `gofmt` formatter had never run on any Go code here, including both `depguard` rules that guard the layering ADRs. Every module config carried a `revive` exclusion for a linter that was not enabled there, which is the fingerprint of the assumption that these files extended the root rather than replacing it. The module configs are gone, and the root config, which now carries their rules, resolves from any directory in the repository. `golangci-lint run` reports only some formatting violations, so `task lint` and both CI lint jobs gained an explicit `golangci-lint fmt --diff` step; `task lint` and `task fmt` also cover `plugins/` now, the way CI already did.
- An ACP spawner's `adapter_config.command` did not go through the same trust check the `command` column gets on every other spawner: the column is validated on create and update against the trusted-command allow-list (bare names like `claude`/`npx`, or absolute paths under a trusted bin directory), while `adapter_config.command` — the value an ACP spawner actually execs — only checked the key name and a 4096-character length cap. `ValidateAdapterConfig` now runs any adapter_config key that holds a command value (currently only `acp`'s `command`) through the same `services.ValidateSpawnerCommand` check, keyed off a small per-adapter-type table so a future adapter gains the same coverage automatically. This is defense in depth, not a new privilege boundary: `/api/spawners` is loopback-only, and in the default configuration (`auth.mode: none`) `RequireAdminOrBypass` passes every request through as already-implicit-admin — "admin-gated" does not hold unqualified. `adapter_config.args` is checked too: it is split the way the spawner splits it, and every token shaped like a filesystem path (`/…`, `./…`, `../…`) must clear the same trust check, so `{"command":"/bin/sh","args":"/tmp/payload.sh"}` is now rejected while the documented ACP default `-y @agentclientprotocol/claude-agent-acp@0.68.0` still passes. Two shapes remain uncovered by design: a bare filename, which resolves against a working directory the operator already controls, and a remote package spec such as `npx <tarball>`, which is what the documented default itself does. Separately, a directory that is writable by *others* is no longer treated as a trusted bin directory — on macOS `/usr/local/bin` is commonly `drwxrwxrwx`, so any local user could plant a binary there and have it allow-listed. Group-writable directories such as Homebrew's are still trusted, since they are writable by an owning group rather than by everyone, and a rejection now names the excluded directory instead of claiming the path is simply not listed. An empty `command` is still accepted — for ACP it means "use the bundled default agent", which the factory fills in as `npx -y @agentclientprotocol/claude-agent-acp@0.68.0` and which is itself allow-listed, so rejecting empty would make every unconfigured ACP spawner unsavable. The trust check itself was also tightened to accept a path whose *named* location, not only its resolved target, sits under a trusted bin directory — Homebrew's `npx` resolves through `../lib/node_modules/npm/bin/npx-cli.js`, whose parent is not itself trusted, and without this an existing ACP spawner pointed at a Homebrew-installed `npx` or `node` could no longer be saved at all.
- A task advanced from the dashboard or over MCP no longer kills the agent it just started. The advance handed the HTTP request's context all the way down to the process launch, and that context ends when the response is written — so the adapter process was killed within milliseconds of being spawned and the stage failed with an opaque adapter error, while the same task advanced by the orchestrator's own tick loop ran fine. Every non-Claude adapter was affected — ACP, custom, Anthropic, OpenAI and Ollama — the native Claude spawner, which launches without a context, was not. A dispatched spawn now runs on a context that outlives the request and is cancelled only when the orchestrator shuts down.
- Raising **Max parallel orchestrators** now takes effect for adapter dispatches instead of only for the scheduler. The pool that bounds HTTP and ACP dispatches was sized once from the built-in default, while the scheduler read the setting live — so a raised limit left a dispatch waiting behind a pool that never grew, for as long as the turn it was queued behind (up to the ACP gate's 30-minute timeout), and a lowered limit was exceeded rather than enforced. The pool now reads the same live value the scheduler does, including from a dispatch that is already waiting, so a change made in settings reaches a parked waiter instead of only the next one to arrive.
- A spawner adapter's description is no longer truncated after two lines in **Settings → Spawners**. The ACP entry uses that text to disclose that it denies permission requests, and the clamp cut the disclosure off — so an adapter could be enabled without its security-relevant behaviour ever being visible. The full text is now rendered, and reachable by keyboard and screen reader rather than on hover.
- A permission gate that fails transiently no longer disables the gate for the rest of an ACP session. A denied request was answered with `reject_always` whenever the agent offered it, which is right for a considered refusal but wrong for a timeout or an unreachable gate: the agent remembers the refusal and stops asking, so every later call of that kind is rejected without anyone being asked. Denials now carry their reason — a substantive deny may still fall back to `reject_always`, while a gate error or a panicking gate prefers `reject_once` and cancels rather than remembering. An allow still never falls back to `allow_always`. A gate that is not configured at all counts as substantive: it refuses every call for the session's lifetime, so remembering that is accurate.
- A panic inside a permission or event callback no longer takes the server down with it. Both callbacks are supplied by the caller and were invoked unguarded, so one bad consumer ended the process. They are now contained, and a panicking permission callback resolves to a denial rather than to an approval — the failure is closed, not open.
- Sessions that never ask for tool permission no longer report **Needs permission** while a tool runs. An unresolved `tool_use` in the transcript is the only signal a JSONL gives for "blocked on a prompt", and a long `Bash` looks exactly like a waiting one — so an agent started with `--dangerously-skip-permissions` or `--permission-mode bypassPermissions` raised the triage band for every tool it ran, burying the prompts that were real. Agents now carry `permissionsBypassed`, read from the process command line, and the inference is skipped for them. Task-driven permission requests are unaffected: those are real rows, not an inference. `acceptEdits` deliberately still counts as prompting — it only auto-accepts edits.
- Spawning an agent into a project that already has one running now opens the **new** session instead of the running agent's. A dashboard-spawned agent is given its session id up front (`--session-id`), so the scanner binds it from the first tick. Previously the id only became known once claude had written `sessions/{pid}.json`, and until then the resolver fell back to "newest session file in this project with recent activity" — which is exactly the running agent's session. Resumed sessions and custom spawner adapters are untouched.
- A project's **Name** and **Description** are now length-checked on update, not only on creation, and the forms stop the input before it becomes a request. `PATCH /api/projects/{id}` accepted any length, so the 200-character name limit and the 10,000-character description limit that both create paths (HTTP and the `create_project` MCP tool) enforce could be walked past by renaming the project a second later; a `PATCH` could also blank a name that creation requires. Both fields in **Settings → Projects** and in the compact **Create new project** panel now carry the matching `maxlength`, so the UI can no longer send a value it knows the server will reject with a 400 it does not render.
- Choosing **No grouping** while a spawner filter is active now sticks. Filtering to one spawner takes **Spawner** out of the grouping options, and the control fell back to showing "No grouping" while still storing "Spawner" — so picking the value already displayed changed nothing, and clearing the filter brought spawner grouping back against the explicit choice. The hidden choice is parked instead: the stored grouping is always one the control can show, an explicit pick replaces what was parked, and clearing the filter restores a grouping only if it was never overridden.
- Clearing a filter chip or **Clear all** no longer drops keyboard focus onto the page body. Focus moves to the chip that took the cleared one's place, and to the search field when the last chip goes — the one control that is always present.
- Sending a message while offline no longer leaves the composer stuck on a host that registers no service worker. The queue-and-retry path awaited `navigator.serviceWorker.ready` to register a Background Sync, and that promise only ever settles once a worker controls the page — in the desktop shell, which deliberately registers none, it never does. The message was already queued, but the send never completed: the button stayed disabled and no "Offline — message queued" status appeared. The wait is now capped, after which the send finishes without a Background Sync registration and the existing SSE-reconnect flush delivers the queued message.
- A new project's **Slug** is now derived from its **Name** as you type, instead of being an empty field you had to fill in yourself. Typing a name like `DIW-ReviewApps` and leaving the slug alone previously meant hand-writing a slug that satisfied `^[a-z0-9][a-z0-9-]{0,63}$`, and any capital letter or stray character came back from the server as a raw-regex validation error after submitting. The slug keeps tracking the name until you type your own, after which it is left alone; clearing the slug field hands it back to the name. Derived slugs are capped at the 64 characters the pattern allows, so a long name cannot generate a slug the server rejects. An existing project's slug is never rewritten when its name is edited — it is a lookup key other records already point at, so renaming it stays an explicit action. Every slug field now says so in helper text below it, and in every case spells out the accepted format. In **Settings → Projects** it distinguishes the two modes the same form renders in: for a new project, that the slug is filled in from the name and that typing into it takes it over; for an existing one, that it is a key other records point at. The compact **Create new project** panel on the task form and the task form's own **Slug** (which follows the **Title**) each say it for the one mode they have. So a name that produces no slug at all — an entirely non-Latin one, for example — no longer ends in an unexplained "Name and slug are required", nor in the server's raw pattern. The quick-create panel also stops inventing a slug at submit time: it used to fall back to one derived from the name, which sent an empty slug for a name that derives nothing and silently replaced a slug you were part-way through retyping. What it sends is now always what is on screen, and its **Slug** is marked required like **Name** and **Path**.
- `create_task` (MCP) now rejects a non-string `spawnerId` instead of ignoring it. A JSON number or boolean read as an absent value, so the task was created with the project's default spawner and the call still reported success — the requested spawner was silently dropped. The argument is now read through the same strict extractor as `projectId`/`projectSlug` and a wrong type fails the call with `spawnerId must be a string`.
- `/fork` now appears in the slash-command menu, and `/pr-comments` and `/vim` no longer do. Claude Code's built-in commands cannot be discovered — the CLI exposes no machine-readable listing — so the dashboard curates them per version, and that list had been pinned to 2.1.161 while the CLI moved to 2.1.224. It was wrong in both directions: `/fork` had been added and was missing, while `/pr-comments` and `/vim` had been removed and were still offered. The list is re-curated against 2.1.224, and the menu now surfaces the version-drift warning it already computed — previously `builtinsMayBeStale` reached the Config explorer but was discarded on the way to the command menu, so a command missing because of drift was indistinguishable from one that does not exist. The warning appears on any `/` query, including the one that matters most — a query that matches nothing at all — and is marked up as a status message (`role="status"`) so screen readers announce it instead of dropping it as a non-option child of the suggestion listbox.
- The slash-command menu now shows each command's **argument template** next to its name — `/branch-review` displays `[base-branch] [--apply-fixes]`, `/review-and-fix` its full flag list. The templates were always there in the command files' `argument-hint:` frontmatter, but the dashboard's frontmatter parser only ever read `description:`, so the field was dropped before it reached the API and the menu had nothing to render. Options were therefore only discoverable by reading the description prose, which for skills is the entire trigger paragraph. The parser now also reads `argument-hint:` (in either key order, and correctly unquoting YAML's doubled-apostrophe and backslash escapes, which real hints use), `SlashCommand`/`CommandDetail`/`SkillEntry` carry it, `GET /api/slash-commands` returns it as `argumentHint`, and the menu renders it. `GET /api/config/commands` and `GET /api/config/skills` emit the same field for API consumers; the Config explorer itself does not render it. Skills are included: each is typeable as `/<name>`, so the hint survives the skill → command conversion. Built-ins show no template — they have no file on disk to read one from. Because a command or skill file may come from an installed plugin, the hint is sanitised where that trust boundary is crossed — in the parser, so every API consumer inherits it: values that are not valid UTF-8 are dropped, C0/C1 control characters and Unicode bidi overrides are removed, and the hint is capped at 120 characters. The menu clips it again at 60 characters and carries the full value in the row's tooltip, so a long hint cannot stretch the suggestion row.
- The macOS desktop app no longer needs a **Reload** click after every rebuild. It registers no service worker: the shell serves this SPA from its own in-process server, so a precache buys no offline capability there — it only pinned the previous build's assets until the update prompt was accepted. The shell's bootstrap page marks its redirect with `?shell=desktop`, and the SPA registers a worker only when that marker is absent, so a browser visiting the same origin still registers one and keeps the PWA behaviour (offline precache and the "A new version is available" prompt). A worker installed by an earlier build is unregistered and its workbox caches deleted on launch, otherwise it would keep serving that build forever. Registrations and caches are origin-scoped, not per-host, so a browser PWA on the same origin loses its precache whenever the shell starts and re-registers on its next visit.
- Launching the macOS desktop app while another instance is already running now fails fast instead of opening a window onto the *other* instance's server. The shell waited for `/api/system/health` to answer 200, which a running instance does immediately — long before the new process's own server reaches its listener and reports `address already in use` — so the second window rendered the first instance's embedded SPA. A freshly built app therefore showed the previous build's frontend, which looked like a stale build rather than a port conflict. The shell now binds the dashboard address before any other startup work and keeps that listener, handing it to the server to serve on: the claim and the bind are the same act, so two launches started together cannot both get past it. The bound address is also the single source for the health poll and the webview's target URL, so a non-default `DASHBOARD_PORT` no longer sends the window to a port nothing listens on. A refused start now also says so in a dialog: an app double-clicked in Finder has no terminal attached, so the message on stderr reached nobody and the launch looked like the app doing nothing at all. The same dialog covers the two other startup failures — the server never becoming ready, and the window itself failing to open.
- Picking a nav item now collapses the floating sidebar instead of leaving it expanded across the view it just opened, where its 220px swallowed clicks on anything underneath. Hover expansion re-arms once the pointer leaves the nav, and keyboard focus expansion is tracked separately so it is never suppressed.
- The collapsed sidebar is a clean icon rail again. It no longer scrolls sideways: every collapsed nav item rendered an absolutely positioned label box beside its icon, and because that box sits inside the nav's scroll container — where a non-`visible` overflow on one axis forces `auto` on the other — the 43px-wide rail carried 132px of scrollable content. Expanding the rail no longer pushes the page either: while unpinned, the nav now floats above the content instead of widening the layout (pinning still reflows, which is the point of pinning), and keyboard focus expands it just as hovering does. The group captions that separate Monitor/Build/Insights are hidden in the rail, so collapsed groups are separated by a short rule instead of running together as one undifferentiated column of icons.
- `scripts/gen-licenses.sh` can no longer silently drop an entire Go module from `THIRD_PARTY_LICENSES.md`. On PR #329, Dependabot bumped a module without running `go mod tidy`; `go-licenses` died outright on it, and the script's `|| true` tolerance — there to swallow benign classification failures, not build failures — swallowed that too, so the Go dependency count silently fell from 72 to 63 while the script still exited 0. The script now (1) runs `go mod tidy -diff` for every scanned module before collecting anything and fails immediately, naming the module directory and the exact `go mod tidy` command to fix it, and (2) fails per-module if a module whose `go.mod` declares external requires ends up contributing zero rows, naming that module directory too. `MIN_GO_DEP_ROWS` remains as a total-collapse floor; these two guards close the gap where a single module can vanish without tripping it.
- The orphan-run sweep now also reaps a `running` stage_run left behind by a crashed or killed orchestrator process — previously only non-terminal runs on a parked task, `on_hold` runs with a dead PID, and stuck `pending` runs were reaped, so a server killed mid-dispatch (before a PID was ever persisted) left a `running` row that nothing would ever pick back up. The new check only fires when the run started *before* this orchestrator process did, so a legitimately long-running async HTTP/LLM-adapter stage (which spawns no local process at all) is never mistaken for an orphan. What happens to such a run is decided by the same recovery rule the server applies at startup: a live PID is left alone, a run with a recorded session is re-queued and respawned with `--resume`, and only a run with nothing to resume from is failed.
- Token totals and costs no longer leak between unrelated sessions on Linux. The parser's incremental token-scan cache was keyed solely by inode number, and Linux filesystems (ext4, overlayfs) recycle a freed inode number almost immediately — so a newly created session file that inherited a deleted session's inode resumed that session's cached byte offset and running total, reporting a lifetime token count and cost that belonged to a session that no longer exists. macOS/APFS effectively never recycles inode numbers, which is why this only ever showed up on Linux. The cache is now keyed by path and each entry is pinned to the inode it was seeded from, so a recycled inode at a different path can never be resumed while same-path log rotation is still detected.
- Spawning an agent from inside the macOS desktop app no longer opens a second desktop window instead of starting the agent. The spawner re-executes the running binary as `<self> pty-host -- claude …` and points the MCP config at `<self> channel`; inside the desktop app `os.Executable()` is the wails binary, which parsed no arguments at all and went straight to opening a window, leaving the spawn request hanging on a PID line that never arrived. Both subcommand names now live in one place and are dispatched by a shared, cobra-free entry point that the CLI and the desktop shell both call before anything else starts.
- The macOS desktop app no longer hangs when you quit it. `http.Server.Shutdown` waits for active connections but never cancels their request contexts, and every SSE handler only observes `r.Context().Done()` — so with the dashboard open its permanent streams kept the drain running into the full `shutdown.timeoutSeconds` (10 s) before it gave up with `context deadline exceeded`, and the desktop shell blocks on that drain while closing. Request contexts now derive from a context that outlives the start of shutdown by a short grace window (`RequestGraceDefault`, 2 s) before being cancelled, so an ordinary in-flight request (a DB write, a git commit, a worktree operation) still gets to finish instead of being torn out mid-mutation, while the permanent streams still release `Shutdown` well inside `shutdown.timeoutSeconds` once that grace window elapses (measured: 10.0 s down to ~0.27 s). The shell's drain wait is additionally bounded, and set above the server's own `shutdown.timeoutSeconds`, so a future handler that ignores its context cannot make the app unquittable.
- Agent cards keep their prompt input out of the way until you need it: it is revealed on hover or keyboard focus instead of occupying a row on every card. The slash-command menu opens downward in that compact layout, since the reveal wrapper clips anything above the input.
- The `waiting` agent status now reads **Quiet** everywhere (card, roster row, detail modal) instead of "Waiting", which collided with the needs-you band's "waiting for you". It means 30 s–5 min without new activity — the process is alive and nothing is blocked on you; the card's badge tooltip spells that out.
- Status and resource badges carry explanatory tooltips, the card output preview fades out instead of cutting mid-line, and the CPU/MEM/DISK readouts turn amber/red on the same thresholds as the usage bar.

- An AskUserQuestion answer picked in the needs-you band no longer gets cleared while you are still choosing. Two independent causes: the option rows of every question card shared one hard-coded radio `name`, and a radio `name` is a document-wide group — so with two cards on screen (two agents with a question, or a band card plus the terminal overlay) the browser silently unchecked the first card's radio, and Vue never repaired it because its bound `:checked` value had not changed. Each card now gets its own group name. Second cause: the question card reset its local state whenever the `pendingQuestion` prop object changed identity — and every SSE frame (every ~3 s, driven by volatile fields such as uptime) delivers a freshly deserialized object, so selections and typed custom answers were wiped mid-answer unless you clicked fast. The card now keys its reset off the screen's *content* signature, so only a genuinely different question clears what you entered.
- The AskUserQuestion **review/submit screen** ("Ready to submit your answers?" → Submit answers / Cancel) is now detected and answerable, in the needs-you band and the Terminal overlay. It carries none of the meta-rows the question detector requires, so a multi-question flow went invisible to the dashboard exactly at the final keypress and could not be completed from the UI at all. Detected server-side alongside the modal via the pty broker's new `GET /screen` endpoint (the older `GET /question` is still honoured as a fallback for broker processes started before the upgrade) or a single `tmux capture-pane` snapshot, and delivered over the existing `POST /api/agents/{pid}/answer-question`. Agents carry it on a new `pendingConfirm` field.
- The needs-you band no longer offers **Allow AskUserQuestion**. An open question is an unresolved `tool_use`, so it surfaced through the same pending-tool path as a real permission request — but it waits for an *answer*, not a grant, and the button would have written a standing allow-rule for a tool nobody has to approve. The underlying signal is kept (it is the only thing a session whose screen cannot be probed can show) and now reads "Waiting for your answer — open the terminal to reply".
- Task export (CSV/JSON) in the pipeline board now downloads via a hidden `<a download>` link instead of `window.open`, which silently opened no window (and thus downloaded nothing) in the desktop shell's WKWebView. The worktree panel's copy-path button and the API-key/onboarding copy-to-clipboard buttons now route through the shared `useClipboard({ legacy: true })` fallback instead of a bare `navigator.clipboard.writeText`, so they degrade to `execCommand('copy')` instead of failing silently under WKWebView's stricter clipboard permissions.
- `formatCost`/`formatTokens` no longer throw on `undefined`/`null`/`NaN` input — a partial `Agent` (missing cost or token fields) silently aborted the agent detail modal's rendering; both now degrade to the existing "no data" (`—`) display.
- The AskUserQuestion terminal overlay again appears on current Claude Code releases: detection matched the meta-row copy exactly, but v2.1.205 renders `Type something.` with a trailing period, which silently disabled the overlay. Detection now normalizes the meta-row copy (trailing punctuation, prefix) so cosmetic wording tweaks no longer break it.
- The AskUserQuestion terminal overlay now traps keyboard focus: Tab and Shift+Tab cycle within the modal instead of leaking back to the inert terminal underneath, matching the ARIA modal-dialog pattern.
- Non-Claude provider agents (Codex, Gemini, Junie, pi) no longer always show as `idle`: the provider parser now reads each session's activity timestamp, so their status reflects real activity.
- Plugin UI slots now correctly load manifests and modules from the SP2 proxy path
  (`/api/plugins/{id}/proxy/ui-manifest.json`, `/api/plugins/{id}/proxy/{module}`).
  The loader was still pointing at the pre-SP2 settings path after the route migration,
  causing all `ui_extension` addons to silently fail to load.
- Auto-approved permission requests now store the canonical `granted` outcome instead of `approved`, which broke the client's outcome-based rendering for auto-approved requests.
- A task run that hit its iteration limit no longer writes `done` and then immediately overwrites it with `failed` — the final stage run now reflects the actual outcome on the first write.
- The refinement chat now surfaces file-read errors instead of hanging indefinitely when a referenced file cannot be read.
- Removed dead computed properties and an unused watcher left over from earlier refactors in `App.vue` and `ProjectSettings`.
- The GitHub OAuth plugin now returns a clear "authentication denied" response when a user declines consent, instead of a misleading "missing code" error.
- The status line no longer mislabels cumulative session cost as an hourly rate (shows `$x.xx` instead of `$x.xx/h`).
- The Anthropic spawner plugin now fails a stream on accumulation errors instead of continuing with incomplete state that could defeat its refusal / max-token detection.
- History import now shares one implementation across the API-key settings and cost-analytics views, so the API-key view gains the "already running" (409) and malformed-frame handling it previously lacked.
- The parser session-cache TTL now tracks a configured non-default SSE interval, so idle-agent caching is not silently defeated when the scan loop is slowed down.
- `Registry.Shutdown()` now actually waits for plugin processes to exit before returning, and SIGKILLs any straggler that ignores SIGTERM instead of leaving the escalation in a goroutine that died with the process. Previously the wait-then-escalate step ran detached, so `Shutdown` returned immediately — the server process could exit right after, and a plugin that ignored SIGTERM was never killed. All plugins are signalled and waited on against one shared 5s deadline (not 5s per plugin), so stopping several unresponsive plugins costs one timeout, not a multiple of it.

### Accessibility

- The xterm terminal view now enables screen-reader mode.
- The Task modal's close button now has an accessible label.
- Toast auto-dismiss now pauses while a toast has keyboard focus.

### Docs

- `CONTRIBUTING.md` told contributors to re-trigger the onboarding flow with `agent-dashboard settings set onboarding.completed false` and left out the restart. That key applies live only to the process that writes it, so against a running dev server the command reported success and the flow stayed hidden. The instruction now says why the restart is needed.
- `PRIVACY.md` now discloses the issue-tracker import feature (GitHub / Jira) as an opt-in outbound data transfer.
- Fixed a duplicate `ADR-0006` filename collision in `docs/architecture/adr/` — the eval-drift-detection ADR is renumbered to `ADR-0008`.

### Added

- **A stage result submitted through `set_stage_output` now records an audit event.** A stage's structured result reaches the orchestrator on one of two channels: the `set_stage_output` MCP tool, which writes `stage_runs.output` directly and is used as-is, or a fenced JSON block scraped from the session transcript when the tool was unavailable. Both end in the same column, so afterwards nothing distinguished them — and the pipeline's single most common failure, `agent did not produce a ```json output block`, means *both* channels failed rather than that the agent botched a format. `POST /api/channel-stage-output` now records `stage_output_submitted` against the task, naming the stage run, stage and iteration, so the split between the two channels is countable. Rejected submissions record nothing, and a failing audit write is logged rather than failing an otherwise accepted result.

### Security

- **A stage run's MCP credentials now end when its agent does, on every path that ends it.** Per-stage-run keys are revoked on the transitions that end a run (done, fail, next, both iterate branches), but three paths ended an agent without ending its run, leaving a usable key until `expires_at`:
  - **Cancelling a task** wrote the terminal stage only on the task row, so the abandoned agent's stage run stayed `running` and kept its credential. Both cancel routes — `DELETE`-style cancel on the HTTP handler and the `task_cancel` MCP control tool — funnel through `NotifyTaskTerminated`, which now ends every non-terminal stage run of the task as `cancelled`. A run that already reached its own outcome is left untouched.
  - **Requeueing a run** (rate-limit or infra retry) left its key valid, and once `sweepRequeueableRuns` promoted the run back to `pending` the respawn minted a **second** key for the same `stage_run_id` — two concurrently valid credentials, of which only the newer was ever revoked. A requeue is only ever decided from a completion result, so the agent has already exited; the run is now released at the transition.
  - **`SpawnResult.Cleanup` had no production caller.** It deletes the temp `--mcp-config` file carrying the run's bearer token, and the settings allow-list entries the spawn wrote. `stage_handlers.go` read `.PID` off the result and discarded the rest, so those files accumulated in `os.TempDir()/dashboard-<uid>/` for the life of the machine. The file is `0600` inside a `0700` per-uid directory, so this was never an exposure to other OS users — but every agent the dashboard spawns runs as that same user and can read another run's token with `Bash` or `Read`. Cleanup is now registered per stage run and runs at the same moment the credentials are revoked. This narrows the window from unbounded to the agent's own lifetime; it does not isolate concurrent agents from each other, which the same-uid process model cannot do.

  The registry is in-process: a server restart drops pending entries and leaves those files behind, where `expires_at` remains the only cap.

- Go toolchain raised to 1.26.6 (pinned in `.go-version`), clearing five standard-library vulnerabilities `govulncheck` reports against 1.26.5: [GO-2026-6218](https://pkg.go.dev/vuln/GO-2026-6218) quadratic complexity in `net/url`'s `resolvePath`, [GO-2026-6090](https://pkg.go.dev/vuln/GO-2026-6090) unbounded post-handshake messages in `crypto/tls`, [GO-2026-6089](https://pkg.go.dev/vuln/GO-2026-6089) `ReadHeaderTimeout` not applied to the unencrypted HTTP/2 check in `net/http`, [GO-2026-5972](https://pkg.go.dev/vuln/GO-2026-5972) unbounded recursion in `encoding/asn1`, and [GO-2026-5026](https://pkg.go.dev/vuln/GO-2026-5026) Punycode label handling in `golang.org/x/net/idna`. No code changed: `govulncheck` evaluates the standard library of the toolchain in use, so the pin is the fix.
- `plugins/oauthkit` is now vulnerability-scanned. It was present in the test and lint matrices but absent from the security matrix, so `govulncheck` had never run against the module that holds the shared OAuth CSRF and session handling. Deriving all matrices from one source closes the gap and prevents the next one.
- Dependabot now covers every Go module. It was configured for four (`server`, `sdk`, `github-oauth`, `office365-oauth`) out of eight, leaving `desktop`, `oauthkit`, `voice-whisper`, `voice-webspeech` and `anthropic-spawner` without dependency updates.
- Go toolchain raised to 1.26.5 (pinned in `.go-version`), clearing [GO-2026-5856](https://pkg.go.dev/vuln/GO-2026-5856) / CVE-2026-42505 in the standard library's `crypto/tls`: TLS handshakes using Encrypted Client Hello could be de-anonymized by a passive network observer, because pre-shared-key identities were disclosed in the unencrypted client hello. `govulncheck` evaluates the standard library of the toolchain actually in use, not the `go` directive, so the pin is what closes this.
- `golang.org/x/text` raised to v0.39.0, clearing [GO-2026-5970](https://pkg.go.dev/vuln/GO-2026-5970) — an infinite loop on invalid input, reachable from the ent migration path.
- `PATCH /api/settings/*` is authenticated — it changes security-sensitive settings such as `auth.mode`, `git.allowPush`, and `worktree.force`. It was described here as admin-authorized; that is not what ships. The admin gate was removed across the router because nothing ever wrote `is_admin`, so it rejected every authenticated user and passed everything through in bypass mode. Under the default `auth.mode = none` these routes are reachable by any local process on the loopback interface with a matching `Origin` header — the posture is written up in [Authentication and the local-trust default](docs/guides/security.md#authentication-and-the-local-trust-default), and making the role grantable is tracked in [#427](https://github.com/lx-wnk/Agent-Dashboard/issues/427).
- The agent bash allow-list filter now also rejects output redirection (`>`/`>>`/`<`), single-`&` backgrounding, and newline command separators, closing allow-list-widening patterns that could append to shell startup files or chain an extra command past the first-token check.
- Provider session discovery now skips symbolic links, so a crafted symlink placed under a provider directory can no longer be read as a session file (arbitrary-file-read guard).
- The dashboard-channel plugin now validates the `dashboard_reply` message argument and caps inbound `POST /message` bodies (64 KiB).

### Removed

- `/api/quota` handler and `usage-data/*.json` file reader (Claude Code never writes that file; the endpoint always returned null).
- Pruned unused TS-era dependencies never imported by the shipped app: `express`,
  `nodemailer`, `web-push`, `cookie-parser`, `supertest`, and their `@types/*`.
- Inert `permissions` and `plugin.json slots[]` manifest fields removed from the `Descriptor` Go type and `plugin.schema.json`. Both were parsed but never enforced or consumed — a security-shaped field with no enforcement is misleading. Slot bindings for UI extensions are declared in `ui-manifest.json` (authoritative since SP4a); the `plugin.json` copy was a divergeable duplicate. Old manifests carrying these fields still load correctly (`additionalProperties: true`). The unused `Registry.AllWithCapability` method is also removed.

### Fixed

- Plugin enable/disable in the UI now calls the live lifecycle endpoints (`POST /api/plugins/{id}/activate` and `/deactivate`) instead of the `PATCH /api/settings/plugins-enabled/{id}` endpoint that was removed in SP2. Plugins with the `auth_provider` capability still require a server restart after activation; the UI surfaces this with a notice.
- Terminal worktree cleanup no longer force-removes a finished task's worktree
  when its branch still holds unpushed commits or uncommitted changes. Previously,
  if git-push was disabled (no `DASHBOARD_ALLOW_GIT_PUSH=true`) or a push failed,
  finalization committed only locally and the subsequent cleanup discarded the
  branch — orphaning the work as dangling commits. Cleanup now detects unpushed
  work and retains the worktree (audited as `worktree_retained_unpushed`) instead.
- Plan-review gate: opening a `plan_review` task via deep-link, Spotlight, or
  cross-modal navigation now routes to the plan-approval panel instead of the
  generic task modal — the routing rule that previously lived only in the
  pipeline-board click handler is now applied to every navigation path. (#221)
- Plan-review panel now refreshes while a plan is still generating instead of
  showing "No plan output available yet" indefinitely: it polls the plan status
  until the gate settles, and re-fetches when reopened for a different task. (#222)
- Agent cards no longer show "No output yet" while an agent is actively working —
  the card now falls back to the current action / last tool when there is no
  assistant text yet.
- Startup: databases created before the per-stage pipeline config landed stored
  `pipeline_configs` as a bare (key, value) table; the new `(project_id, key)` index
  forced a table rebuild that failed with `NOT NULL constraint failed: ...id`. The DB
  layer now rebuilds the legacy table into the scoped `(id, key, project_id, value)`
  shape before auto-migrate (id backfilled from key, existing settings preserved),
  so existing installs upgrade cleanly. Idempotent (PR follows #206).
- Pipeline: a task reaching a terminal stage (`done`/`cancelled`) left its git
  worktree on disk, keeping its `source_branch` checked out. Because the
  duplicate-branch guard only inspected non-terminal tasks, a new task assigned the
  same branch then failed worktree creation. Terminal tasks now auto-remove their
  worktree (force, freeing the branch and reclaiming disk; cancellation discards
  uncommitted work, audit-logged as `worktree_removed`). The branch-collision
  preflight is now authoritative against git (`git worktree list`), so a branch held
  by any leftover or manual worktree yields a descriptive error on task create/refine
  and at worktree creation.
- Performance: the cost heatmap resolved chart color tokens once per cell
  (10 DOM probes × 168 cells per render); it now resolves them once per render and
  splices the per-cell alpha, which also stops the component test from timing out
  on slower CI runners.
- Pipeline: a task whose git worktree could not be created (e.g. its `source_branch`
  was already checked out by another worktree) silently stalled forever in
  `implementation` — the orchestrator returned a bare error before any stage run was
  created, so the picker just retried every tick with nothing surfaced in the UI.
  Worktree-creation failures are now recorded as a failed stage run and follow the
  normal retry/requeue path.
- Pipeline: creating or refining a task now rejects a `source_branch` already used by
  another active (non-terminal) task, preventing the duplicate-branch state that caused
  the silent stall (one branch can back at most one worktree).
- Accessibility: the light-mode `--fg-faint` text token now meets WCAG 2.2 AA
  contrast (4.97:1) on raised surfaces; it previously fell to 4.34:1 on
  `--raised`, below the 4.5:1 threshold at the small sizes used across the UI.
- Accessibility: the login gate now uses a `<main>` landmark and an `<h1>`
  heading, moves keyboard focus to the login control when it appears, and
  announces OAuth failures (`?error=`) via a `role="alert"` region.
- Worktree panel now emits a `change` event after create/remove so the task view
  can react; previously the action ran but the parent was never notified.
- Production build now embeds the real Vue SPA. `vite build` writes to
  `server/frontend/dist` (the `go:embed` source); previously it emitted to the
  repo-root `./dist`, so `task build` silently shipped the placeholder frontend.

### Security

- Resolve all `pnpm audit` advisories: bump `dompurify` to `>=3.4.11` (the only
  production-reachable one) and `vite` to `>=6.4.3`; pin transitive `undici`
  (`^7.28.0`), `esbuild`, `@babel/core`, and `brace-expansion` via workspace
  overrides. `pnpm audit` reports no known vulnerabilities.
- Hardened the live-injection endpoint (`POST /api/agents/{pid}/message`): rate
  limiting, audit logging, per-session token rotation, and control-character
  sanitization (PR #188).

[Unreleased]: https://github.com/lx-wnk/Agent-Dashboard/commits/main
